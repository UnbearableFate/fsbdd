# S1-06：Torch/HF Learner Inner Training Core

## 元数据

| 字段 | 值 |
|---|---|
| Stage | stage1 |
| 前置 loops | S1-00, S1-01 |
| 规范条款 | LEARN-01, LEARN-02, PROG-04, PROG-05, TEL-01, TEL-04 |
| Acceptance | 本 loop 为后续 acceptance 提供组成证据 |
| 最高资源级别 | Miyabi 1 compute node |
| 9 节点门禁 | 不适用（基础训练系统尚未完成） |

## 单一目标

实现单GPU、单进程、独立数据shard的自定义HF causal LM训练循环，暴露完整optimizer-step安全边界、tokens/loss/throughput与per-fragment counters。

## 非目标

- 不发布proposal
- 不采用global fragment
- 不使用DDP/Trainer控制global_step

## ORIENT

Codex 在修改前必须落盘：

- 当前 source spec hashes、branch/base commit 与 dirty state；
- 前置 loops 和 acceptance 是否有可复算证据；
- 当前唯一 failing gap；
- 预计修改文件与不会修改的接口；
- 本 loop 的资源、walltime、model/data/config；
- 适用时 compatible 9N baseline ID、loss/runtime阈值和fault/heterogeneity overlay；
- `miyabi-development` commit 与 host routing（涉及 Miyabi 时）。

ORIENT 结果不完整时不得进入 RED。

## SPECIFY / RED

- 真实tiny HF model/data 10-step test：参数不变、loss缺失或NaN时失败。
- gradient accumulation fixture证明micro-batch不增加local step。
- 扫描运行时确保未初始化torch.distributed process group。

保存失败命令、exit code、fixture/config、seed、stdout/stderr。RED 必须在错误或缺失实现上确实失败。

## IMPLEMENT / GREEN

- 加载冻结model/tokenizer/dataset manifest；AdamW inner optimizer；scheduler。
- 每完整optimizer.step后emit safe-boundary event并增加各fragment local steps/tokens。
- structured JSONL loss/throughput；独立learner seed/shard。

只修当前 RED；额外重构或新策略拆成后续 loop。

## HARDEN

- bf16/fp32（环境支持时）、grad accumulation、padding token count、resume-from-global initialization。
- 1-node真实160M 10-step smoke；offline assets。
- 慢数据/empty batch/overflow错误传播。

低层检查未通过时不上更大拓扑。

## Miyabi Final Gate

不适用（基础训练系统尚未完成）

适用时按 `docs/06_8L1S_50X10_GATE.md` 产生新的、不可复用的 run package。队列等待不计 active runtime；所有阈值在提交前冻结。

## CHECK

- Checker核对loss对应真实参数更新。
- 核对每learner没有共享optimizer/RNG/data cursor。

Checker 使用新上下文和 `templates/CHECKER_REPORT.md`，不得仅复述 Maker 结论。

## PERSIST 输出

- learner core
- tiny/real 10-step evidence
- data/model manifests
- local step/token semantics report

另必须更新：

- `plans/PROGRESS.yaml`
- `plans/ACCEPTANCE_TRACEABILITY.csv`
- loop evidence index/checksums
- 下一最小 failing gap

## 完成定义

- RED 在错误实现上失败；
- GREEN 与 HARDEN 全通过；
- 本 loop 相关 requirement 未被隐式改写；
- 适用时新的 9N 50×10 门禁通过；
- Checker 为 `PASS` 或允许关闭的 `PASS_WITH_FOLLOWUPS`；
- 证据、config、commit 与进度已持久化；
- 没有未登记 blocker。
