# S4-04：Inverse-staleness Weighting、Same-base Consumption 与 Latest Reorder

## 元数据

| 字段 | 值 |
|---|---|
| Stage | stage4 |
| 前置 loops | S4-03 |
| 规范条款 | STALE-05, STALE-08, STALE-10, PROP-04, PROP-06 |
| Acceptance | A-STALE-05, A-PROP-04, A-PROP-05 |
| 最高资源级别 | Miyabi 2/9 nodes |
| 9 节点门禁 | 强制：本 loop 结束前提交新的 Miyabi 8 learners + 1 syncer、9 distinct compute nodes、H=50、target_global_cycles=10 run；loss/runtime/topology/protocol gates 全部通过。 |

## 单一目标

在selected set上应用 `tokens/(1+lambda*s)`，正确处理未选proposal、same-base trajectory一次消费与延迟旧publication不回退latest。

## 非目标

- 不做lambda消融结论
- 不把未选择视为消费
- 不允许同base靠新sequence重复贡献

## ORIENT

Codex 在修改前必须落盘：

- 当前 source spec hashes、branch/base commit 与 dirty state；
- 前置 loops 和 acceptance 是否有可复算证据；
- 当前唯一 failing gap；
- 预计修改文件与不会修改的接口；
- 本 loop 的资源、walltime、model/data/config；
- 适用时 compatible 9N baseline ID、loss/runtime阈值和fault/heterogeneity overlay；
- `miyabi-development` commit 与 host routing（涉及 Miyabi 时）。

ORIENT 结果不完整时不得进入 RED。

## SPECIFY / RED

- weight property/golden先红。
- 同base sequence replacement在一次成功后再被消费失败。
- 旧publication晚完成覆盖latest失败。
- 未选择proposal被错误consume失败。

保存失败命令、exit code、fixture/config、seed、stdout/stderr。RED 必须在错误或缺失实现上确实失败。

## IMPLEMENT / GREEN

- 计算/记录raw r与normalized w；fp32。
- publication成功原子推进sequence/base frontiers。
- latest monotonic guard；未选保持eligible直至自然失效。

只修当前 RED；额外重构或新策略拆成后续 loop。

## HARDEN

- lambda 0.5/1/2、tokens extremes、mixed s。
- reordered completion、retry、restart。
- 9N检查fresh/stale weight mass与consumption。

低层检查未通过时不上更大拓扑。

## Miyabi Final Gate

强制：本 loop 结束前提交新的 Miyabi 8 learners + 1 syncer、9 distinct compute nodes、H=50、target_global_cycles=10 run；loss/runtime/topology/protocol gates 全部通过。

适用时按 `docs/06_8L1S_50X10_GATE.md` 产生新的、不可复用的 run package。队列等待不计 active runtime；所有阈值在提交前冻结。

## CHECK

- Checker从raw metadata重算weights/frontiers。
- 9N无duplicate contribution。

Checker 使用新上下文和 `templates/CHECKER_REPORT.md`，不得仅复述 Maker 结论。

## PERSIST 输出

- weight/consumption implementation
- A-STALE-05/A-PROP-04/05 evidence
- new 9N stale gate

另必须更新：

- `plans/PROGRESS.yaml`
- `plans/ACCEPTANCE_TRACEABILITY.csv`
- loop evidence index/checksums
- 下一最小 failing gap

## 完成定义

- RED 在错误实现上失败；
- GREEN 与 HARDEN 全通过；
- 本 loop 相关 requirement 未被隐式改写；
- 适用时新的 9N 50×10 门禁通过；
- Checker 为 `PASS` 或允许关闭的 `PASS_WITH_FOLLOWUPS`；
- 证据、config、commit 与进度已持久化；
- 没有未登记 blocker。
