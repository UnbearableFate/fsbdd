# S4-02：Old-base Displacement 与 Staleness Boundary/Rejections

## 元数据

| 字段 | 值 |
|---|---|
| Stage | stage4 |
| 前置 loops | S4-01 |
| 规范条款 | INV-06, INV-07, STALE-01, STALE-04, STALE-10, PROP-07 |
| Acceptance | A-STALE-01, A-STALE-02, A-STALE-03, A-STALE-04 |
| 最高资源级别 | Miyabi 2/9 nodes |
| 9 节点门禁 | 强制：本 loop 结束前提交新的 Miyabi 8 learners + 1 syncer、9 distinct compute nodes、H=50、target_global_cycles=10 run；loss/runtime/topology/protocol gates 全部通过。 |

## 单一目标

允许s=1 proposal进入候选并严格使用声明的old base计算 `G^b-L`；拒绝too-stale、missing/future/wrong identity/over-progress。

## 非目标

- 不完成fresh anchor/选择策略
- 不支持S_max>1主路径
- 不改变outer apply point

## ORIENT

Codex 在修改前必须落盘：

- 当前 source spec hashes、branch/base commit 与 dirty state；
- 前置 loops 和 acceptance 是否有可复算证据；
- 当前唯一 failing gap；
- 预计修改文件与不会修改的接口；
- 本 loop 的资源、walltime、model/data/config；
- 适用时 compatible 9N baseline ID、loss/runtime阈值和fault/heterogeneity overlay；
- `miyabi-development` commit 与 host routing（涉及 Miyabi 时）。

ORIENT 结果不完整时不得进入 RED。

## SPECIFY / RED

- 复用Stage0错误公式反例，production path必须对齐oracle。
- boundary matrix s=0,1,Smax,Smax+1/future。
- missing/wrong content identity和progress cap fixtures。

保存失败命令、exit code、fixture/config、seed、stdout/stderr。RED 必须在错误或缺失实现上确实失败。

## IMPLEMENT / GREEN

- eligibility打开S_max=1；lookup exact base payload。
- 统一pseudo-gradient函数；typed rejection reasons。
- 运行时trace记录current/base/staleness/displacement norm。

只修当前 RED；额外重构或新策略拆成后续 loop。

## HARDEN

- current/base参数明显不同；corrupt base；race中version前进。
- same proposal在选择前变too-stale。
- 9N注入速度差使真实s=1出现。

低层检查未通过时不上更大拓扑。

## Miyabi Final Gate

强制：本 loop 结束前提交新的 Miyabi 8 learners + 1 syncer、9 distinct compute nodes、H=50、target_global_cycles=10 run；loss/runtime/topology/protocol gates 全部通过。

适用时按 `docs/06_8L1S_50X10_GATE.md` 产生新的、不可复用的 run package。队列等待不计 active runtime；所有阈值在提交前冻结。

## CHECK

- Checker从payload重新算一个s=1 gradient。
- 9N gate必须观察eligible/selected stale或若本loop只到eligibility至少有可验证s=1候选；不得mock。

Checker 使用新上下文和 `templates/CHECKER_REPORT.md`，不得仅复述 Maker 结论。

## PERSIST 输出

- stale displacement
- boundary/rejection matrix
- A-STALE-01..04 evidence
- new 9N stale gate

另必须更新：

- `plans/PROGRESS.yaml`
- `plans/ACCEPTANCE_TRACEABILITY.csv`
- loop evidence index/checksums
- 下一最小 failing gap

## 完成定义

- RED 在错误实现上失败；
- GREEN 与 HARDEN 全通过；
- 本 loop 相关 requirement 未被隐式改写；
- 适用时新的 9N 50×10 门禁通过；
- Checker 为 `PASS` 或允许关闭的 `PASS_WITH_FOLLOWUPS`；
- 证据、config、commit 与进度已持久化；
- 没有未登记 blocker。
