# Evidence Index: S1-13

## Current status and identity

- Loop status: implementation and hardening; Stage 1 is not closed.
- Latest Checker-authorized candidate: `d9c3f014916666013e3d2348f3bd5d22d4ae486a`
  on `codex/S1-13-stage1-close-9n`, scoped to exactly one one-node preflight.
  Job `2392798.opbs` consumed that attempt and failed one positive test fixture;
  no learner/syncer or L2 workload ran. The earlier complete targeted suite
  remains PASS at `bac11aeae6c730fc25712bdef38190a38eb4e8da`.
- Code organization boundary: production FS-based Decoupled DiLoCo code is
  under `src/fsbdd/diloco/`; evidence, stress, experiment, and stage support is
  under `src/fsbdd/auxiliary/`.
- Source hashes: Research Plan `2c0db314...d835`; Stage 0-4 Spec
  `1ea71fb6...a7d`.
- Miyabi skill commit: `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`.
- Frozen 9N config SHA-256:
  `a466e534ea1a897e38d22aae872b5f41a242e32c29c71dd26c08dcf649aacfc2`.
- Frozen long-run config SHA-256:
  `198a2b8e9497653128295b03c550136c9c7c94fba4c7d9b4f31401156254a5aa`.
- Gate contract SHA-256:
  `8fb3734eaaad876b8ceaa3dc8124b32f752ac9ba1611944e16c31c2e4f1a3f84`.
- Evidence contract SHA-256:
  `83f5a47d2afd7f70ab1a1fe98887dbf47a361e8799e8c10f9869f39582ae1a57`.
- Reduced correction-reproduction contract SHA-256:
  `bc6b8479bdc9a61e813f571fdefe839d7856dc41ef14a5475499a31231948fb7`.
- Reviewed failure ledger SHA-256:
  `812aebb9033e8a6017bb6e8de9b905ed999de362b1047177e0f6219e7d30f2c3`.

The only user-owned dirty path is the repository-root `AGENTS.md`; it is not
part of any checked candidate. Formal runs continue to use a clean detached
worktree bound to an exact commit.

## Preserved failed experiment

- Experiment ID: `s1-13-long-smoke-20260715T161208Z-0023c3d`.
- PBS job/queue/group: `2391236.opbs` / `debug-g` / `xg24i002`.
- Code commit: `0023c3d05d978e659f9b11d233728b459fe156aa`.
- Raw evidence:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-13/long-smoke-20260715T161208Z-0023c3d`.
- Raw checksum inventory SHA-256:
  `f620f3af0c5a01eff66a8d81a568902602bc868dec241427f402e8f8988580b9`.
- Scheduler trace SHA-256:
  `258f007a56ea51c7233d154c5f779735a0f1137ad99b3fd53b334b0571497f59`.
- MPI stderr SHA-256:
  `6184385f7a6bbf079b5afa989f1316648a6855f7ae069a0260552c25d7ae49dd`.

The run passed 308 Stage 1 tests and all four learners completed 3,000 local
optimizer steps, for 24,576,000 aggregate input tokens. The syncer exhausted
the frozen 1,200-second active budget at global vector `[35,35,35,35]`, below
the required 59 cycles. It completed 140 contiguous fragment updates; the
exact median update latency was 1.758271361 seconds. The reviewed failure,
root-cause evidence, and expected correction are recorded in
`reports/stage1/S1-13-experiment-failures.json`.

## Review and correction history

After the long-smoke failure, production and auxiliary source were separated,
then the complete production path and active S1-13 support path were reviewed.
Corrections include bounded/canonical POSIX reads, exact record-to-authority
binding, fail-closed proposal/global-state/commit identity checks, removal of
redundant current-base bytes and repeated payload hashing, worker-side proposal
materialization, bounded immutable adoption, selective learner shard
verification, exact token-weighted accumulation, and strict gate accounting.

Every active S1-13 PBS script now installs ERR, nonzero EXIT, and catchable
scheduler-signal failure capture. Commands at risk of PBS walltime use a
pre-walltime timeout. Each failed submission writes an immutable raw JSON
record containing the failure, immediate cause, review-required root cause,
and expected next action; no subsequent experiment is allowed until the
reviewed ledger is updated.

Checker Phase A authorized exactly one identity-bound one-node targeted
regression at `f0bb4fb5cbe6b2115eaf2b63db66a9732b4d70f6`. That job failed; no
learner/syncer runtime reproduction followed it.

## Preserved failed targeted regression

- Experiment ID/job: `fsbdd-s1-13-targeted-2392180.opbs` / `2392180.opbs`.
- Code commit: `f0bb4fb5cbe6b2115eaf2b63db66a9732b4d70f6`.
- Raw evidence:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-13/targeted-f0bb4fb-20260715T180720Z`.
- Result: compile, Ruff, Pyright, and all active Bash syntax gates passed;
  pytest reported 332 passed and 7 failed.
- Checksum inventory SHA-256:
  `a492a0e678ed39d8fd25083115a4ebfc0444ab269b31bf8412b5a017e7108361`.
- Scheduler trace SHA-256:
  `ebdaec75784502b57f975a2bd5626d9102da46651064c3302ce2015d77d0cdf6`.
- Pytest log SHA-256:
  `635bb6561b00b404c786e74ecd9339dff3137833561ecc8326f2b16e1f06a9c8`.
- Automatic raw failure SHA-256:
  `5cb2180aa2ee2eb79e67cd76bf714a6e1a817df35a08ac75978f44ed5b7b6721`.

The reviewed ledger records every failed test, its root cause, and its expected
correction. Candidate `152f0e2` implements those seven corrections. It also
closes both Checker follow-ups: the syncer no longer retains update/inventory
history in live memory, and pre-authorization failure capture can no longer
write into a rejected/reused experiment root. The durable JSONL stream is the
post-run analyzer evidence; live role state retains only bounded scalar counts.

The same Checker then authorized exactly one targeted retry at
`681e013f55dba602ec9bb91b5c9d6bcc516168d5`.

## Preserved failed targeted retry

- Experiment ID/job: `fsbdd-s1-13-targeted-2392261.opbs` / `2392261.opbs`.
- Raw evidence:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-13/targeted-681e013-20260715T182501Z`.
- Result: compile, Ruff, Pyright, and Bash syntax passed. Pytest emitted 263
  completion markers (262 pass, one fail), then PBS killed the incomplete suite
  at 1,842 seconds with exit status `-29`; no node was abnormal.
- Peak scheduler resident memory: `12,222,272 KB`; CPU time: `00:42:31`.
- Checksum inventory SHA-256:
  `99fbc6ec73c92678472f147d45c03cc9f43ba2a867898b85088018173e4f99af`.
- Scheduler trace SHA-256:
  `fd23eaaecb24bc554f2dc199a7b2d4cb91897d9cbfb94bec3294ac31c39ebccc`.
- Partial pytest log SHA-256:
  `8651d3f6dd5c55516bc4c07005bc45cedd49782138bae9678f5078d711840195`.
- Manually backfilled immutable raw failure SHA-256:
  `96c396bbde205404fc0eaf446921a6c48a6c4b1370108d11fb1562b6d0dfdd9b`.

The reviewed cause is twofold. The positive readiness analyzer fixture omitted
the newly exact `logical_syncer_id` field. Separately, the S1-11 concurrent
reader test retained every tight-loop observation until its writer completed;
the history grew linearly and the zero-delay readers could starve the writer.
PBS hard termination also bypassed the ordinary EXIT trap. Candidate
`5b061cb` adds the missing identity plus a negative mutation, converts reader
evidence to bounded distinct states plus scalar counts and throttled polling,
and makes future targeted stalls verbose, faulthandler-attributable, and
pre-walltime-captured.

Maker login-node static checks for candidate `a026505` pass: complete Python
compile, Ruff, Pyright with zero errors or warnings, `git diff --check`, JSON
and YAML integrity, and Bash syntax for every active S1-13 PBS script and its
failure helper. Candidate `a026505` adds only the bounded focused reproduction
script on top of the correction and persisted review state.

## Passed focused correction validation

- Experiment ID/job: `fsbdd-s1-13-targeted-focus-2392542.opbs` /
  `2392542.opbs`.
- Exact code commit: `a02650506593803584a060cb6f839eff14ebbe78`.
- Raw evidence:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-13/targeted-focus-a026505-20260715T191211Z`.
- Result: all 21 selected tests passed in 0.42 seconds. The concurrent-reader
  atomicity test passed in 0.18 seconds; all readiness analyzer positive and
  negative identity cases passed.
- Scheduler result: exit status `0`, walltime `00:00:03`, peak resident memory
  `271,616 KB`, and no abnormal node.
- Checksum inventory SHA-256:
  `1bea86e54941e1e66b2d3e242f10daaf1b04f597ddccabd39eb9c55ece701110`.
- Scheduler trace SHA-256:
  `3970698faa26e5f1d0df4c544e8971adc3cbb8c92ddcc517449e5fb7418526bb`.
- Pytest log SHA-256:
  `6cb1e43432ed70f6f94160b15fa7d588abb15a26d51ed5fbce3554e76c59559e`.
- PBS stderr SHA-256:
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

The same Checker admitted exactly this one-node focused reproduction after
independently reviewing the cause attribution and confirming that bounded
distinct observations preserve the complete atomicity assertion. This pass
validates both corrections implicated by job `2392261`; it does not authorize
the complete targeted suite or any runtime workload.

## Targeted attempt-budget amendment

The frozen targeted budget of three attempts is exhausted by jobs `2392180`,
`2392261`, and the Checker-required focused reproduction `2392542`. The focused
run was deliberately limited to the 21 directly implicated tests, so it cannot
stand in for the complete Stage 1 targeted correctness gate.

Before the fourth submission, the loop state explicitly increased only the
one-node targeted budget from three to four. The same Checker reviewed the
amendment and authorized exactly one complete identity-bound suite. Job
`2392578` consumed that final targeted attempt. No prior job was reclassified,
and the learner/syncer runtime, L2, 9N, long-smoke, and formal-run budgets are
unchanged.

## Passed complete targeted suite

- Experiment ID/job: `fsbdd-s1-13-targeted-2392578.opbs` / `2392578.opbs`.
- Exact code commit: `bac11aeae6c730fc25712bdef38190a38eb4e8da`.
- Raw evidence:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-13/targeted-bac11ae-20260715T192015Z`.
- Result: compileall, Ruff, Pyright with zero errors or warnings, and all active
  S1-13 Bash syntax gates passed; all 342 Stage 1 tests passed in 30.29 seconds.
- Scheduler result: exit status `0`, walltime `00:00:51`, peak resident memory
  `4,961,984 KB`, and no abnormal node.
- Checksum inventory SHA-256:
  `901275dd40eec55585a864e4488f4ee408082ff01ce0c216c5bd2ae25c992c88`.
- Scheduler trace SHA-256:
  `acd02973a1b483e48752d13a501b6ab827df4f8e5563d2f8b252912de74c6319`.
- Pytest log SHA-256:
  `80ceb3e7b0a06ff32d2998d48105d40be7b3e5a3f40f9a74b5801de72b0e5971`.
- PBS stderr SHA-256:
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

This closes the corrected complete targeted correctness gate. It does not yet
validate the long-smoke throughput corrections on the real shared-filesystem
learner/syncer path.

The same Checker independently returned Phase B PASS for job `2392578` and
identified the two-node 1+1 workload as the smallest topology that exercises
proposal publication, syncer readiness, global commit/adoption, and cross-host
shared-filesystem throughput. The one-node learner-only smoke is not a valid
substitute.

## Corrected two-node reproduction preflight

The original `l2_real: 2` budget was consumed by two pre-correction runs: job
`2389739` was terminated after exposing the first runtime defect, and job
`2389813` passed the old path. Neither validates the corrections made after the
failed five-node long smoke. The loop now records a reasoned amendment from two
to three L2 attempts, scoped to exactly one corrected two-node reproduction;
all larger-run budgets remain unchanged and same-Checker preauthorization is
mandatory.

Checker precheck at `54f224d` prohibited submission for four concrete reasons:
the frozen package had no pre-run validator, package validation preceded final
checksum construction, the new MPI/analyzer path had no lower-level dynamic
evidence, and the launcher did not freeze or report CPU binding. No experiment
was submitted after that verdict.

The corrected `pbs/stage1_s1_13_smoke_2n.pbs` now:

- binds a clean detached worktree to `EXPECTED_COMMIT` and the frozen config,
  asset marker, gate contract, and dedicated reproduction contract hashes;
- exclusively creates both shared and result roots before binding failure
  capture;
- launches the 1+1 roles under one two-rank MPI launcher used only for process
  placement, explicitly uses `--bind-to none`, captures `--report-bindings`
  evidence, and applies a 900-second TERM/kill-after supervisor plus 720-second
  role timeout inside the 1,800-second PBS walltime;
- checks exact fragment payload sizes, zero retained-current-base bytes, one
  background materialization trace per published proposal, readiness cache
  miss/hit reconciliation, bounded live state, ten-cycle cadence, and runtime;
- emits code, config, asset, module, scheduler, filesystem, GPU, binding,
  role-map, log, package-manifest, validator, and final checksum evidence; the
  validator verifies the preliminary inventory after manifest creation,
  appends its own digest, then verifies exact retained-file coverage.

A separate initial `reproduction_preflight: 1` budget was registered without
changing any learner/syncer runtime budget. Its one-node job executes only the
new reproduction tests and the exact-clean-commit frozen-package validator;
any passing result must return to the same Checker before the third L2 attempt
can be considered.

## Failed reproduction preflight 2392798

- Exact code commit: `d9c3f014916666013e3d2348f3bd5d22d4ae486a`.
- Raw evidence root:
  `runtime_runs/S1-13/reproduction-preflight-d9c3f01-20260715T201400Z`.
- Result: 9 selected tests passed and 1 failed in 0.41 seconds; scheduler exit
  status 1, walltime 10 seconds, peak resident memory 1,065,920 KB, no abnormal
  node, and empty pytest stderr.
- Passed evidence includes both explicit MPI 1+1 dispatch cases, the analyzer
  and final checksum lifecycle, post-manifest mutation rejection, and the
  two-node static contract.
- Failure: the positive miniature clean-repository fixture omitted
  `src/fsbdd/auxiliary/stage1/reproduction.py`, but the preflight validator
  intentionally hashes that analyzer into its package identity report.
- Expected solution: create that deterministic fixture source and make the
  validator emit a structured blocked result for an absent frozen source/test
  surface. Both corrections are implemented and login static checks pass.
- Evidence hashes: checksum inventory
  `125331c4c5b2dfda01e8b2f2fb3de9a6586927940e62e9e12d6adc11f06ffb3b`;
  pytest log
  `50598d8369858b1daed16271d4c1fbb7edab5b59785242e22752c0241a58a678`;
  tracejob
  `9c434d0472e964c2542881144d837263806bc730633a625a00b499cefb81d409`;
  raw failure
  `a89a449793b0e6c34e85aa6cbc4b59d8ee67810b8e7bcdde080f323cce350408`.

Because the sole registered attempt was consumed, the loop records an explicit
amendment from `reproduction_preflight: 1` to `2`, scoped to one corrected
one-node attempt and leaving all learner/syncer and larger-run budgets
unchanged. The same Checker must authorize the amendment and exact correction.

Login-node static review passes for the corrected script and analyzer:
compileall, Ruff, Pyright with zero errors or warnings, Bash syntax, JSON
integrity, and `git diff --check`. Per Miyabi routing rules, the new analyzer's
dynamic tests and the real workload have not been executed on the login node.

## Next admissible evidence

1. Return job 2392798's immutable evidence, reviewed failure, exact correction,
   and reasoned second-attempt amendment to the same Checker.
2. If authorized, submit exactly one corrected one-node dynamic preflight.
   Preserve any failure automatically and review its cause and expected
   solution in the failure ledger before another experiment.
3. If the preflight passes, return its complete evidence to the same Checker;
   submit the two-node reproduction only after a new explicit authorization.
4. A new 9N or formal long experiment remains prohibited until the reduced
   reproduction passes and the preceding failure's expected solution is
   validated.

## Requirement and acceptance links

| IDs | Current evidence |
|---|---|
| INV-01, INV-08 | production/auxiliary architecture boundary and exact immutable record-to-state authority binding |
| PROG-02 | failed smoke preserves exact learner steps, tokens, contiguous update count, and final version vector |
| TEL-01..04 | structured role logs, scheduler trace, checksums, failure ledger, and automatic failure capture |
| A-LEARN-01, A-PERF-01 | not yet closed; reduced runtime correction validation, 9N gate, and formal long run remain pending |

## Known limitations

- The preserved smoke is a failed preflight and cannot support a Stage 1 PASS.
- The focused correction verification and complete targeted suite pass, but
  no corrected real learner/syncer runtime reproduction has run.
- The performance root-cause corrections have no reduced real-runtime evidence
  until the subsequent Checker-authorized reproduction completes.
- No 9N or formal one-billion-token evidence package exists yet.
