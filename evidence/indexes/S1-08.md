# Evidence Index: S1-08

## Identity

- Checked candidate: `b0267c68749659744ee40a17574698095b069476` on
  `codex/S1-08-latest-adoption`
- Source hashes: Research Plan `2c0db314...d835`; Stage 0-4 Spec
  `1ea71fb6...a7d`
- Frozen config SHA-256:
  `8a49b0c26250404becb9e6f29285553db735dbde15174bdeabc4e768272150b0`
- Miyabi skill: `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`
- PBS job/queue/group: `2385842.opbs` / `debug-g` / `xg24i002`;
  exit 0, no abnormal nodes, 42 seconds
- Actual roles: mixed-version learner `mg0020` with one GH200 GPU; global
  fragment writer `mg0022` with no Torch import and zero GPU memory
- Selected raw evidence:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-08/formal-l2`
- Package validator: `admissible`, 28 listed = 28 actual payload files,
  manifest `12695512...e34f`
- Run identity: `s1-08-2385842.opbs`; PBS qtime
  `2026-07-15 13:16:58 JST` = manifest `2026-07-15T04:16:58Z`

All state, role, config, source, skill, scheduler, path, and clean candidate
identities reconcile. All 29 checksum entries verify independently, every
packaged stderr file is empty, and PBS stderr is empty.

## RED and correction history

The retained semantic RED exits 1 because a naive whole-checkpoint adoption
changes a non-target fragment, a full-vector alignment guard blocks a valid
mixed-version model, and clearing Adam state destroys inner moments.

The first targeted L1 run preserved a two-test failure caused by hashing a
scalar Adam step tensor as raw bytes. The corrected path passed seven tests.
Checker Phase A then blocked candidate `e1d8afd` because a temporarily
unreadable named payload stopped the background worker permanently. Candidate
`b0267c6` adds typed `PublicationNotReady`, retries only that eventually
readable condition, retains fatal wrong-identity/content-regression behavior,
and passes an actual background-worker transient-read-to-adoption fixture. The
corrected eight-test L1 run passed before the sole L2 submission.

## GREEN and hardening

One background worker polls only the four fixed current slots, verifies each
complete compound state, and retains at most one latest-wins pending value per
fragment. At a completed zero-gradient optimizer boundary, adoption validates
and decodes the full target fragment before copying only its ordered
parameters, then updates its base identity and resets only that fragment's
local-step/token counters. No version-vector equality condition exists.

The writer publishes fragment 0 versions 1, 2, and 3 before enabling polling,
plus fragment 2 version 1. At boundary 2 the learner directly adopts fragment
0 from 0 to 3 and independently adopts fragment 2 from 0 to 1, yielding
`[3,0,1,0]`. The two target post-hashes equal the verified writer payloads;
every non-target hash is unchanged. All optimizer-state hashes and initialized
entry counts `[13,12,12,15]` are identical before and after both adoptions.
Only target counters reset and proposal sequences remain unchanged.

Steps 3 through 20 are 18 consecutive real HF optimizer steps under the mixed
vector `[3,0,1,0]`, all with finite token-weighted loss. The learner begins
before the first writer publication and has no application collective, socket,
RPC, or whole-vector barrier. Polling completes 98 cycles and 392 fixed-slot
reads, ignores 389 repeated-current observations, ends with no pending state or
error, and never exceeds one pending state per fragment. Deterministic tests
also cover malformed payloads, active gradients, wrong map identity,
eventually-readable payloads, latest replacement, tied ownership, and immutable
old-base in-flight proposal coexistence. The full formal Stage 1 suite reports
160 passed tests.

## Adoption lag and transfer trace

Writer publication-completion and learner adoption wall-clock rows join by
fragment, version, and content identity. Fragment 0 reports 0.763858 seconds
and fragment 2 reports 0.780523 seconds from publication completion to actual
copy completion; each has one extra completed local step before adoption.
Their verified FS-to-CPU reads take 0.003024 and 0.002680 seconds, while the
target CPU-to-GPU copies take 0.000410 and 0.000375 seconds.

## Files

| Class | Path | SHA-256 / result |
|---|---|---|
| manifest | `manifest.json` | `12695512c3b0b69bf40810b1fd09bb2c3c6b14ec41971403664ba6e41ec9e34f` |
| checksums | `checksums.sha256` | `8b5f6e038680e0320492495b46fb607a7b9011e799f8da7c55452bfecd070a75` |
| validator | `validation-summary.json` | admissible 28/28; `c066ff36...2a86` |
| adoption summary | `analysis/adoption-summary.json` | pass; `4dc1194d...bde` |
| tests | `stdout/pytest.log` | 160 passed; `d050ec50...2289` |
| requirements | `analysis/requirements.json` | 8/8 complete; `a3ab69ab...d387` |
| learner role | `runtime/roles/mixed_version_learner.json` | `e03d5ea6...4419` |
| writer role | `runtime/roles/global_fragment_writer.json` | `9f54f90d...86f1` |
| Checker | `reports/checkers/S1-08.md` | Phase B `PASS`; `83bfdcda...ee3` |

## Requirement and acceptance links

| IDs | Evidence |
|---|---|
| LEARN-08 | complete fixed-slot fetch, transient retry, strict safe boundary, target-only reset and continued training |
| LEARN-09 | positive initialized AdamW entry counts and canonical state hashes preserved exactly |
| LEARN-10, INV-09 | independent `[3,0,1,0]` state and 18 consecutive finite mixed-version steps without alignment |
| LEARN-11 | gated immutable old-base proposal remains unchanged across adoption and later base context |
| GLOBAL-04 | one direct fragment-0 transition from version 0 to version 3 with two skipped intermediates |
| A-LEARN-02 | full before/after per-fragment parameter identities prove only each target changes |
| A-LEARN-03 | checked component evidence for sustained mixed-version training; primary Profile A closure remains S1-12 |

## Reproduction

```bash
PYTHONPATH=src python -m pytest -q tests/stage1
(cd runtime_runs/S1-08/formal-l2 && sha256sum -c checksums.sha256)
PYTHONPATH=src python -m fsbdd.cli evidence validate runtime_runs/S1-08/formal-l2
```

Torch/HF runtime commands must execute on a Miyabi compute node, not a login
node.

## Known limitations

- The formal L2 run proves one real learner and one independent writer; the
  multi-learner syncer and complete Profile A closure remain later Stage 1
  loops.
- Full identity auditing intentionally synchronizes all tiny formal-model
  hashes as evidence instrumentation. Production adoption defaults to
  target-only copy without that audit; transfer-overlap performance gates are
  Stage 2 work.
- This loop does not replace the Stage 1 M=4/160M/at-least-one-billion-token
  run or the first 8+1 gate in S1-13.
