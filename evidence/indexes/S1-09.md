# Evidence Index: S1-09

## Identity

- Checked candidate: `40a1707bb5b3716b98fee5ebc9c89fb7af6f404f` on
  `codex/S1-09-syncer-readiness`
- Source hashes: Research Plan `2c0db314...d835`; Stage 0-4 Spec
  `1ea71fb6...a7d`
- Frozen config SHA-256:
  `8ff3fe364fca10bb48fe80ebf0bcede63163731e47cc26fc87512b298e3c225d`
- Miyabi skill: `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`
- PBS job/queue/group: `2386129.opbs` / `debug-g` / `xg24i002`;
  exit 0, no abnormal nodes, 37 seconds
- Actual roles on distinct hosts: syncer scheduler `mg0045`; proposal writer
  `mg0046`
- Selected raw evidence:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-09/formal-l2`
- Selected scheduler audit:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-09/scheduler-2386129-audit2`
- Package validator: `admissible`, 28 listed = 28 actual payload files,
  manifest `fcf07962...7675`
- Run identity: `s1-09-2386129.opbs`; PBS qtime
  `2026-07-15 14:11:04 JST` = manifest `2026-07-15T05:11:04Z`

All code, config, source, skill, scheduler, role, path, and clean detached
candidate identities reconcile. All 29 checksum entries verify independently,
every packaged stderr file is empty, and the selected PBS stderr is empty.

## RED and correction history

The retained semantic RED exits 1 with four independent failures: a naive
file-count quorum admits duplicates, CPU poll count makes an empty fragment
ready, a hot fragment starves fragment 1, and selection changes with input
order.

Candidate `f030691...` passed local and compute hardening, but Checker Phase A
found that its analyzer accepted one weight record for eight frozen proposals
and that its evidence overstated independent `Q`/`Q_fresh` behavior under
`S_max=0`. Candidate `40a1707...` binds exact proposal/weight cardinality and
correspondence, content and token facts, float32 raw and normalized weights,
and a recomputed selection identity. It also proves the scope-correct
`eligible_distinct == fresh_distinct` invariant for every configured
`Q_fresh` value under `S_max=0`; stale-inclusive independent quorum remains
assigned to S0A-01/S4-03. Checker Phase A recheck returned `ADMISSIBLE`.

## GREEN and hardening

One logical syncer owns a bounded state machine for all four fragments. Each
poll performs exactly `M*F = 32` fixed-slot reads, whether the proposal store
contains only current proposals or 10,000 unrelated history objects. Readiness
uses distinct learners, current fragment authority, fragment-bound consumed
frontiers, `Q`, `Q_fresh`, and an injected nondecreasing monotonic clock.
Quorum loss cancels grace; recovery receives a complete new interval.

Profile A freezes four selections under `M=8`, `F=4`, `Q=8`, `Q_fresh=8`,
`S_max=0`, grace zero, and at most eight contributors. Each selection has
exactly eight distinct fresh learners, complete immutable proposal facts and
float32 token weights. The deterministic cyclic claim order is `[0,1,2,3]`;
the maximum continuously-ready service window is four claims, and concurrent
claims leave exactly one active lease.

The decoupled fixture uses `Q=4`, `Q_fresh=4`, and 0.5-second grace. Its final
five-learner selection includes the arrival before freeze, excludes the
arrival after freeze, and retains an identical selection and weights. Baseline,
history-loaded, restarted, and repeated polls all use 32 reads. Restart rebuilds
from fixed fragment authorities without scanning history and preserves all
selection identities. After installing eight frontier entries per fragment,
the repeated poll rejects all 32 proposals and selects none. Resident frozen
proposal state returns to zero.

Both filesystem-only application roles report `torch_imported=false` and zero
current-process GPU memory before and after their workloads. The complete
validation plane ran the full test suite and an explicit Torch-version probe;
the scheduler therefore records a separate 2,097,152-byte peak on `mg0045`,
zero on `mg0046`, and zero utilization on both. This role-scoped distinction is
the only non-blocking Checker follow-up; no rerun is required. The full formal
Stage 1 suite reports 205 passed tests.

## Files

| Class | Path | SHA-256 / result |
|---|---|---|
| manifest | `manifest.json` | `fcf07962b115544b049ffbddb021a87e28012b73a95687e8079540b9961b7675` |
| checksums | `checksums.sha256` | `7215bc9a8a452e6f9c27412146a03f9ddb338346e83cb7c069579de37111fad5` |
| validator | `validation-summary.json` | admissible 28/28; `d7af6b78...8a08` |
| readiness summary | `analysis/readiness-summary.json` | pass; `2409b3af...77cf` |
| tests | `stdout/pytest.log` | 205 passed; `a79a138f...6d72` |
| requirements | `analysis/requirements.json` | 10/10 complete; `87fe6672...8192` |
| syncer role | `runtime/roles/syncer_scheduler.json` | `32f999bf...9b1b` |
| writer role | `runtime/roles/proposal_writer.json` | `8bdb001e...63af` |
| scheduler audit | `scheduler-2386129-audit2/package-validation.json` | pass; `44081db7...bf4e` |
| Checker | `reports/checkers/S1-09.md` | `PASS_WITH_FOLLOWUPS`; `c969918a...f32f` |

The earlier `runtime_runs/S1-09/scheduler-2386129` directory is retained only
to preserve an audit-command working-directory error. It is not selected
evidence and does not alter the finalized formal package or selected audit.

## Requirement and acceptance links

| IDs | Evidence |
|---|---|
| INV-02, SYNC-01 | one logical syncer owns all fragments and one active lease |
| SYNC-02 | fixed per-fragment authority, readiness, grace, frontier, and bounded resident state |
| SYNC-03, STALE-09 | distinct `Q`/`Q_fresh` readiness with exact `S_max=0` eligible-equals-fresh scope |
| SYNC-04 | Profile A immediate freeze and decoupled fixed grace with quorum loss/recovery |
| SYNC-05 | cyclic `[0,1,2,3]` service and maximum window `F=4` |
| SYNC-09 | immutable frozen proposal facts, weights, content identities, and selection identity |
| A-PROP-02 | four selections each contain one contribution from each of eight distinct learners |
| A-PROP-03 | repeated fixed polling after frontier installation yields zero reselections; atomic publication closure remains S1-11 |

## Reproduction

```bash
PYTHONPATH=src python -m pytest -q tests/stage1
(cd runtime_runs/S1-09/formal-l2 && sha256sum -c checksums.sha256)
PYTHONPATH=src python -m fsbdd.cli evidence validate runtime_runs/S1-09/formal-l2
```

Torch/HF runtime commands must execute on a Miyabi compute node, not a login
node.

## Known limitations

- S1-09 freezes selections but does not merge them; streaming direct merge and
  the fragment-wise outer optimizer remain S1-10.
- Frontier installation is proven as a state transition, but atomic parameter,
  optimizer-state, version, and frontier publication remains S1-11.
- Complete numerical end-to-end and Profile A closure remain S1-12, and the
  first 8+1/50x10 baseline remains S1-13.
- Positive-staleness production readiness and independently binding
  `Q_fresh < Q` with stale eligible proposals remain S4-03.
