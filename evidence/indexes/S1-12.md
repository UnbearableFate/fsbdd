# Evidence Index: S1-12

## Identity

- Checked candidate: `33fc1d8c5773273ec203b7ceb722a204978bff4a` on
  `codex/S1-12-profile-a-e2e`
- Source hashes: Research Plan `2c0db314...d835`; Stage 0-4 Spec
  `1ea71fb6...a7d`
- Frozen config SHA-256:
  `686551db4cc556e0ea923f2c1482e3f0ed489b3e5173efa8b0791a069bf36977`
- Evidence/numeric/matrix/escalation contract SHA-256 values:
  `e0cea9df...314`, `b46f0da1...de`, `3c474681...ce3`,
  `a38a5db2...948`
- Miyabi skill: `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`
- PBS job/queue/group: `2388867.opbs` / `debug-g` / `xg24i002`;
  exit 0, no abnormal nodes, 83 seconds
- Actual roles on distinct hosts: learner-00 plus logical syncer `mg0010`,
  learner-01 `mg0012`, learner-02 `mg0013`, learner-03 with slowdown injection
  `mg0014`
- Selected raw evidence:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-12/formal-l2-retry1`
- Selected scheduler evidence:
  `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-12/scheduler-2388867.opbs`
- Package validator: `admissible`, 1,671 listed = 1,671 actual payload files,
  manifest `6395bf83...35cd`
- Run identity: `s1-12-2388867.opbs`; PBS qtime
  `2026-07-15 21:28:56 JST` = manifest `2026-07-15T12:28:56Z`

All code, config, source, skill, scheduler, role, path, and clean detached
candidate identities reconcile. Every package checksum and scheduler checksum
verifies, every packaged stderr file is empty, and selected PBS stderr is
empty.

## RED and correction history

The selected semantic RED at
`runtime_runs/S1-12/red-92b9006-semantic` exits 1 with the three intended
failures: unweighted proposal merging disagrees with the token-weighted
Stage-0 oracle, an evaluator that chases latest mixes versions after capture,
and a peer barrier lets one half-speed learner reduce unaffected throughput.

The initial implementation integrated Profile A, frozen evaluation, progress,
stop, and a four-rank HF workload. Checker Phase A blocked candidate `cbf182c`
because speed evidence was not produced by the integrated production path,
numeric analysis trusted trace arrays and omitted outer momentum, and
evaluation access counts were asserted. Candidate `395ff04` corrected these
gaps; targeted job `2388775.opbs` passed 21 tests and role checks, and an exact
preflight package was admissible `18/18`.

Formal job `2388799.opbs` completed the runtime and 288 tests but the analyzer
rejected a producer field that serialized an all-false predicate rather than
the actual false distributed state. The failed evidence remains immutable at
`runtime_runs/S1-12/formal-l2`. Analyzer replay `2388820.opbs` appropriately
rejected the old record; full role-producer reproduction `2388832.opbs` passed
after the serialization correction. The same Checker returned
`ADMISSIBLE_RETRY` for `33fc1d8`, and the single authorized retry passed.

## GREEN and hardening

`ProfileAFragmentExecutor` performs readiness selection, streaming
token-weighted merge, fragment-wise outer update, and compound publication.
The numeric run stores the encoded pre-state, four proposal payloads, and
compound successor for both fragments across 50 contiguous cycles. The
analyzer decodes all 100 updates and recomputes parameters and outer momentum;
the maximum parameter relative L2 is `1.1958231855191269e-08`, momentum error
is `0.0`, and the last-ten error remains below `1e-6`. The Checker independently
recomputed fragment 0 cycle 37 from raw encoded evidence and obtained parameter
and momentum relative L2 `0.0`.

The four real single-GPU learner ranks run an integrated snapshot publisher,
adoption coordinator, and asynchronous Profile A syncer over common ten-second
intervals. Raw control rates are `[128,128,128,128]` input tokens/s. With only
learner-03 scheduled at half rate, raw rates are `[128,128,128,64]`; every
unaffected absolute relative change is `0.0`. The integrated syncer completes
8 control and 10 injected-phase updates, publications/adoptions remain active,
and every role reports `torch.distributed=false`. All four learners also
continue for five consecutive finite optimizer steps at mixed vector `[1,0]`.

Evaluation first captures an ordered vector plus exact content and authority
identities, then materializes and restarts exclusively from immutable named
payloads. Captured and loaded `[1,1]` identities remain exact while a concurrent
writer advances current authorities to `[5,5]`. The measured restart audit is
one manifest read, two frozen-state reads, and zero current, latest,
unauthorized, or path-escape reads.

The full formal Stage 1 suite reports 288 passing tests. The Checker verdict is
`PASS`.

## Files

| Class | Path | SHA-256 / result |
|---|---|---|
| manifest | `manifest.json` | `6395bf839ee2c4e3dc47729022d448646a21584fc6bc8b803c005c9e3ab835cd` |
| checksums | `checksums.sha256` | `c919e25fc7efa294e56f8d3cfe3753e61d0a6e3c391bd46945ea554acccc88bd` |
| validator | `validation-summary.json` | admissible 1671/1671; `d0b2b444...21d2` |
| Profile A summary | `analysis/profile-a-summary.json` | pass; `a779e0fe...3182` |
| tests | `stdout/pytest.log` | 288 passed; `1d7a43d4...b4f7` |
| requirements | `analysis/requirements.json` | 10/10 complete |
| learner roles | `runtime/roles/learner-00..03.json` | four distinct hosts; pass |
| syncer role | `runtime/roles/syncer.json` | `[50,50]`; pass; `88ebbc2f...ba6` |
| scheduler trace | `scheduler-2388867.opbs/tracejob.txt` | exit 0; `cfbb7ed9...ed13` |
| Checker | `reports/checkers/S1-12.md` | `PASS` |

## Requirement and acceptance links

| IDs | Evidence |
|---|---|
| GLOBAL-05, A-EVAL-01 | immutable vector/content/authority manifest, concurrent successor writer, exact payload restart, and measured complete read audit |
| PROG-01..04 | independently typed learner steps/tokens, fragment counts/accepted contributions, minimum-derived global cycle, and frozen stop policy |
| REPORT-02 | protocol/runtime-only claim with matched communication, compute, and token flags all false |
| A-ALG-01 | 100 encoded authoritative transitions plus independent raw cycle-37 parameter and momentum recomputation |
| A-LEARN-01 | common-interval raw counter rates with integrated publications, adoptions, and syncer commits during half-speed injection |
| A-LEARN-03 | five consecutive finite mixed-version optimizer steps on every learner without alignment wait |

## Reproduction

```bash
PYTHONPATH=src python -m pytest -q tests/stage1
(cd runtime_runs/S1-12/formal-l2-retry1 && sha256sum -c checksums.sha256)
PYTHONPATH=src python -m fsbdd.cli evidence validate runtime_runs/S1-12/formal-l2-retry1
```

Torch runtime commands must execute on a Miyabi compute node, not a login
node.

## Known limitations

- This protocol/runtime acceptance run makes no matched-token quality or
  efficiency claim.
- The M=4, 160M, at-least-1B-token long run and first 8+1/50x10 nine-node
  baseline remain S1-13 and are not replaced by this small Profile A run.
