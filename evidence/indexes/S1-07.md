# Evidence Index: S1-07

## Identity

- Checked candidate: `d267052d08d4e88537e7cd8c5e9a14a13a8d5ae4` on `codex/S1-07-snapshot-publish`
- Source hashes: Research Plan `2c0db314...d835`; Stage 0-4 Spec `1ea71fb6...a7d`
- Frozen config: `f4d5e0bb...a8b8`; runtime model registry `c2a36317...8ef9`; fragment map `df9a3625...d652`
- Miyabi skill: `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`
- PBS job/queue/group: `2385625.opbs` / `debug-g` / `xg24i002`; exit 0, no abnormal nodes, 41 seconds
- Actual roles: GPU snapshot writer `mg0017`; filesystem proposal reader `mg0018`
- Selected package: `/work/xg24i002/x10041/fsbdd/runtime_runs/S1-07/formal-l2`
- Package validator: `admissible`, 28 listed = 28 actual payload files; manifest `5cf0f93c...1af8`
- Run identity: `s1-07-2385625.opbs`; PBS qtime `2026-07-15 12:33:02 JST` = manifest `2026-07-15T03:33:02Z`

The exact clean candidate, config, source, skill, run, scheduler, path, role,
model, and fragment-map identities agree across the manifest, role records,
analyzer, and captured `qstat`. All 29 checksum entries (manifest plus 28
payload files) verify. All packaged stderr files and PBS stderr are empty.

## RED and correction history

The retained semantic RED exits 1 with all three intended failures: a naive
scheduler bursts every fragment at one step, a live parameter view changes
during optimizer mutation, and an in-flight task follows a later base update.

Checker Phase A blocked candidate `5554d6e` because production telemetry grew
with publication history, several counterexamples were incomplete, formal
analysis recorded rather than asserted close predicates, and scratch/scheduler
and role identity preflight was incomplete. Candidate `d267052d` keeps at most
three resident trace records per fragment and streams terminal evidence;
exercises 1,001 captures with constant resident state; proves gradient-time
rejection, bytewise immutable staging, old-base in-flight plus new-base pending
coexistence, restart sequence reservation, replacement/skip, and injected
publication failure/abandonment; fails every formal predicate closed; and binds
scratch, qstat, roles, ranks, job/run/config, and manifest identities. The same
Checker context returned `ADMISSIBLE` before the sole L2 submission.

## GREEN and hardening

The byte-weighted midpoint scheduler derives offsets `[1, 2, 3, 0]` for the
formal fragment byte vector `[25632, 19488, 19488, 25824]`. Exactly one target
fragment is due at each step of unified `H=4`; the exact communication rate is
`90432 / 4 = 22608` bytes per learner optimizer step. The API also reports the
general `sum_f(fragment_bytes_f / H_f)` rational form and has a nonuniform
interval fixture.

Twenty real GPU safe boundaries produce twenty immutable fp32 target-fragment
snapshots. The deliberately gated fragment-zero publication is in flight
before training returns and is released only afterward. Training does not wait
for a reader, syncer, quorum, or the gated filesystem write. The bounded state
never exceeds one in-flight and one pending snapshot per fragment; three old
pending snapshots are replaced, seventeen proposals publish, and final
resident trace state is 4 of the 12-record bound. Transfer telemetry reports
0.01552649 seconds GPU-to-CPU and 0.554284379 seconds CPU-to-filesystem, with no
background error.

Every published/cancelled terminal trace is checked for its due step, local
steps, processed input tokens, bootstrapped global base version/content
identity, exact target-only byte count, parameter digest, and transfer timing.
The remote reader performs 273 fixed-slot poll rounds, observes no partial or
corrupt proposal and no sequence regression, and its four final records match
the writer and capture traces field for field. The final chains are at local
steps 17, 18, 19, and 20 with 544, 576, 608, and 640 processed tokens. The full
Stage 1 suite reports 150 passed tests.

## Files

| Class | Path | SHA-256 / result |
|---|---|---|
| manifest | `formal-l2/manifest.json` | `5cf0f93c9ba99cb59dd0107d13b389b3020a0bffb3d3eabfb63be2556c141af8` |
| checksums | `formal-l2/checksums.sha256` | `3c040c18af8511dfa8d98cc09525689bef6208193351808174061d2b1bf614b7` |
| validator | `formal-l2/validation-summary.json` | admissible 28/28; `a860c44f...d42d` |
| summary | `formal-l2/analysis/snapshot-publish-summary.json` | pass; `c54478e9...be83` |
| tests | `formal-l2/stdout/pytest.log` | 150 passed; `476841bc...b6d2` |
| requirements | `formal-l2/analysis/requirements.json` | 7/7 complete; `4aa49b01...66f7` |
| writer role | `formal-l2/runtime/roles/snapshot_writer.json` | `1ed2ff80...778` |
| reader role | `formal-l2/runtime/roles/proposal_reader.json` | `532b0be9...f4c7` |
| scheduler | `runtime_runs/S1-07/scheduler-2385625` | exit 0, two hosts, no abnormal nodes, 41 s, empty stderr |
| Checker | `reports/checkers/S1-07.md` | Phase B `PASS`; `b9b3601f...049a` |

## Requirement links

| IDs | Evidence |
|---|---|
| LEARN-03, PROG-06 | independently rederived byte-aware offsets, exact unified budget, and nonuniform-interval rational fixture |
| LEARN-04 | strict safe-boundary event/progress/zero-gradient checks plus mutation rejection and immutable staged-byte fixture |
| LEARN-05 | slow publication begins before training return and completes only after release; writer never waits for the reader/syncer |
| LEARN-06 | one in-flight plus one latest-wins pending slot, fixed `3F` telemetry state, replacement/skip/failure/abandonment evidence |
| LEARN-07 | complete target-only fp32 bytes and exact bootstrapped global base, local-step, token, sequence, and checksum chains |
| LEARN-11 | old-base in-flight and new-base pending proposal coexist without metadata rewrite; later proposal publishes on the new base |

## Reproduction

```bash
PYTHONPATH=src python -m pytest -q tests/stage1
(cd runtime_runs/S1-07/formal-l2 && sha256sum -c checksums.sha256)
PYTHONPATH=src python -m fsbdd.cli evidence validate runtime_runs/S1-07/formal-l2
```

## Scope limitations

- Stage 1 permits the finite synchronous target-fragment staging measured here;
  complete GPU/publication overlap and performance acceptance remain Stage 2.
- This loop does not poll or adopt newer global fragments; S1-08 owns adoption.
- The formal run has one learner writer and one remote reader. Multi-learner
  syncer integration and the 8+1 gate begin in later Stage 1 loops.
