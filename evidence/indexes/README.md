# Evidence indexes

Create one tracked index per loop from the execution package's
`templates/EVIDENCE_INDEX.md`. Raw stdout, metrics, payloads, storage snapshots,
and run directories remain outside Git.
