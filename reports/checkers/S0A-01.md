# Checker Report：S0A-01/9dbec27

## Verdict

`PASS`

S0A-01 通过。先前 `reports/checkers/S0A-01-blocked-1.md` 的阻塞项已解除：历史 proposal-ID 集合已替换为按 `(learner, fragment)` slot 保存最后 identity 的固定大小状态，且新的 duration-scaling/M=1 回归覆盖 constant 与 lognormal 两条路径。共享 decision vectors、grace/readiness/same-base 语义、原始 RED 以及更正后的 GREEN/HARDEN 证据均有效。

## 独立输入

- source hashes：`RESEARCH_PLAN.md = 2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`；`STAGE0-4_SPEC.md = fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`。
- loop card：`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/loops/stage0/S0A-01.md`，SHA-256 `65940960b1334d96799a19a7da1fda6424fa3bbdc8cee3cf3c2f48c3554009a4`。
- checker template：SHA-256 `0cf29aa65c6a0d691cdb2661e5cacc3f2beb1619d729e98add082e36539ae297`。
- checked commit：`9dbec27ae562b20174e55e53b84d55b9c2795c6f`；先前 blocked commit `ecbc32a25397fe1c91e314d926c4218078c072f3` 是其 ancestor；`git diff --check ecbc32a..9dbec27` 通过。
- RED：`evidence/raw/S0A-01/20260715T000500Z-red-n4b2c-96de0c07d935`，commit `c6692fde7250905aa5ef965346719fb06ae6a8d9`。
- corrected GREEN：`evidence/raw/S0A-01/20260714T141300Z-green3-n3f8b-933359ca8de4`，commit `9dbec27...`。
- corrected HARDEN：`evidence/raw/S0A-01/20260714T141500Z-harden3-n7a2d-fbdaaee6cf1a`，commit `9dbec27...`。
- 9N package：不适用；Stage 0 且 `CAP_BASE_TRAINING` 尚未建立。

## 重新构建的目标

- 单一目标：以稳定 event queue 模拟 M learners/F fragments 的 H/offset、速度、upload/visibility、distinct-learner Q、fresh anchor、grace、S_max、one-per-learner 与 same-base once-only consumption，并与 Stage 0-C oracle 共享决策向量。
- 受影响条款：`SIM-01`、`SIM-02`、`STALE-01`、`STALE-07`、`STALE-08`、`STALE-09`；HARDEN 同时要求 `INV-08` bounded state。
- Acceptance：`A-SIM-01`。
- 用户门禁：Stage 0 无训练/9N；本 Checker 只做 Git/file/hash/shell/PBS-history 静态检查，未在 login node 运行项目 Python。

## RED 有效性

- 原始 RED 的 3 个确定性 surrogate 均以具体断言失败：按文件数误形成 quorum、visibility 时重写 snapshot base、以及 insertion-order winner；raw unittest exit 为 `1`。
- RED commit 是 checked commit 的 ancestor；RED target 在两 commit 间未变化。失败对应 `SIM-02`、`STALE-01`、`STALE-07`，不是 import error、timeout 或环境失败。

## 代码与协议检查

- scope/data plane：修正仅涉及 Stage 0 simulator/test 与证据 wrapper，并保留 blocked 报告；无训练、storage、Torch/HF 或网络数据面变更。
- shared vectors：simulator conformance evaluator 与 event-loop selection 都调用 Stage 0-C `select_proposals`。共享 JSON 的 7 个 cases 覆盖 duplicate learner、fresh-over-stale、consumed base/sequence、new-base jump、future/too-stale/wrong identity 和 empty boundary；GREEN/HARDEN 全过。
- ordering/readiness：event key 为 `(time, priority, identity)`；候选顺序为 lower staleness → larger exact effective tokens → newer sequence → fixed identity。quorum 与 fresh anchor 均按 eligible distinct learners 计算；grace deadline 时重新选择当时 eligible set。
- consumption：成功 update 才推进固定大小 sequence/base frontier；未选择 proposal 不消费，same-base replacement 由 `consumed_base` 拒绝。
- bounded fix：append-only `accepted_ids` / `counted_rejections` 已删除。`last_accepted_by_slot` 与 `last_counted_rejection_by_slot` 每个 `(learner, fragment)` 至多一个 entry，因此上界均为 `M*F`，不随 duration/update history 增长。结果显式暴露两项 slot 数；M=4/F=4 长运行和 M=1/F=1 的 80s→2000s constant/lognormal scaling tests 均断言该上界。
- deterministic/serialization：per-learner seeded RNG 不依赖初始化顺序；相同 seed、逆序 initialization 结果一致，变更 seed 结果不同；result 可 canonical JSON 序列化且 trace 有 cap。

## 手工 grace-window 重放

对 `test_stale_01__visibility_and_heterogeneity_create_real_stale_events` 的 fragment 1：H=10、offset=5、step intervals `(1, 5/3, 7/3, 3)`、upload=0.5s、visibility=5s、Q=2、Q_fresh=1、grace=1s、S_max=1。learner 0/1 的 base-0 proposals 分别于 `t=10.5`、`t=83/6` 可见；第二项出现时满足 2 fresh，grace 在 `83/6` 打开并于 `89/6` 更新 version `0→1`。learner 2 的 base-0 proposal 到 `103/6` 才可见，故保持真实 `s=1`；其与 learner 3 的 stale proposal因 fresh=0不能单独推动下一 update。learner 0 在 adoption 前产生的后续 base-0 proposal随后以 `consumed_base` 拒绝。该 timeline 符合 STALE-01/08/09 与固定 grace 语义。

## Maker 未列出的反例

1. 固定 M/F/H/delays，仅把 duration 从 80s 扩到 2000s：accepted proposal 数必须增长，而 identity-accounting containers 仍不超过 M*F。新测试覆盖此反例，旧 append-only 实现会失败。
2. M=1 曾是缺失边界；当前 `_step_interval` 的单 learner 分支已由 constant/lognormal 两种 speed model 的 scaling regression 覆盖。

## 运行证据

- 三包均逐项通过 `sha256sum -c checksums.sha256`。checksum-manifest SHA-256：RED `280aa043e539385ed7022454aa45b7eb16811b0144a8671f9cf2a5d635f6e1f1`；GREEN `dcbd6f4832292c5cc97225ee0e8575e433d1c2a226b9a085a673e17e4d526ff1`；HARDEN `7fab8a769218f4f768d22c00bbea86daebd32469b7fc56f9977fa4bb7f9b312e`。
- corrected GREEN/HARDEN 的 digest preimage 可独立复算：`sha256(config_digest_inputs.sha256)` 分别为 `933359ca8de43b88eb2e32a95229c489dc9957b00242c2b54a44c6b470ac61a7` 与 `fbdaaee6cf1a148bffc4e4a1ec933d6784f01ca01f4b9d51b9283b5e162b1abf`，与 manifest 和 run-ID 前缀一致。全部 6/9 个 input snapshots 同时匹配 preimage hashes 与 commit `9dbec27` 的 Git blobs。
- commit/environment binding：GREEN/HARDEN 保存 clean branch status、`9dbec27...`、source hashes、compute host/nodefile、modules与 skill identity；RED 保存 clean `c6692fd...`。PBS history 确认 jobs `2382610`、`2382661`、`2382662` 均 finished/exit 0、单节点 `debug-g`。
- GREEN：7 tests passed；HARDEN：S0C oracle + simulator 共 25 tests passed，含 shared-vector conformance、bounded scaling、determinism 与真实 stale path。
- 原始 RED 不含 digest preimage且其 `Z` timestamp label与实际 Japan-local提交时间不一致；其唯一 ID、checksum、commit binding和特异失败仍可验证。更正后的 closure runs 使用准确 UTC timestamp并保留完整 digest preimage，故该保留 caveat 不影响结论。

## Acceptance 判定

| ID | 证据 | 结论 |
|---|---|---|
| `SIM-01` | M=4 fresh/stale profiles、H/offset、两种 speed model、delays、Q/grace/S_max、M=1 boundary | `PASS` |
| `SIM-02` / `A-SIM-01` | 同一 `select_proposals` API；共享 vectors 7/7；GREEN/HARDEN | `PASS` |
| `STALE-01/07/08/09` | exact ordering、fresh quorum、frontier、automatic tests与手工 grace replay | `PASS` |
| `INV-08` / loop HARDEN | fixed slot maps、完整 state diagnostics、duration-scaling regression | `PASS` |
| `S0A-01` | 特异 RED + corrected GREEN/HARDEN + commit-bound immutable evidence | `PASS` |

## Follow-ups

无影响当前关闭的 follow-up。CHECK 后仍须按正常 PERSIST 顺序记录本报告、evidence index、loop/progress/acceptance-traceability 状态；该记账不改变本 Checker verdict。

## 签署

- Checker session/date：independent Checker `/root/s0a01_final_checker` / 2026-07-14 Asia/Tokyo
- checked commit：`9dbec27ae562b20174e55e53b84d55b9c2795c6f`
- report hash：由本文件落盘后外部计算；不可自嵌入。
