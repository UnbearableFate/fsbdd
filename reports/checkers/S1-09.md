# Independent Checker Report: S1-09

## Verdict

**PASS_WITH_FOLLOWUPS** — closable.

Checked candidate: `40a1707bb5b3716b98fee5ebc9c89fb7af6f404f`

Selected formal run: `s1-09-2386129.opbs`

Selected evidence: `runtime_runs/S1-09/formal-l2`

Manifest SHA-256: `fcf07962b115544b049ffbddb021a87e28012b73a95687e8079540b9961b7675`

Selected scheduler audit: `runtime_runs/S1-09/scheduler-2386129-audit2`

The earlier `runtime_runs/S1-09/scheduler-2386129` directory is not selected evidence; it preserves an audit-command working-directory error and does not alter the formal run or selected audit.

## Checker history

Phase A initially checked candidate `f030691ce6334b1e89cac0fdd35c9a727e1109b0` and returned `BLOCKED_PRECHECK` for two reasons:

1. The formal analyzer did not bind the cardinality and correspondence of frozen proposals and weights. My independent counterexample replaced the eight Profile A weight records with one record having normalized weight `1.0`; the old analyzer accepted it even though seven selected proposals had no weight.
2. The requirement evidence overstated independent `Q`/`Q_fresh` behavior under the loop's frozen `S_max=0` scope.

Candidate `40a1707...` corrected both points. `FrozenSelection` now emits ordered proposal facts, and the analyzer checks exact cardinalities; proposal/content/learner/token correspondence; fresh-only staleness; float32 raw and normalized weights; and a recomputed canonical selection identity. The mutation suite includes the missing-weight counterexample plus correspondence, token, raw-weight, normalized-weight, content-identity, and selection-identity corruptions. The production property covers every valid `Q_fresh` value for its fixture and proves the scope-correct invariant `eligible_distinct == fresh_distinct` under `S_max=0`. The matrix, contract, and traceability record defer stale-inclusive independent anchoring to S0A-01/S4-03. Phase A recheck returned `ADMISSIBLE`.

## Identity and package integrity

- The detached formal worktree is clean at the exact checked commit.
- Manifest code, config, source, skill, path, scheduler, and role identities agree with packaged records.
- Packaged config, contract, and matrix hashes match the checked candidate: `8ff3fe36...`, `af5ed55d...`, and `609930d5...` respectively.
- All 29 checksum entries verify. The validator reports `admissible`, with 28 listed payload files equal to 28 actual payload files and no errors.
- Requirement validation reports the exact ten required IDs complete: `INV-02`, `SYNC-01`, `SYNC-02`, `SYNC-03`, `SYNC-04`, `SYNC-05`, `SYNC-09`, `STALE-09`, `A-PROP-02`, and `A-PROP-03`.
- All packaged stderr files and both selected scheduler-audit stderr files are empty.
- The PBS job exited 0 with no abnormal nodes and `resources_used.walltime=00:00:37`.

## Requirement findings

| Area | Independent finding |
|---|---|
| RED validity | The retained erroneous surrogate exits 1 with four semantic failures: file-count quorum, poll-count readiness, hot-fragment starvation, and order-dependent selection. It is a recorded erroneous surrogate rather than an import/collection failure. |
| Fixed-slot and bounded data path | Baseline, 10,000-object history, restart, and repeated Profile A polls each perform exactly `M*F = 32` fixed-slot reads. Final resident selected proposals are zero, and authority/frontier state remains fixed-cardinality. |
| Time, ties, grace | Injected nondecreasing monotonic nanoseconds define observation time; regression is rejected. Grace starts at the first qualifying observation, quorum loss cancels it, recovery receives a full interval, and equal-time fragment processing plus cyclic claiming is deterministic. |
| Quorum | Quorum uses distinct learners. With `S_max=0`, every eligible proposal is current-base and fresh, so the tested Stage 1 invariant is `eligible_distinct == fresh_distinct`; stale-inclusive independent `Q_fresh` anchoring remains outside this loop. |
| Freeze stability | Four Profile A selections each contain eight distinct fresh learners. Ordered proposal facts, float32 weights, content identities, and selection identities are analyzer-bound. History and restart selection identities are unchanged. The grace selection contains five learners, includes the pre-freeze arrival, excludes the post-freeze arrival, and retains identical weights and selection identity. |
| Fairness and single syncer | The deterministic claim order is `0,1,2,3`; unit hardening covers 32 continuously-ready claims with maximum service window `F=4`. Concurrent claim attempts produce one active lease. One logical syncer role owns all fragments. |
| Restart and once-only behavior | Restart reconstructs from current fragment authorities without history scanning. Advancing exactly eight fixed frontier entries per fragment makes every repeated Profile A proposal ineligible, leaving all four fragments waiting with zero repeated selections. |
| Profile A | Formal configuration is `M=8`, `F=4`, `Q=8`, `Q_fresh=8`, `S_max=0`, grace zero, and `max_contributors=8`. All 205 Stage 1 tests pass. |
| Topology and filesystem | PBS allocated distinct hosts `mg0045` and `mg0046`. The actual and declared role maps agree: syncer scheduler on `mg0045`, proposal writer on `mg0046`. Application coordination is shared POSIX filesystem only; MPI is launcher only. |
| Analyzer | The corrected analyzer rejects the independently derived missing-weight counterexample and the added semantic corruption family, validates all formal selections, and emits `status=pass`. |

## GPU topology judgment

The `gpus_used=0` assertion conforms under the evidence contract's explicit role-scoped observation: both application role processes report `torch_imported=false` and current-PID GPU memory of zero before and after their workloads. The syncer therefore remains CPU-only, and the proposal writer also uses no GPU.

The scheduler trace separately reports a 2,097,152-byte peak on `mg0045`. This occurred in the validation plane, which runs the complete Stage 1 test suite and an explicit Torch-version probe before launching the two readiness roles; `mg0046` remained at zero, and scheduler GPU utilization was zero on both hosts. This does not invalidate the role-scoped topology assertion, but the field name `gpus_used` is broader than its frozen observation point.

## Non-blocking follow-up

- In a later evidence-contract cleanup, rename or qualify `gpus_used` as application-role GPU use and record validation-plane GPU accounting separately. Alternatively, hide GPUs from CPU-only validation commands where compatible with the full Stage 1 test suite. This is documentation/evidence precision only and does not require rerunning S1-09.

## Final decision

`S1-09` satisfies its frozen scope and formal L2 evidence contract. The loop may close as **PASS_WITH_FOLLOWUPS**.
