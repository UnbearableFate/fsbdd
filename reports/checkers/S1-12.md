# S1-12 Independent Checker Report

## Verdict

**PASS** for checked commit
`33fc1d8c5773273ec203b7ceb722a204978bff4a` against base
`92b9006efa04d6b6962c14a9b1b03efcbfa3dc18`.

## Phase A and correction history

1. The first Phase A review blocked candidate `cbf182c`: the speed trial was
   standalone rather than integrated with the production learner/publication/
   adoption/syncer path, the numeric analyzer trusted trace arrays and omitted
   an outer-momentum gate, and the evaluation latest-read count was asserted
   rather than measured.
2. Candidate `395ff04` corrected those blockers. Targeted job `2388775.opbs`
   passed 21 tests and role checks, encoded 100 authoritative numeric
   transitions, measured exactly one evaluation-manifest read plus two frozen
   payload reads and no forbidden reads, and exercised the integrated syncer.
   The exact-candidate preflight package was admissible, so Phase A returned
   **ADMISSIBLE**.
3. Formal job `2388799.opbs` completed the four-node runtime and 288 tests but
   the analyzer correctly rejected a producer record whose field denoting the
   actual distributed state contained the result of an all-false predicate.
   Analyzer replay `2388820.opbs` correctly continued to reject that immutable
   old record. Candidate `6990f5c` corrected producer serialization, and the
   full role-producer reproduction `2388832.opbs` passed 21 tests and role
   checks with every actual distributed-state field false.
4. The same Checker admitted the exact retry candidate `33fc1d8` before the
   second and final allowed L2 submission.

## Checked Phase B identities

- Formal package: `runtime_runs/S1-12/formal-l2-retry1`
- PBS job: `2388867.opbs`, queue `debug-g`, group `xg24i002`
- Code: `33fc1d8c5773273ec203b7ceb722a204978bff4a`, clean
- Config SHA-256:
  `686551db4cc556e0ea923f2c1482e3f0ed489b3e5173efa8b0791a069bf36977`
- Stage 0-4 specification SHA-256:
  `1ea71fb6e34fef0d7b5558d8e7999dfdedeb82957f4331afcf6f80279d1f5a7d`
- Research-plan SHA-256:
  `2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`
- Miyabi skill commit: `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`
- Roles: four independent single-GPU learners on distinct hosts `mg0010`,
  `mg0012`, `mg0013`, and `mg0014`; the logical syncer is colocated with rank
  zero after local work. MPI is launcher-only and the application data plane
  is shared-filesystem-only.
- Formal manifest SHA-256:
  `6395bf839ee2c4e3dc47729022d448646a21584fc6bc8b803c005c9e3ab835cd`

The package validator reports admissible `1671/1671`; the Checker independently
verified all 1,671 payload checksums. The scheduler audit is checksum-valid,
records four distinct execution vnodes, no abnormal nodes, `Exit_status=0`,
and `00:01:23` walltime. All formal stderr artifacts are empty, and the full
Stage 1 suite reports `288 passed`.

## Independent raw-fact checks

The Checker inspected the encoded authorities and role evidence rather than
relying only on the analyzer summary.

- Fragment 0, cycle 37's encoded pre-state, four encoded proposal payloads,
  prior outer momentum, and encoded compound successor were independently
  decoded. Stage-0 float32 token weighting plus Nesterov SGD reproduced both
  parameters and momentum with relative L2 `0.0`.
- The formal analyzer covers all 100 authoritative updates. Its maximum
  parameter relative L2 across 50 consecutive updates is
  `1.1958231855191269e-08`, maximum outer-momentum relative L2 is `0.0`, and
  the last-ten maximum does not amplify beyond the frozen `1e-6` threshold.
- The evaluation manifest identity and both named immutable payloads validate.
  The captured and loaded vector remains exactly `[1,1]` with identical
  content identities while current authorities advance to `[5,5]`. The
  restart read audit records exactly one manifest read and two named frozen
  payload reads, with zero current-authority, latest-resolution, unauthorized,
  or path-escape reads.
- The common ten-second control interval yields raw learner rates
  `[128,128,128,128]`; slowing rank 3 to half schedule yields
  `[128,128,128,64]`. Unaffected changes are all `0.0`, every producer record
  reports `torch.distributed=false`, and the integrated asynchronous syncer
  completes 8 control and 10 injected-phase updates while publications and
  adoptions remain active.
- Each learner performs five consecutive finite optimizer steps while its
  version vector is mixed `[1,0]`. Per-learner local steps/input/target tokens,
  per-fragment outer counts/accepted tokens, and the minimum-derived global
  cycle remain separately reported.
- The selected semantic RED fails the intended unweighted-merge,
  latest-chasing-snapshot, and peer-barrier counterexamples.

These observations satisfy GLOBAL-05, PROG-01..04, REPORT-02, A-ALG-01,
A-EVAL-01, A-LEARN-01, and A-LEARN-03 for S1-12.

