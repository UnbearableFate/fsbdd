# Checker Report: S0B-03 / corrected `4d2e1e2`

## Verdict

`PASS`

The corrected runtime proves both acceptance gates on the frozen Miyabi
Lustre path. The prior metadata timing defect is fixed, its corrective RED is
valid and precedes the fix, and the selected primary and repeat independently
contain complete synchronized measurements. No follow-up affects closure.

## Independent inputs

- Source hashes: `STAGE0-4_SPEC.md =
  fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`;
  `RESEARCH_PLAN.md =
  2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`.
- Base: `7cd3e6ffcf36343d782b241c9815c57d62f87a69`.
- Blocked candidate: `4ef52290b607ded61bb8c519a06aedd324a5e665`.
- Corrective RED commit: `74027564ff048af02cf33456a621795253b0a0df`.
- Corrected runtime: `5424daecc01adfaf633450a1449c154c0784edbe`.
- Checked candidate: `4d2e1e2579ec703241a360ae698d2bc3392f7879`.
- Config digest:
  `7b2e1047f86c5f8f9a32caf100272d72f63892679b0670d771077b3de2fac19b`.
- Prior Checker: `reports/checkers/S0B-03-blocked-1.md`, SHA-256
  `dad6ef2140852711acefce71690e9bf285cc4fdad4e8e522f2b3d338ed67d6c1`.
- Miyabi skill: clean
  `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`.
- Review mode: read-only static inspection on `miyabi-g1`; only read-only
  scheduler history queries were added. No project Python, imports, tests,
  benchmarks, or file edits ran in the Checker session.
- The nine-node gate is not applicable before `S1-13`.

All selected manifests passed complete verification and list every
non-manifest package file exactly once:

| Package | Job | UTC qtime | Exit / inner result | Checksum-manifest SHA-256 | Listed/actual |
|---|---|---|---|---|---:|
| initial RED | `2383721` | `2026-07-14T17:04:06Z` | 0 / expected 1, five failures | `2c3a717928cc671ebc47f2c5060923edf66021c83d0cf620b433c96a6d660a1f` | 22/22 |
| corrective RED | `2383846` | `2026-07-14T17:38:10Z` | 0 / expected 1, one failure | `4eb003eafdd98dbc25af3757ccdd5478214442af622e02b910dc6ab86665357e` | 23/23 |
| focused | `2383865` | `2026-07-14T17:40:32Z` | 0 / 8 of 8 | `c74cb681ebfc72a65b645add2135ddf08e431ad4d0aeb6f55a6cf04c5ee257cc` | 24/24 |
| full Stage 0 | `2383871` | `2026-07-14T17:41:15Z` | 0 / 57 of 57 | `f027b37211f0eb324a61778edc6ff83f98d500e1a716d7742a54b1a396cf438a` | 34/34 |
| primary | `2383872` | `2026-07-14T17:41:15Z` | 0 / MPI 0 | `677d5b67177b3cde52e719a45022ce2e6257e8feb428a795a3a1608e0efed3bd` | 57/57 |
| repeat | `2383885` | `2026-07-14T17:44:45Z` | 0 / MPI 0 | `ea99f2cfe5da11222033ea4e8d61c9818c6478372627a004edd6605ac401b3e9` | 57/57 |

Each run prefix exactly matches scheduler UTC qtime. Runtime snapshots for
config, source, PBS, and tests match their recorded commits. Worktrees and the
skill checkout were clean. Old nonsynchronized full runs, old-runtime test
packages, the regex-debug run, and superseded RED are excluded from closure.

## Reconstructed target

- Measure bounded `M*F` latest-reference metadata polling and the frozen
  50/100/250/500 MiB single/16-stream sequential-I/O matrix, then decide the
  Stage 1 storage profile.
- Requirements: `BENCH-03`, `BENCH-04`, `FS-05`, `FS-06`; optional
  `BENCH-05`.
- Acceptance: `A-BENCH-03`, `A-BENCH-04`.
- The 8+1/50x10 gate is not applicable before `S1-13`.

## RED validity

Initial RED commit `86714ef` precedes implementation `59ee940`. It
deterministically rejects history scanning, incomplete metadata/bandwidth
matrices, missing steady-demand math, and omission of the `10% * H`
comparison, without timeout-sensitive assertions.

Corrective RED `7402756` is after the blocked candidate and before fix
`5424dae`. Its frozen old summarizer accepts a complete synthetic matrix with
only disjoint rank-local intervals, so the test fails exactly because no
`StorageHarnessError` is raised. The corrected suite also has a 16-second
serialized-work case: `10240 / 16s = 640/s`, which correctly fails the
`5120/s` gate. Focused and full suites then pass 8/8 and 57/57.

## Code and protocol inspection

- Scope is limited to Stage 0 benchmark config/source/PBS/tests/reports/plan
  state. There is no Torch version, dependency, model, production learner or
  syncer, training, or GPU-runtime change.
- PBS is compute-only, uses two distinct hosts and 16 ranks at eight per host,
  and leaves MPI as launcher-only.
- For every metadata cell all 16 ranks publish readiness; rank 0 starts a
  monotonic timer and publishes the phase; active ranks work while inactive
  ranks stay in the barrier; all ranks publish completion; rank 0 stops only
  after all completions.
- The summarizer rejects missing/duplicate coordinator intervals, wrong
  phase/timing provenance, incomplete ready/done rank sets, invalid active
  ranks, and intervals shorter than the slowest rank-local workload.
- Reference discoveries and syscalls are distinct, avoiding readdir
  overcounting as syscalls.
- Payloads are deterministic, file-fsynced before replacement, read from the
  opposite host, and digest-verified. Aggregate bandwidth uses rank-0 rounds.
- Discovery ignores history, coordination reuses fixed filenames, payloads
  are removed after each phase, and the run root is removed after summary.

## Independent counterexamples

1. Sixteen barrier entries could duplicate rank 14 and omit rank 15. The
   summary requires exact ordered rank set `0..15`; both runs have zero
   malformed or duplicate-member barriers.
2. Correct bytes and digests do not prove an opposite-host read. Joining every
   raw read to its source writer finds 136 reads/run, zero same-host mappings,
   zero invalid rank-offset mappings, and zero digest mismatches.
3. Duplicate coordinator rounds could conceal the interval used. Each run has
   exactly 32 unique bandwidth phase/operation rounds, all on rank 0 and no
   shorter than the slowest stream-local elapsed time.

## Runtime evidence

### Metadata

Each corrected run contains 16 unique roles, 1716 raw rank-local records, 216
unique metadata cells, 216 summary cells, and 216 unique coordinator
intervals all and only on rank 0. Independent recomputation found zero invalid
active-rank sets, duplicate rank records, incomplete barriers, provenance
mismatches, too-short intervals, or aggregation mismatches.

Minimum coordinator-minus-local margin is `17.609260ms` in the primary and
`16.341569ms` in the repeat. Limiting cells are genuine read workloads:

| Run | Limiting cell | References | Coordinator | Max local | Rate |
|---|---|---:|---:|---:|---:|
| primary | M16/F32 warm flat read repeat 0 | 42912 | 1.060520994s | 1.011097912s | 40463.131086/s |
| repeat | M16/F32 warm per-learner read repeat 1 | 41792 | 1.060617853s | 1.012180310s | 39403.447605/s |

Frozen demand is `16 * 32 / 1s = 512/s`; required capacity is `5120/s`.
The worse rate is `39403.447605/s`, or `76.9598586x` demand and
`7.69598586x` the gate.

### Bandwidth

Each run has 272 raw operations (136 writes and 136 reads), 32 unique summary
cells, and 32 unique coordinator rounds. Independent recomputation found zero
byte, digest, same-host, coordinator-duration, aggregate-rate, or total-byte
errors. The worst corrected 16-stream write is `0.323807765s` for 16
concurrent 500MiB payloads, versus `5s`; this is `0.647616%` of `H` and about
`15.44x` inside the threshold.

### Boundedness and cleanup

Both runs preserve fixed records `6272 -> 6272` and negative history
`11000 -> 11000`; bounded discovery stays at 512 references; control files
are 35; payload and temporary files are zero; and the run roots are removed.
History-scan p50 grows `0.1765605 -> 0.3626085 -> 1.9720995ms` in the primary
and `0.1641600 -> 0.3669445 -> 2.0137480ms` in the repeat, while normal
discovery never enumerates history.

## Acceptance determination

| ID | Evidence | Conclusion |
|---|---|---|
| `A-BENCH-03` | two synchronized matrices; worse `39403.447605/s` vs `5120/s`; barriers/provenance/aggregation verified | `PASS` |
| `A-BENCH-04` | complete single/16-stream matrix; worst write `0.323807765s <= 5s`; digests/opposite-host mappings verified | `PASS` |
| `FS-05` | normal discovery exactly `M*F=512`, independent of history | supported |
| `FS-06` | bounded fixed/history/control state; zero payload/temp; roots removed | supported |
| `BENCH-05` | not executed and no object-store claim | optional/not applicable |

## Follow-ups

None required for S0B-03 closure. Optional `BENCH-05` remains outside the
accepted claim.

## Signature

- Checker: fresh independent `/root/s0b03_corrected_checker`.
- Date: 2026-07-15 Asia/Tokyo.
- Checked base: `7cd3e6ffcf36343d782b241c9815c57d62f87a69`.
- Checked blocked candidate: `4ef52290b607ded61bb8c519a06aedd324a5e665`.
- Checked runtime: `5424daecc01adfaf633450a1449c154c0784edbe`.
- Checked candidate: `4d2e1e2579ec703241a360ae698d2bc3392f7879`.
