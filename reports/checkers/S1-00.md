# Checker Report：S1-00/harden-fda87cb-2384507

## Verdict

Phase A：`NOT_REQUIRED`

Phase B：`BLOCKED`

The selected package is internally consistent as a concrete artifact: its 12
listed payloads equal the 12 actual payloads, all 13 checksum entries
(`manifest.json` plus those payloads) verify, its inventory digest recomputes,
and the source/config/role identities in that manifest are self-consistent.
S1-00 nevertheless cannot close. The general validator accepts malformed
formal manifests through its validation/finalization path, the supposedly
frozen resolved configuration retains mutable nested dictionaries, structured
log callers can overwrite the authoritative envelope, and the RED artifact
only reaches the first of two assertions rather than establishing the frozen
schema/identity/manifest/validator RED matrix. These are gaps in this loop's
single objective, not later storage- or proposal-protocol work.

## 独立输入

- source hashes：`STAGE0-4_SPEC.md =
  1ea71fb6e34fef0d7b5558d8e7999dfdedeb82957f4331afcf6f80279d1f5a7d`；
  `RESEARCH_PLAN.md =
  2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`。
  Checker 静态重算与 manifest 一致。
- loop card：`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/loops/stage1/S1-00.md`，
  SHA-256 `2f4e67b9e787bc65dffe4c43143123d35c89640401da77b7baaca3c6e953842d`。
- code commit/diff：base
  `0e704d76da207119469857bd9660cae45a6351e4`；checked commit
  `e88c94526e9bb3118d40338f22a66053d513f5da`；20 files，1094 insertions、
  11 deletions；`git diff --check` 通过。Formal HARDEN package 绑定 code commit
  `fda87cb085217456b2b38b1ce0d8b7bb8229dd12`；从该 commit 到 checked
  commit 只改变 evidence index、progress、traceability 与 loop state，不改变
  implementation/tests。
- resolved config：`configs/resolved.json`，digest
  `dd1dc08de331759cc49de1959bc1f2d9ba305ff985b07c2efb9084746fda3c50`；
  Profile A skeleton 显式包含 DISC-03 全部算法/调度字段、model/data/storage/seed
  与 stop criteria。
- RED：`runtime_runs/S1-00/red-fda16e6/stderr/red.log`，SHA-256
  `17713a0649c74e4fe36363ca4f95668eb7021172921a0c4f5329c96cb59e2fd3`。
- GREEN/HARDEN：selected package
  `runtime_runs/S1-00/harden-fda87cb-2384507`；`stdout/pytest.log` 报告
  66 passed，56 subtests passed。
- 9N package：不适用；loop 卡最高资源级别 L1，首次 9N gate 在 S1-13。
- requirement matrix：`reports/stage1/S1-00-requirement-matrix.csv`，SHA-256
  `916833dc13795408d33eac8032b0c15ef359468531047f5cb23d4bad93eef307`；
  machine summary 对 loop card 派生的六个 ID 报告 6/6 complete。
- package validator summary：`validation-summary.json` SHA-256
  `b16da75575caec66836d2f5aa10b0c3fd858b7ddcc90920e8a32da89f344b6bc`，
  status `admissible`、listed/actual `12/12`。

## Phase A：昂贵运行前检查

- requirement/acceptance 是否完整：Phase A 不适用；本 loop 无 acceptance ID、
  不允许 L2+ 或 9N。
- 反例是否覆盖主要错误实现：冻结合同列出了主要 mutation fixtures，但 RED 与
  HARDEN 的实际 tests 没有逐项执行该合同，详见下文。
- authoritative observation 与 aggregation 是否有效：六行 matrix 使用 set equality、
  exact field validation 与 zero-error validator；本 loop 无性能聚合。
- run/evidence contract 是否 admissible：selected concrete package 自洽；通用 validator
  contract 不 admissible。
- 允许升级到的最高资源等级：L1；不得因本结论升级资源。

## 重新构建的目标

- 单一目标：建立不可绕过的 typed/frozen config 与 identity/digest，可信 structured
  logging、角色 CLI skeleton，以及 fail-if-exists、两阶段 checksum、可对抗验证的通用
  evidence package 生命周期。
- 受影响条款：`DISC-03`、`ORACLE-05`、`PROP-01`、`FS-07`、`PROG-03`、
  `PROG-04`。本 loop 只提供后续 proposal/storage/runtime acceptance 的组成证据。
- Acceptance：无；不得把 generic identity/package skeleton 当作完整 proposal schema、
  storage reader 或 runtime progress enforcement。
- 用户补充门禁：Miyabi login node 不运行项目 Python；Checker 仅作静态代码、Git、
  file inventory 与 shell checksum 检查。未改 Torch 版本，未运行重型 import。

## RED 有效性

- 错误实现/缺失实现是否失败：部分有效。RED exit 1 确实到达
  `PROP-01: fragment identity mutation was ignored` 的 semantic assertion；不是 import、
  collection 或环境失败。
- 失败是否对应需求：是，对应 `PROP-01` identity component。
- 是否存在偶然 timeout/脆弱断言：没有 timeout；反例是确定性 digest 比较。
- 阻塞 gap：surrogate 在第 29 行首个 assertion 终止，因此第 35 行的 FS-07
  unlisted-file assertion 从未执行。现有 RED 也没有 schema、manifest identity、qtime、
  checksum 或 resume mutation 的 semantic failures。它不能证明 loop 卡完成定义要求的
  `schema/identity/manifest/validator` RED matrix 先红后绿。

## 代码与协议检查

- scope：diff 仅建立 package skeleton、config/identity/logging/manifest/evidence/CLI、
  tests 与 plan/evidence 文档；没有 storage protocol、model load、training、learner/syncer
  data plane、9N 或 Torch version change。`fsbdd.cli` 顶层 imports 都是 stdlib/lightweight
  local modules，四个 role dry-run 不导入 Torch/HF。
- data plane / bounded state / atomic publication / base-consumption / numerical reference：
  均为本 loop 非目标。`EvidencePackage` 的 discovery 是 package-local finite inventory；
  proposal/global publication、base 与 consumption 必须由后续 loops 证明。
- config/identity：strict top/nested field sets、schema version、nonnegative/positive integer
  constraints、offset bounds、fragment digest shape、environment override rejection、canonical
  mapping order 与 SHA-256 digest 的主路径合理。阻塞问题是 `MappingProxyType` 只冻结顶层；
  `raw` 内的 nested dict/list 仍可改写，使 typed field、`to_dict()` 与已缓存 digest 互相
  矛盾。
- manifest/package：builder 主路径拒绝 dirty commit、placeholder、declared/actual mismatch，
  L2+ builder 要求 scheduler fields，finalizer fail-if-exists、列出 actual payloads 并生成
  checksums，validator 比较 listed/actual 与每项 digest。阻塞问题是
  `validate_manifest()` 没有复用 builder 对 `time_source` membership 和 scheduler required
  fields 的检查；formal package finalizer/validator 接受外部 manifest，所以不能把
  builder-only checks 当作 validator guarantees。Identity 子对象也只检查部分字段：空
  `paths`、空且相等的 role maps、缺 code repository/branch、缺 skill repository 都可通过。
- listed/actual/checksum behavior：selected package 的 12 个 manifest payload path 与实际
  non-control files 精确同集；`checksums.sha256` 的 13/13 entries 全部 `OK`；重算 canonical
  file-list digest 为
  `6835168a522eba7b594d12ddd5356eeb9b46f6afe4090c24c66a26d84054c16a`，
  与 manifest 一致；manifest/checksum/summary hashes 与 evidence index 一致。现有 extra-file
  与 truncated-payload GREEN mutations 会被拒绝。
- structured logging：UTC 与 monotonic timestamps、pid/run/role/event 的正常记录存在，
  且 append 后 flush/fsync。可是 `**fields` 放在 envelope 字段之后，调用者可覆盖
  `timestamp_utc`、`monotonic_ns`、`run_id`、`role`、`event` 与 `schema_version`，使日志
  identity/provenance 不可信。
- CLI：console entry point 与 `python -m fsbdd.cli` 均存在；config resolve、requirement
  validate、evidence init/finalize/validate 以及四个 role dry-run 参数路径静态完整。
- mutable-container bounded-state inventory：`ResolvedConfig.raw` 的 nested dict/list 与
  `ProgressSnapshot` 内所有 dict 均是 mutable container。后者是 telemetry value object；
  前者直接违反 frozen config/digest contract，必须修复。
- resume identity：API 要求 exact `code/config/generator` keys 并拒绝 mismatch；selected
  package没有 resume claim。formula identity 此 loop 不适用。
- traceability/scope：global traceability 将六个 source clauses 全部标为 `passed`，但同行
  notes 又说明 PROP-01 proposal schema、FS-07 storage payload checks、PROG-03 runtime
  enforcement 和 PROG-04 runtime reporting 仍由后续 loops 完成。Loop card 明示本 loop
  只提供组成证据，故这些 source-level `passed` 状态超出 checked scope；本 loop 可记录
  supporting/component evidence，但不能提前关闭完整 clause。

## Maker 未列出的反例

1. Deep-freeze bypass：加载合法 config 得 `resolved` 后执行
   `resolved.raw["algorithm"]["S_max"] = 1`。顶层 proxy 不阻止 nested mutation；此时
   `resolved.algorithm.S_max == 0`、`resolved.to_dict()["algorithm"]["S_max"] == 1`，而
   `resolved.digest` 仍是 mutation 前 digest。随后 `freeze_config()` 会写出变异值却返回
   旧 digest，直接绕过 DISC-03 frozen identity。
2. Validator-only manifest bypass：构造一个 otherwise valid finalized L2 manifest，令
   `run_identity.time_source = "not-a-time-source"` 且 `scheduler = {}`。第 126 行只要求
   scheduler 是 dict，第 130 行仅在值恰为 `pbs_qtime` 时检查 qtime；builder 的 allowed-set
   与 required-field checks 不在 validation path。`EvidencePackage.finalize()` 调用该
   validation path，因而可把缺 job/queue/group/nodefile/modules 的 package 判为 admissible。
3. Structured-log identity spoof：调用
   `logger.emit("ready", run_id="other", role="syncer", timestamp_utc="not-utc",
   monotonic_ns=-1)`。第 27 行的 `**fields` 覆盖 constructor 和 clock 生成的 envelope；
   当前 test 只覆盖无冲突字段，因此不会发现。
4. Empty identity maps：把 roles 设为
   `{ "declared": {}, "actual": {} }` 且 paths 设为 `{}`。当前 validator 只比较两个 role
   maps 是否相等，不要求非空 role/path identity，因此错误 role/path provenance 可被正式
   package 接受。

## 运行证据

- node/role：manifest initial host `miyabi-g1`、compute host `mg0012`、execution identity
  `pbs-2384507.opbs`；declared/actual 都是 `test_runner=[mg0012]`。`env/pbs.txt` 记录
  `PBS_JOBID=2384507.opbs` 与 nodefile；module list 为空。资源 profile 标为 L1，manifest
  按 frozen dual-profile contract 未含 scheduler object。
- loss/runtime/global cycles/local steps/fault/perf：本 loop 不训练、不实现 runtime，均不适用；
  没有相应 claim。
- raw evidence 可复算：是；source hashes、inventory digest、manifest/checksum/summary/RED
  hashes均由 Checker 静态复算。Package 具体内容未发现 listed/actual 或 checksum anomaly。
- timestamp source / scheduler identity：selected manifest 使用 `submission_utc =
  2026-07-14T21:27:46Z`。由于 L1 profile 不含 scheduler object，无法从 package manifest
  复核 queue/group/qtime；这不影响上述 concrete checksum 结论，但也不能作为 L2+ PBS
  identity evidence。
- final package validator：对 selected package 的结果是 `admissible`；该具体判断由独立
  inventory/checksum 重算确认。通用 validator 仍因上述 counterexamples 不 admissible。

## Acceptance 判定

| ID | 证据 | 结论 |
|---|---|---|
| `DISC-03` | typed fields、strict schema、resolved config/digest；deep-mutation counterexample | `BLOCKED` |
| `ORACLE-05` | loop-card-derived 6/6 matrix set equality | component `PASS`；source traceability 状态需收窄 |
| `PROP-01` | identity mutation GREEN tests 与有效 PROP-01 RED | generic component supported；完整 clause 尚属 S1-05，且 current frozen identity 可绕过 |
| `FS-07` | selected package 12/12、13/13 checksums；extra/truncate tests | concrete package `PASS`；通用 validator `BLOCKED` |
| `PROG-03` | explicit positive global-cycle and walltime stop config | typed skeleton `PASS`；runtime enforcement 不在本 loop |
| `PROG-04` | distinct typed learner/fragment/global counters | typed skeleton `PASS`；runtime reporting 不在本 loop |
| S1-00 single objective | above plus RED/HARDEN contract | `BLOCKED` |

## 解除阻塞条件

1. Deep-freeze or remove mutable raw config state so no caller can produce a
   value/digest split; add the exact nested mutation regression and confirm
   `freeze_config()` writes bytes matching the returned digest.
2. Make `validate_manifest()` enforce every formal identity rule independently
   of the builder: allowed time sources, full scheduler fields/types/qtime rule,
   nonempty and shaped code/skill/execution/role/path identity. Reject structured
   log reserved-key collisions. Add tests for every fixture frozen in
   `S1-00-evidence-contract.json`, including missing source/skill/config identity,
   invalid time source, qtime mismatch and validator-only L2 manifests.
3. Preserve corrective semantic RED evidence for the newly identified wrong
   implementations, then produce new GREEN/HARDEN immutable evidence and a
   freshly finalized package. The RED runner must execute independent cases so
   one expected failure cannot prevent the rest of the matrix from running.
4. Correct source-level traceability status so this loop records component
   evidence without declaring PROP-01, FS-07, PROG-03 or PROG-04 fully complete
   before their designated runtime/protocol loops.

## Follow-ups

None. All items above affect the current closure contract and therefore cannot
be deferred under `PASS_WITH_FOLLOWUPS`.

## 签署

- Checker session/date：independent `/root/s1_00_checker` / 2026-07-15 Asia/Tokyo
- Checker context ID / Maker chat inherited=false：`/root/s1_00_checker` / `false`
- checked commit：`e88c94526e9bb3118d40338f22a66053d513f5da`
- report hash：由后续 PERSIST 在本文件落盘后外部记录；不可自嵌入。
