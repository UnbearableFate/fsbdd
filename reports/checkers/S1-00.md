# Checker Report：S1-00/harden2-79fd3e7-2384525

## Verdict

Phase A：`NOT_REQUIRED`

Phase B：`PASS`

All four prior unblock conditions are satisfied. The resolved configuration is
now recursively immutable; formal manifest validation independently enforces
time-source, scheduler and complete identity shapes; structured-log envelope
keys are reserved; corrective RED runs every semantic case independently; and
traceability no longer closes proposal, storage or runtime clauses on component
evidence. The corrected selected package is internally complete and
independently checksum-verifiable. No follow-up affects S1-00 closure.

## 独立输入

- source hashes：`STAGE0-4_SPEC.md =
  1ea71fb6e34fef0d7b5558d8e7999dfdedeb82957f4331afcf6f80279d1f5a7d`；
  `RESEARCH_PLAN.md =
  2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`；
  Checker 静态重算与 corrected manifest 一致。
- loop card：`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/loops/stage1/S1-00.md`，
  SHA-256 `2f4e67b9e787bc65dffe4c43143123d35c89640401da77b7baaca3c6e953842d`。
- prior blocked check：checked commit
  `e88c94526e9bb3118d40338f22a66053d513f5da`；report retained in this file's
  Git history.
- corrective RED commits：`f18d672` adds independent checker counterexamples；
  `49c071f` corrects the empty-inventory fixture before implementation.
- implementation fix：`79fd3e748686286a1ef72678ce4fee7227411ad6`。
- checked commit：`b9658c5882e75c5ad72b5c828eb43f731c455f3c`；from implementation fix to
  checked commit, only evidence index and loop-state persistence change.
- corrected selected package：
  `runtime_runs/S1-00/harden2-79fd3e7-2384525`，bound to clean code commit
  `79fd3e748686286a1ef72678ce4fee7227411ad6` and config digest
  `dd1dc08de331759cc49de1959bc1f2d9ba305ff985b07c2efb9084746fda3c50`。
- requirement matrix：six IDs derived from the loop card, summary status
  `complete`、row count 6。
- 9N package：不适用；loop 卡最高资源级别 L1，首次 9N gate 在 S1-13。

## Phase A：昂贵运行前检查

- requirement/acceptance 是否完整：Phase A 不适用；本 loop 无 acceptance ID，且不运行
  L2+ 或 9N gate。
- 反例是否覆盖主要错误实现：是；原始 identity/unlisted-file wrong surrogate 加上
  Checker-derived deep-freeze、validator-only manifest、identity-shape 与 log-envelope
  counterexamples 覆盖 prior blockers。
- authoritative observation 与 aggregation 是否有效：machine-readable requirement set
  equality、pytest semantic assertions、package listed/actual equality 与 SHA-256；本 loop 无
  performance aggregation。
- run/evidence contract 是否 admissible：是；corrected selected package 13/13，zero errors。
- 允许升级到的最高资源等级：L1；未升级。

## 重新构建的目标

- 单一目标：建立不可绕过的 typed/frozen config 与 identity/digest、可信 structured
  logging、角色 CLI skeleton，以及 fail-if-exists、两阶段 checksum、可对抗验证的通用
  evidence-package lifecycle。
- 受影响条款：`DISC-03`、`ORACLE-05`、`PROP-01`、`FS-07`、`PROG-03`、
  `PROG-04`；后四项中涉及 proposal/storage/runtime 的内容仅作为 component evidence。
- Acceptance：无。本 verdict 不宣称 proposal schema、storage protocol 或 runtime progress
  acceptance 完成。
- 用户补充门禁：Checker 在 Miyabi login/control-plane 仅执行 Git/file/hash/shell 静态
  检查；未运行 project Python、重型 import、训练或 GPU runtime。没有 Torch 版本变更。

## RED 有效性

- explicit wrong surrogate：exit 1，单次执行收集并报告四个独立 semantic failures：
  `PROP-01` fragment identity mutation ignored、`FS-07` unlisted file accepted、`DISC-03`
  unknown schema field accepted、invalid time source/empty scheduler accepted。stderr SHA-256
  `4f4b75479937f4ac7f76622967af6b5afb168750dd26d079d00f8947aec74f69`。
- Checker counterexample suite：在 fix 前产生 6 failed / 3 passed；失败明确覆盖 nested config
  mutation、empty role/path maps、missing code/skill identity 与 reserved log fields。stdout
  SHA-256 `509469408771ffe19af75dcad1bf09fc121b5187d30607d7464cafef85bc7e1f`。
- corrected validator-only RED：在 `49c071f`、implementation fix 前，以正确 empty-inventory
  digest 构造 finalized L2 manifest；invalid time source / empty scheduler mutation 到达
  `DID NOT RAISE ManifestError` semantic failure。stdout SHA-256
  `4551a8374eb933603e75ce00c594510d3b79d2088f0964a03e7c095893c78a91`。
- 顺序与脆弱性：corrective tests precede `79fd3e7`；pytest cases 与 surrogate failure list
  让前一错误不会阻止后续 case 执行。没有 import/collection failure、timeout 或环境错误。

## 代码与协议检查

- scope：prior-to-corrected diff only hardens config, manifest validation, logger, tests and
  persistence. No storage protocol, learner/syncer data plane, model load, training, algorithm,
  topology, dependency or Torch version change。CLI remains lightweight/lazy for four dry-run roles。
- deep freeze：`_deep_freeze` recursively turns every JSON dict into `MappingProxyType` and every
  list into tuple；`_deep_thaw` returns an independent mutable serialization copy。The exact prior
  `resolved.raw["algorithm"]["S_max"] = 1` counterexample now raises `TypeError`。The regression
  also canonical-digests the emitted resolved JSON and matches the digest returned by
  `freeze_config()`，so value and identity cannot split。
- config/digest：mandatory DISC-03 fields remain explicit; schema/unknown/missing fields,
  environment overrides, integer/range/offset/fragment identity constraints and canonical ordering
  remain enforced。No algorithm field obtains an environment default。
- validator-independent manifest checks：`validate_manifest()` now rejects time sources outside
  `{submission_utc,pbs_qtime}` and invokes `_validate_scheduler()` itself for every L2+ manifest。
  Scheduler shape is exact and validates nonempty job/queue/group, UTC qtime, SHA-256 nodefile and
  module-list type；`pbs_qtime` still requires exact timestamp equality。
- complete identity shapes：code requires repository/branch/commit/clean；config requires digest；
  source requires both authoritative hashes；skill requires repository/commit；execution requires
  identity/initial host/workflow with optional compute host；role maps and host lists must be
  nonempty and declared exactly equal actual；project/evidence paths must both be nonempty absolute
  paths。Nested shapes reject missing and unknown fields。
- listed/actual/checksum behavior：corrected manifest lists 13 non-control payloads and filesystem
  inspection finds exactly those 13。`checksums.sha256` has manifest plus all 13 payloads；14/14
  entries pass `sha256sum -c`。Recomputed canonical inventory digest is
  `e43d56889c1e1a2f6d590c333c97d90533fe686dd973e94de119c165bf69d207`，
  matching manifest。Manifest/checksum/summary SHA-256 values are respectively
  `20a4b85bbe246e4f8320460c4486f5b8ad83b93e5a6c790f892ae7ad22789ec9`、
  `e18a249d29132b6ddc7c34ea7220583997ae833319efc90e84a264630ea63046`、
  `6431ea95475f5159ec6d8194d64f532c14be7708d46937ff5fb89b77ccb30482`，
  equal the updated evidence index。
- logging：reserved envelope fields are checked before record construction；caller-supplied
  `schema_version/timestamp_utc/monotonic_ns/pid/run_id/role/event` now raise `ValueError`，and
  empty events are rejected。Normal UTC、monotonic、pid、run、role、event logging with flush/fsync
  remains intact。
- CLI：console entry point and `python -m fsbdd.cli` paths remain present for config resolve,
  requirement validation, evidence init/finalize/validate and bootstrap/learner/syncer/checker
  dry-run parsing。
- data plane / bounded protocol state / atomic proposal or global publication / base-consumption /
  numerical reference：not implemented or claimed by S1-00。Package inventory is finite；later
  loops retain responsibility for those protocol facts。
- resume identity：exact `code/config/generator` keys and mismatch rejection remain。Selected
  package has no resume claim；formula identity is not applicable。
- traceability：`FS-07`、`PROP-01`、`PROG-03` and `PROG-04` are restored to `planned` with
  explicit S1-00 component-evidence notes and their later implementation loops preserved。
  `DISC-03` stays passed because this loop now supplies its complete frozen-config foundation；
  `ORACLE-05` stays passed from the maintained authority-derived map。

## Maker 未列出的反例复查

1. Deep nested list mutation：`resolved.raw["schedule"]["offsets"][0] = 1` now fails because
   both the containing mapping and offsets sequence are immutable；`to_dict()` returns a separate
   list, so mutating the returned object cannot alter `resolved.raw` or its digest。
2. Validator-only scheduler shape：take a valid finalized L2 manifest, keep
   `time_source="submission_utc"`, but delete only `scheduler.queue`。This bypasses the qtime-only
   branch yet is rejected by `_validate_scheduler` exact required-set checking；builder use is not
   required。
3. Identity disguise：declared and actual roles both equal `{"test_runner": []}`。Equality alone
   would have passed the old implementation；the corrected nonempty host-list validator rejects it。

## 运行证据

- node/role：initial host `miyabi-g1`、compute host `mg0012`、PBS allocation
  `2384525.opbs`、execution identity `pbs-2384525.opbs`；declared/actual role map both
  `test_runner=[mg0012]`。Selected package records an empty clean-worktree `git-status.txt`。
- tests：corrected selected package reports `17 passed in 0.06s` for the complete S1-00 suite；
  stderr files are empty。Requirement validation reports six expected rows complete。
- loss/runtime/global cycles/local steps/fault/perf：no training/runtime claim；not applicable。
- raw evidence 可复算：是。Checker independently verified authority hashes, every package checksum,
  listed/actual set, inventory digest and index hashes。
- timestamp source / scheduler identity：L1 profile uses `submission_utc =
  2026-07-14T21:41:31Z` and intentionally omits an L2+ scheduler object；PBS job/nodefile remain in
  environment evidence。This package makes no L2 scheduler claim。
- final package validator：`admissible`，errors `[]`，listed/actual `13/13`；independent static
  recomputation agrees。

## Acceptance 判定

| ID | 证据 | 结论 |
|---|---|---|
| `DISC-03` | recursive frozen config、canonical digest/emitted-value regression、explicit Profile A fields | `PASS` |
| `ORACLE-05` | loop-card-derived six-ID set equality and omission regression | `PASS` |
| `PROP-01` | canonical model/fragment/config mutation RED/GREEN; traceability remains planned for proposal schema | component `PASS` |
| `FS-07` | validator-only identity/scheduler mutations, 13/13 listed-actual, 14/14 checksums; storage reader remains planned | component `PASS` |
| `PROG-03` | explicit typed global-cycle and walltime stop config; runtime enforcement remains planned | component `PASS` |
| `PROG-04` | distinct typed learner/fragment/global progress dimensions; runtime reporting remains planned | component `PASS` |
| S1-00 single objective | corrective RED + 17-test HARDEN + admissible corrected package | `PASS` |

## Follow-ups

None for S1-00 closure。Proposal schema、storage payload reader and runtime progress enforcement/reporting
remain normal work in their already assigned later loops; they are not defects in this skeleton loop。

## 签署

- Checker session/date：same independent `/root/s1_00_checker` / 2026-07-15 Asia/Tokyo
- Checker context ID / Maker chat inherited=false：`/root/s1_00_checker` / `false`
- checked implementation commit：`79fd3e748686286a1ef72678ce4fee7227411ad6`
- checked persistence commit：`b9658c5882e75c5ad72b5c828eb43f731c455f3c`
- report hash：由后续 PERSIST 在本文件落盘后外部记录；不可自嵌入。
