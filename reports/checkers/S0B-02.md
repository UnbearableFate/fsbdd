# Checker Report: S0B-02 / `f82ad39`

## Verdict

`PASS`

Candidate `f82ad39f5b0101d54cdd6e8cad43a9b06a707456` resolves the sole
blocker from the `454043a` review. The canonical index and loop state select
the exact-qtime corrected RED package while retaining the original RED only
as pre-implementation ordering evidence. `A-BENCH-01` and `A-BENCH-02` pass;
no new runtime execution is required.

## Independent inputs

- Runtime source: `78e5992383d3fc6900edeb80f6055451bb08933b`.
- Source hashes: `RESEARCH_PLAN.md =
  2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`;
  `STAGE0-4_SPEC.md =
  fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`.
- Loop-card SHA-256:
  `35eaf5b8f0a0e625b01158a5d4654397a9500be7dc2d0c8c629042fd04b0984d`.
- Checker-template SHA-256:
  `0cf29aa65c6a0d691cdb2661e5cacc3f2beb1619d729e98add082e36539ae297`.
- Miyabi skill:
  `https://github.com/UnbearableFate/miyabi-development@c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`.
- Static review ran on `miyabi-g1` without a PBS allocation. It used Git,
  shell parsing, hashes, raw JSON/log inspection, and completed PBS history;
  no project Python, imports, tests, MPI, or benchmark ran on the login node.
- `git diff --check 5e001112..f82ad39` and `bash -n` for the relevant PBS
  scripts pass. The unrelated untracked S0B-03 loop state is outside this
  checked candidate.

## Reconstructed target

Measure empty and loaded cross-node publication-to-readable latency and prove
zero partial/torn observations during at least `100000` concurrent atomic
visibility-record replacements.

- Requirements: `BENCH-01`, `BENCH-02`, supporting `STOR-01` and `FS-02`.
- `A-BENCH-01`: p99 no greater than
  `min(5s, 0.05 * 50s) = 2.5s`.
- `A-BENCH-02`: at least `100000` replacements and zero violations.
- Hardened boundaries: cold/warm states, `256`/`4096`-byte records, four
  readers, active background load, and actual writer kills before and after
  publication.
- The nine-node gate is not applicable before `S1-13`.

## RED validity

The original package
`20260714T155627Z-red-n3f8c-3c37e0b38ef2`, job `2383353.opbs`, binds
pre-implementation commit `6a610b2`. Its expected-failure test records four
deterministic requirement failures: omitted loaded profile, wrong timing
origin, accepted partial direct overwrite, and only `99999` replacements. Its
run prefix is seven seconds earlier than scheduler UTC qtime, so it is used
only to prove ordering.

The corrected package
`20260714T164222Z-red-clock-n5a7d-3c37e0b38ef2`, job `2383657.opbs`, has
a run prefix exactly equal to scheduler UTC qtime. Scheduler and wrapper exits
are `0`, the expected underlying test exit is `1`, and checksum-manifest
SHA-256 is
`97e41b32b0a8c86d162b62c0b3acdb62a78105ac4ae9ac98a8e4bab6149d8196`.
Its PBS wrapper, config, surrogate, digest inputs, exit, and complete stderr
are byte-identical to the original package, including the same four failures
in `0.002s`. Corrected provenance plus original ordering satisfies RED.

## Code and protocol inspection

- Runtime snapshots exactly match `78e5992` and reproduce config digest
  `006abf2a3b433ca2e092fcb5d33914b37eac9875d06b348ba910c786dd7bd28a`.
- MPI only places writer and reader roles on distinct nodes; payloads,
  readiness, acknowledgements, and completion use the frozen Lustre path.
- Visibility timing starts after the writer's `os.replace` and ends when the
  same writer clock observes the validated reader acknowledgement. This is a
  conservative upper bound without cross-host clock subtraction.
- Atomic records enforce exact length, schema, identity, completion marker,
  sequence, and checksum. Publications use a complete, file-fsynced,
  same-directory temporary followed by `os.replace`.
- Directory-entry crash durability is explicitly outside the claim.
- A stable sequence `-1` handshake excludes startup `ENOENT`; failures during
  the replacement window enter the raw violation scanner.
- Validation requires complete `0..99999` transcripts, non-regressing
  timestamps, four readers, final sequence observation, zero violations,
  active loaded traffic, and both kill boundaries.

## Counterexamples

1. A permanently empty scanner is refuted by exactly `5208` unsafe
   direct-overwrite detections.
2. Inflated summary counts are refuted by two raw `100000`-line publication
   transcripts per full run with ordered sequences `0..99999`.
3. A nominal but inactive loaded profile is refuted by background progress
   `1000 -> 5958` and `1000 -> 5997` with `7497` and `7337` observations.
4. Artificially low cross-host timing is excluded because only writer-clock
   elapsed time through the remote acknowledgement is used.
5. Timestamp-mismatched, canceled, and older-source successful attempts do
   not contribute to closure.

## Runtime evidence

| Evidence | Independent result |
|---|---|
| corrected RED `2383657` | exact qtime; four expected failures; raw exit `1`, wrapper/scheduler exit `0` |
| focused `2383535` | exact qtime; 8/8 tests pass |
| primary full `2383537` | `mg0030/mg0031`; empty/loaded p99 `7.939/7.801ms`; two 100000-replacement matrices; zero violations |
| full repeat `2383540` | `mg0027/mg0032`; p99 `7.497/7.951ms`; same matrices pass |
| unsafe `2383599` | `mg0015/mg0027`; `1321+1298+1313+1276=5208` detections |
| full Stage 0 `2383600` | exact qtime; 49/49 tests pass |

All selected jobs are finished with scheduler exit `0`, all selected run
prefixes equal scheduler UTC qtime, and all checksum manifests verify.

Raw recomputation confirms for each full run exactly `2000` latency samples,
`1000` per profile, `500` cold and `500` warm per profile, correct timing
origin, exactly `100000` publications at both sizes, ordered sequence
`0..99999`, four readers ending at `99999`, zero raw atomic violations, and
both kill positions.

## Acceptance determination

| ID | Evidence | Conclusion |
|---|---|---|
| `A-BENCH-01` | two independent empty/loaded distributions; worst p99 `7.951ms` against `2.5s` | `PASS` |
| `A-BENCH-02` | two independent runs, each with 100000 replacements at both sizes, four readers, final checkpoints, and zero violations | `PASS` |
| `STOR-01` / `FS-02` support | payload-first/visibility-last publication on distinct hosts over frozen target Lustre | `PASS` for S0B-02 benchmark scope |

## Follow-ups

None for S0B-02 closure. S0B-03 remains responsible for
`A-BENCH-03/04`. No metadata, bandwidth, crash-durability, training, GPU,
model-quality, or nine-node claim is made here.

## Signature

- Checker: fresh independent `/root/s0b02_final_checker`.
- Date: 2026-07-15 Asia/Tokyo.
- Initial host/workflow: `miyabi-g1`, login/control-plane static-only.
- Checked candidate: `f82ad39f5b0101d54cdd6e8cad43a9b06a707456`.
- Runtime source: `78e5992383d3fc6900edeb80f6055451bb08933b`.
