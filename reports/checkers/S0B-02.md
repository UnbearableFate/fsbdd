# Checker Report：S0B-02/725d7d6

## Verdict

`PASS`

Candidate `725d7d6a48dc28b52fde509a548e3e6a34148318` closes S0B-02 on
the frozen Miyabi Lustre target. The primary full run and an independent full
repeat both satisfy `A-BENCH-01` and `A-BENCH-02`; the deliberately unsafe
writer produces thousands of raw detector hits, so the zero-violation result
is not a constant-zero scanner artifact.

This verdict is limited to cross-node visibility and atomic replacement. It
does not qualify metadata capacity, payload bandwidth, crash durability,
production training/protocol behavior, GPUs, model quality, or the nine-node
gate.

## 独立输入

- Source hashes: `RESEARCH_PLAN.md =
  2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`;
  `STAGE0-4_SPEC.md =
  fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`.
- Loop card: `plans/FS_DILOCO_CODEX_EXECUTION_PLAN/loops/stage0/S0B-02.md`,
  SHA-256 `35eaf5b8f0a0e625b01158a5d4654397a9500be7dc2d0c8c629042fd04b0984d`.
- Checker template: SHA-256
  `0cf29aa65c6a0d691cdb2661e5cacc3f2beb1619d729e98add082e36539ae297`.
- Checked candidate: `725d7d6a48dc28b52fde509a548e3e6a34148318`;
  executable/config source: `78e5992383d3fc6900edeb80f6055451bb08933b`.
  Commits after `78e5992` change only reports, evidence indexes, and plan state;
  `git diff --check 5e001112..725d7d6` passes.
- Runtime input hashes at `78e5992`: config
  `5e2f018659d134ab0861972b0daa5355452c2d511179c5a466a2de299aa70d66`,
  storage harness
  `6c93ddb680a9c4f8b697d3b1dedb84b73e5cb2fe6dd3a37875ae77d8a4e897ad`,
  stress harness
  `eacc1b2a81f237a620aa16346fe9c5b5cb4dc00953b70a4f51b09ce871643b15`,
  and PBS wrapper
  `f3fb98b95041225cbf738311ab404ec26a2c0ba50f460a5a6274d1d5ecaf00d0`.
  Their ordered digest preimage reproduces config digest
  `006abf2a3b433ca2e092fcb5d33914b37eac9875d06b348ba910c786dd7bd28a`.
- Miyabi skill: installed clean checkout
  `https://github.com/UnbearableFate/miyabi-development@c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`;
  installed and packaged `SKILL.md` SHA-256 is
  `60a62d28a9c12b57cd67d8b584ce53e0eae5fba3c91e29d3ab6134b2a33ba95c`.
- Nine-node package: not applicable before S1-13.

The Checker ran on `miyabi-g1` without a PBS allocation. Only Git/file/JSON,
SHA-256, `bash -n`, and completed-job scheduler history were inspected. No
project Python, tests, imports, benchmark, MPI launch, or runtime workload ran
on the login node.

## 重新构建的目标

- Single objective: measure empty and loaded cross-node
  publication-to-readable latency and prove zero partial/torn observations
  during at least `100000` concurrent visibility-record replacements.
- Requirements: `BENCH-01`, `BENCH-02`, supporting `STOR-01` and `FS-02`.
- Acceptance: `A-BENCH-01` requires p99 no greater than
  `min(5s, 0.05 * 50s) = 2.5s`; `A-BENCH-02` permits zero violations.
- Hardened boundaries: cold/warm paths, two record sizes, four readers,
  active background replacement load, and actual writer kills before and
  after visibility publication.

## RED 有效性

- The pre-implementation RED package is
  `20260714T155627Z-red-n3f8c-3c37e0b38ef2`, PBS `2383353.opbs`, at commit
  `6a610b25d93949151d30a6feba5515d43a2de63d`; implementation commit
  `e9a5397...` is later.
- All four required counterclaims fail specifically: payload-write-start is
  rejected as the timing origin, omission of the loaded profile is rejected,
  a partial direct overwrite is counted as a violation, and `99999`
  replacements cannot satisfy the `100000` floor. Raw test exit is `1`; the
  expected-failure PBS wrapper exits `0`.
- These are deterministic requirement failures completed in `0.002s`, not
  timeouts or dependency failures. The separate runtime unsafe control further
  proves the detector against the actual target filesystem.

## 代码与协议检查

- Scope is confined to the Stage 0 storage config/harness/stress runner, PBS
  wrapper, focused tests, RED surrogate, evidence, and plan state. Learner,
  syncer, optimizer, model/data, and Torch behavior are unchanged.
- The PBS wrapper fails closed outside an `mg<number>` PBS allocation, requires
  two distinct hosts, validates the clean skill checkout and frozen config
  digest, rejects existing evidence directories, captures in-job modules and
  filesystem identity, and passes `bash -n`.
- MPI is launcher-only: rank 0 is writer, rank 1 reader, one rank per node.
  Application publications, acknowledgements, readiness, and completion use
  only the frozen filesystem root. No socket/RPC/distributed application data
  path is present.
- Latency payloads are completed before a small atomic visibility record. Each
  sample starts after the writer's visibility `os.replace` returns and ends
  when the same writer clock observes the reader's validated acknowledgement.
  This writer-local round trip is a conservative upper bound and avoids
  cross-host clock subtraction.
- Atomic records have exact size, complete schema, run/size/sequence identity,
  completion marker, and checksum. The writer file-fsyncs a unique
  same-directory temporary before `os.replace`; per-replacement directory
  durability is correctly excluded from the atomic-visibility claim.
- Reader scanning begins only after a valid stable sequence `-1` record is
  observed, and the writer cannot start replacements until the reader-ready
  record. Thus startup `ENOENT` is excluded, while every read error or schema,
  length, identity, or checksum failure during the replacement window is
  written to the raw violation log.
- Summary validation requires the complete `0..99999` writer transcript,
  monotonic publication timestamps, exact transcript digest, all four readers,
  minimum observations, final sequence observation, zero violations, active
  loaded background work, and both kill boundaries.

## Maker 未列出的反例

1. A zero-length violation file could come from a detector that never rejects
   partial data. The independent unsafe run directly overwrites two halves of
   the same 4096-byte record and records exactly `5208` raw partial/torn hits
   across four readers, matching its role result and summary.
2. A summary could claim `100000` operations while preserving only endpoints.
   In both full runs, each record size has exactly `100000` parseable raw
   publication lines, ordered sequence `0..99999`, valid timing origins,
   non-regressing writer timestamps, and a file SHA-256 equal to the role's
   transcript digest. Every reader checkpoint stream ends at sequence `99999`
   with `final=true` and zero violations.
3. A nominal loaded profile could finish its background work before sampling.
   The primary run begins loaded sampling after replacement `1000`, advances
   to `5958`, and records `7497` remote background observations; the repeat
   advances from `1000` to `5997` with `7337` observations. Both report zero
   incomplete background records.
4. Cross-host timestamps could make a low latency artificial. Acceptance uses
   only the writer's monotonic elapsed time from publication completion through
   remote validation and acknowledgement, so network, polling, payload
   verification, acknowledgement publication, and return visibility can only
   increase the measured bound.

## 运行证据

- All six reviewed packages have an exact checksum-manifest file set and every
  `sha256sum -c` entry passes. Checksum-manifest SHA-256 values are RED
  `0fdfecc3db78a56dcaa9a32a509b5abdc56e6371a6e395c44a94cf70ba1203da`,
  focused GREEN
  `bb49d63d9d9906af109ad9ae488b7e503d571f6df552e1cb0f395c6402c9d2bf`,
  primary full
  `b0da24c0ad6c1d7ad09046143123852aa9fa2fbaf33acf35828b21c2ad891280`,
  independent full repeat
  `21d3de9427f016df272d89ec3f124d76a3c2365ca5476934f08890f49b6a0794`,
  unsafe control
  `0a8055c387eeeb6fa6b0132d0700cc2206942ba4faa0dab8f4482a1a1081d0c1`,
  and full Stage 0 tests
  `0f465e517f97f790971d62631bbc39221cd208e6da4bc971630903b816144c92`.
- Primary full run `20260714T161909Z-fullfix-fullfix-n6c9a-006abf2a3b43`
  (PBS `2383537.opbs`) maps writer `mg0030` and reader `mg0031`. Its 2000
  immutable latency records contain exactly 500 samples for each
  empty/loaded x cold/warm combination. Independently recomputed empty
  p50/p95/p99/max are `5.805/7.130/7.939/163.547 ms`; loaded values are
  `5.572/7.010/7.801/8.834 ms`.
- The primary run preserves `100000` writer publications at each of 256 and
  4096 bytes. Four remote readers record respectively
  `166406/165979/165117/164840` and
  `165990/165542/165992/166207` complete observations. All end at sequence
  `99999`; sequence regressions and raw violations are zero. Both
  `before_visibility` and `after_visibility` SIGKILL probes pass on both roles.
- Independent repeat `20260714T161927Z-green-startup-n2e9a-006abf2a3b43`
  (PBS `2383540.opbs`) maps writer `mg0027` and reader `mg0032`, reproduces
  empty/loaded p99 `7.497/7.951 ms`, completes another `100000` replacements
  per size with four readers, zero raw violations, and both kill probes.
- Unsafe run `20260714T162735Z-unsafefix-unsafefix-n2e5c-006abf2a3b43`
  (PBS `2383599.opbs`) maps `mg0015/mg0027`; its four readers detect
  `1321/1298/1313/1276 = 5208` violations in 1000 unsafe writer iterations.
- Focused compute-node evidence reports `8` tests passing; the full Stage 0
  evidence reports `49` tests passing. Both raw and PBS exits are `0`.
- Scheduler history binds all jobs to the recorded queue/group/hosts and
  reports finished state and exit `0`. Run-ID UTC timestamps match scheduler
  qtime after timezone conversion. Each full run used `00:08:00` of its
  `00:30:00` allocation.

## Acceptance 判定

| ID | Evidence | Conclusion |
|---|---|---|
| `A-BENCH-01` | Primary and repeated 1000-sample empty/loaded writer-clock upper-bound distributions; worst p99 `7.951 ms` against frozen `2.5 s` | `PASS` |
| `A-BENCH-02` | Two independent matrices, each `100000` replacements at 256 and 4096 B, four concurrent remote readers, complete writer transcripts, final reader checkpoints, empty raw violation scanner; unsafe detector control is positive | `PASS` |
| `STOR-01` / `FS-02` benchmark support | Payload-first/visibility-last implementation and two-host target-Lustre evidence | `PASS` for S0B-02 benchmark scope only |

## Follow-ups

None for S0B-02 evidence closure. S0B-03 remains the required next loop for
`A-BENCH-03/04`; it is outside this verdict. The post-Checker PERSIST commit
must mechanically record this verdict/result commit and update acceptance
traceability before declaring the loop persisted.

## 签署

- Checker session/date: independent re-Checker `/root/s0b02_rechecker` /
  2026-07-15 Asia/Tokyo.
- Initial host/workflow: `miyabi-g1`, Miyabi login/control-plane static-only
  review.
- Checked candidate: `725d7d6a48dc28b52fde509a548e3e6a34148318`.
- Runtime source: `78e5992383d3fc6900edeb80f6055451bb08933b`.
- Report hash: compute externally after persistence; a file cannot contain its
  own stable digest.
