# Checker Report: S0B-02 / `454043a`

## Verdict

`BLOCKED`

The corrected RED evidence is valid and, combined with the original ordering
evidence, satisfies the RED requirement. `A-BENCH-01` and `A-BENCH-02` are
technically supported. Candidate `454043a` was nevertheless blocked because
its canonical evidence index and loop state still selected the old
timestamp-mismatched RED package. The successor candidate corrects those two
references and requires a fresh recheck.

## Independent inputs

- Checked commit: `454043a659a15a54d90177f35b689f0d9c40a271`.
- Runtime commit: `78e5992383d3fc6900edeb80f6055451bb08933b`.
- Source hashes: `RESEARCH_PLAN.md =
  2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`;
  `STAGE0-4_SPEC.md =
  fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`.
- Checker workflow: static Git, shell, JSON, hashing, raw-log, and completed
  scheduler-history review on `miyabi-g1`; no project runtime on the login
  node.

## RED validity

Original job `2383353.opbs` binds pre-implementation commit `6a610b2` and
produces four deterministic requirement failures, but its run prefix is seven
seconds earlier than scheduler UTC qtime. It is admissible only for ordering.

Corrected job `2383657.opbs` has run prefix and scheduler UTC qtime
`20260714T164222Z`, scheduler exit `0`, expected underlying test exit `1`, and
checksum-manifest SHA-256
`97e41b32b0a8c86d162b62c0b3acdb62a78105ac4ae9ac98a8e4bab6149d8196`.
Its config, surrogate, outputs, and four failures are byte-identical to the
original: missing loaded profile, wrong timing origin, accepted partial direct
overwrite, and only `99999` replacements. Corrected provenance plus the
original pre-implementation ordering therefore satisfies RED.

## Runtime evidence

| Evidence | Independently verified result |
|---|---|
| focused job `2383535` | 8/8 tests pass |
| primary full job `2383537` | empty/loaded p99 `7.939ms`/`7.801ms`; 100000 replacements at each size; zero violations |
| full repeat `2383540` | p99 `7.497ms`/`7.951ms`; same atomic matrix passes |
| unsafe job `2383599` | 5208 raw partial/torn detections |
| full Stage 0 job `2383600` | 49/49 tests pass |

All closure run prefixes equal scheduler UTC qtime, all scheduler and wrapper
exits pass their contracts, and all checksum manifests verify. Raw
recomputation confirms `2000` latency samples per full run, `100000` ordered
publication records for each of `256` and `4096` bytes, four readers ending at
sequence `99999`, zero atomic violations, active loaded traffic, both actual
writer-kill boundaries, and exactly `5208` unsafe detector hits.

## Acceptance determination

| ID | Conclusion |
|---|---|
| `A-BENCH-01` | `PASS`: both profiles in two runs are far below the frozen 2.5-second p99 limit |
| `A-BENCH-02` | `PASS`: two 100000-replacement-per-size matrices have four readers and zero incomplete observations |

## Blocker

At checked commit `454043a`, `evidence/indexes/S0B-02.md` and
`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/plans/loop_states/S0B-02.yaml` still
selected job `2383353`. Update both to select corrected job `2383657`, retain
job `2383353` only for ordering, and run a fresh independent Checker. No new
runtime execution is needed.

## Signature

- Checker: fresh independent `/root/s0b02_rechecker`.
- Date: 2026-07-15 Asia/Tokyo.
- Checked commit: `454043a659a15a54d90177f35b689f0d9c40a271`.
