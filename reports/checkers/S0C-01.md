# Checker Report：S0C-01/ed9c492

## Verdict

`PASS`

S0C-01 的单一目标及其对 A-ALG-00 的支撑部分通过。先前报告的三个阻塞项均已解除：RED 现在以具体数值差异拒绝错误 stale 公式；weighting 测试直接约束真实 API 并覆盖 `s >= 3`；三个更正后的 immutable run package 均通过完整 checksum 验证。A-ALG-00 的 consumption 与 outer-optimizer reference transitions 仍按 traceability 由 S0C-02 主关闭，不影响本 loop 的 `PASS`。

## 独立输入

- source hashes：`RESEARCH_PLAN.md = 2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`；`STAGE0-4_SPEC.md = fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`。Checker 在 login node 静态重算一致。
- loop card：`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/loops/stage0/S0C-01.md`，SHA-256 `c6c20e8f2fb7580a9a15db47ec88c319fbf775ffee6bf11abf5546e7abf13da0`。
- code commit/diff：base `cd4ae9defa2688184077e09725175eb91d28b692`；checked commit `ed9c49246976d31b604b8a79f88c61e137448d9b`；11 files，644 insertions/1 deletion。`git diff --check cd4ae9d ed9c492` 通过，run 中 branch clean。
- resolved config：stdlib-only CPU oracle；canonical JSON fixture；binary32 operation/accumulation；tolerance `1e-6`；property seeds `20260714` 与 `7182818`。未修改 Torch 版本，也未导入 Torch/HF 或生产 syncer/storage。
- RED：`evidence/raw/S0C-01/20260714T223500Z-red2-n7f3a-905cf345193c`。
- GREEN：`evidence/raw/S0C-01/20260714T224000Z-green2-n8c2b-64f3b51d3094`。
- HARDEN：`evidence/raw/S0C-01/20260714T224200Z-harden2-n4d9e-64f3b51d3094`。
- 先前结论：`reports/checkers/S0C-01-blocked-1.md`；其解除阻塞条件逐项重新检查，未复用旧 RED/GREEN/HARDEN run。
- 9N package：不适用；`CAP_BASE_TRAINING` 尚未建立且 loop card 明确免除。

## 重新构建的目标

- 单一目标：建立独立的 fresh/stale scalar/small-vector reference math；stale 使用 `G_base - L_local`，明确拒绝 `G_current - L_stale`；证明 `tokens/(1+lambda_s*s)` 的归一化与性质。
- 受影响不变量/条款：ORACLE-01、ORACLE-02、INV-07、OPT-01、STALE-05。
- Acceptance：S0C-01 为 A-ALG-00 supporting loop；S0C-02 是 primary loop。
- 用户补充门禁：Stage 0 不触发 9 节点门禁；Miyabi login node 不运行项目 Python。

## RED 有效性

- 错误实现/缺失实现是否失败：是。隔离 RED surrogate 明确计算 `G_current - L_stale`；fixture 中 current `[10,8]`、old base `[7,5]`、local `[6,4]`，错误结果为 `[4,4]`，规范结果为 `[1,1]`。unittest 因该 expected/actual 差异失败，test exit code 为 1。
- 失败是否对应需求：是，失败点直接对应 INV-07/ORACLE-01，而非 import error、timeout 或环境故障。PBS wrapper 将此预期失败判为成功，因此 scheduler `Exit_status = 0` 与 raw test exit 1 的组合正确。
- 是否存在偶然 timeout/脆弱断言：无。RED 运行 1 test、0.002s；断言使用 canonical exact small-vector fixture。

## 代码与协议检查

- scope：diff 仅增加纯 oracle、canonical fixtures/tests、隔离 RED surrogate、Stage 0 CPU evidence wrapper、loop orientation 与先前 Checker 报告/ignore 规则。没有 learner、syncer、storage、训练拓扑、算法策略或 Torch 依赖变更。
- data plane：不适用；静态搜索未发现网络或生产 runtime dependency。
- bounded state/discovery：不适用；所有 API 仅处理调用方传入的小向量，没有历史或 discovery 状态。
- atomic publication：runtime publication 不适用。Evidence finalization 已改为最后一次日志写入后生成 checksum，且不再经异步 `tee` 修改已哈希日志。
- base/consumption：`validated_pseudo_gradient` 先拒绝 identity mismatch、future base 与 `s > S_max`，再以传入 old base 调用 `pseudo_gradient(base, local)`。Consumption 属 S0C-02。
- numerical reference：每个基本运算、raw mass、顺序累加及 normalized weight 均显式 round 到 finite binary32；正确 displacement 方向为 `G_base - L_local`。
- weighting mutation coverage：测试直接调用 `inverse_staleness_weights`。等 tokens、`staleness=[0,3]`、`lambda_s=1` 的断言要求 `[0.8,0.2]`，会拒绝先前反例 mutation `s % 3`（其结果为 `[0.5,0.5]`）；另有 500 个随机向量对独立公式逐值比较，`s` 覆盖 0..32，并有 0..7 的实现级单调性检查。
- canonical invalid vectors：JSON 已包含并由同一测试循环实际消费 `wrong-base-identity`、`future-base` 与 `too-stale-base`，分别验证 identity、future 与 staleness rejection。
- config/identity：三个 run ID 均含 UTC timestamp、唯一 submission nonce 与 12-char config digest；manifest 保存完整 nonce/digest、target、expected result、commit、source hashes 与 raw root。GREEN/HARDEN 同 resolved config 但使用不同 nonce/run ID。

## Maker 未列出的反例

1. 独立 `s=1` 手算：取 current `G^9=[12,-2]`、old base `G^8=[3,5]`、stale local `[1,1]`，正确 stale displacement 为 `[2,4]`，错误 current-relative displacement 为 `[11,-3]`。再取 fresh local `[8,-4]`，fresh displacement `[4,2]`；stale/fresh tokens 为 40/20 且 `lambda_s=1` 时 raw mass 均为 20，故 merged displacement 为 `[3,3]`。它必须作用于 current state，得到 `[9,-5]`；作用于 old base 会得到 `[0,2]`，使用错误 stale displacement则得到 `[4.5,-1.5]`。三种结果可明确区分，当前 API 方向与规范结果一致；transition API 仍由 S0C-02 实现。
2. 若 weighting 实现把 `s=3` 错当 fresh（例如周期化/截断为 0），`[0,3]` 的显式 API 断言立即从预期 `[0.8,0.2]` 变成 `[0.5,0.5]` 并失败；这补上了先前只在测试自身计算公式、未约束被测实现的空洞。

## 运行证据

- checksum：Checker 对三个指定 run 分别执行 `(cd RUN && sha256sum -c checksums.sha256)`，各 14/14 entries 全部 `OK`；每个目录恰有 14 个非-manifest 文件且 manifest 无遗漏/多余路径。
- RED：commit `ed9c492`，job `2382286.opbs`，target `tests.red_cases.s0c01_wrong_formula_surrogate`，1 test 数值失败，raw test exit 1，预期结果 `fail`，PBS exit 0。
- GREEN：commit `ed9c492`，job `2382300.opbs`，9 tests/0.014s，test exit 0。
- HARDEN：commit `ed9c492`，job `2382301.opbs`，9 tests/0.014s，test exit 0；覆盖随机 weights、`lambda=0`、极不均衡 tokens、single contributor、mixed fresh/stale、`s>=3`、identity/future/too-stale rejection。
- host/role：`qstat -f -H` 独立确认三项 scheduler history：均由 `x10041@miyabi-g1` 提交至 `short-g`、group `xg24i002`，在单一 compute host `mg0056` 完成，`job_state=F`、`Exit_status=0`、walltime 1s。scheduler 的 submit arguments 与各 manifest 的 run ID、target、nonce、digest 一致；GPU utilization/power/memory 均为 0，符合纯 CPU oracle 行为。
- environment：run 保存 nodefile、module list（`nvidia/25.9`、`nv-hpcx/25.9`）、Python 3.13.13、clean branch/commit 与 source hashes。Miyabi skill checkout及 manifest commit 均为 `ad1fd34a9de976b4fb26ba47d9a1770430884765`。
- Checker 自身：初始 host `miyabi-g1`，无 PBS allocation；仅执行 Git/files/hash/shell/PBS history 静态检查和 `bash -n`，未运行 project Python。
- loss/runtime/global cycles/local steps/fault/perf overlay：纯数学 oracle 不适用；没有超出证据的训练或性能 claim。

## Acceptance 判定

| ID | 证据 | 结论 |
|---|---|---|
| ORACLE-01 / INV-07 / OPT-01 | canonical fresh scalar/vector、stale `s=1`；数值特异 RED；GREEN/HARDEN；独立 current-state手算 | `PASS` |
| ORACLE-02 / STALE-05 | golden weights、binary32 implementation、500 random formula comparisons、implementation-level monotonic check、`s=3` mutation-killing case、boundary cases | `PASS` |
| A-ALG-00（S0C-01 支撑部分） | 以上 immutable evidence 与 checked diff | `PASS`；完整 acceptance 按既定 traceability 等待 S0C-02 primary closure |

## Follow-ups

无影响当前关闭的 follow-up。CHECK 之后仍须按正常 PERSIST 顺序记录本报告、evidence index、loop/progress/traceability 状态；该记账不改变本 Checker 对 checked commit 与 immutable evidence 的 verdict。

## 签署

- Checker session/date：independent Checker `/root/s0c01_rechecker` / 2026-07-14 Asia/Tokyo
- checked commit：`ed9c49246976d31b604b8a79f88c61e137448d9b`
- report hash：由后续 PERSIST 在本文件落盘后外部记录；不可自嵌入。
