# S1-08 Independent Checker Report

## Context and verdict

- Checker context: `/root/s1_08_checker`
- Maker chat inherited: no
- Checked candidate: `b0267c68749659744ee40a17574698095b069476`
- Selected package: `runtime_runs/S1-08/formal-l2`
- Scheduler supplement: `runtime_runs/S1-08/scheduler-2385842`
- Phase A: `BLOCKED_PRECHECK` for `e1d8afd`, then `ADMISSIBLE` for `b0267c6`
- Phase B: `PASS`

The same independent Checker context performed both phases. It did not submit
the formal job or modify implementation/runtime evidence. This report is the
only Checker-authored repository change.

## Phase A correction history

Candidate `e1d8afd` treated a temporarily unreadable named global payload as a
fatal background-poller error. That contradicted STOR-01's eventually-readable
primitive: a visibility record could become readable before its named payload,
then the worker would stop permanently instead of eventually adopting it.

Candidate `b0267c6` introduces typed `PublicationNotReady`, retries only that
condition in the fixed-slot worker, and leaves malformed identity, same-version
content replacement, and version regression fatal. Its actual background-worker
regression injects one transient unavailable read, observes a retry with no
worker error, then proves discovery and safe-boundary adoption. Corrected L1
evidence reports 8 passes, after which Phase A authorized one formal L2
submission.

## Final package and scheduler audit

- PBS job `2385842.opbs` exited 0 with no abnormal nodes. Scheduler accounting
  reports 42 seconds active runtime; PBS stderr is empty.
- The learner ran on `mg0020` with one GH200 GPU. The filesystem-only global
  writer ran on distinct host `mg0022`, imported no Torch, and used zero GPU
  memory.
- PBS qtime `Wed Jul 15 13:16:58 2026` JST converts to
  `2026-07-15T04:16:58Z`, matching the manifest time source and timestamp.
- The manifest binds exact clean candidate `b0267c6`, branch, config
  `8a49b0c2...0b0`, source hashes, Miyabi skill commit, clean project/evidence
  paths, scheduler identity, module inventory, and identical declared/actual
  role maps.
- The validator reports `admissible`, 28 listed = 28 actual payload files, with
  no errors. All 29 checksum entries verify. Manifest SHA-256 is
  `12695512c3b0b69bf40810b1fd09bb2c3c6b14ec41971403664ba6e41ec9e34f`.
- Analyzer SHA-256 is
  `4dc1194d26698cd38373ae91c1f52aa4ee154f944ee42a2923dc62724ea88bde`
  and its status is `pass`. All five packaged stderr logs are empty,
  requirement validation is 8/8 complete, and the exact formal candidate's
  Stage 1 suite reports 160 passed tests.

## Independent trace reconciliation

The raw role records, writer publication rows, and analyzer were independently
joined by fragment index and target version.

- Fragment 0 advances directly `0 -> 3`, reports a version jump of 3 and two
  skipped intermediate versions, and adopts writer content identity
  `d458e2b8...93c6`. Its post-adoption target digest
  `099ffe82...192c` equals the verified writer payload; the other three
  fragment parameter digests remain byte-identical.
- Fragment 2 advances independently `0 -> 1` and adopts content identity
  `2dc3f682...5fdc`. Its post-adoption digest `548b84ff...a5f0` equals the
  verified writer payload; all non-target digests again remain identical.
- Both adoptions occur at completed safe boundary 2. Only the target fragment's
  version, local-step counter, and processed-token counter change; proposal
  sequences and every non-target counter remain unchanged.
- For both adoptions, all four canonical optimizer-state hashes are exactly
  equal before/after. Initialized-state entry counts `[13,12,12,15]` are also
  positive and unchanged, ruling out the empty-state/reset false positive.
- Resident pending state never exceeds one per fragment, finishes empty, and
  contains no worker error. Resident adoption traces are 2 of the fixed
  four-record bound.

## Latest-only mixed-version and non-barrier judgment

Training starts before the writer's first publication and completes 20
consecutive optimizer steps. After the boundary-2 independent adoptions, steps
3 through 20 all use mixed vector `[3,0,1,0]`; all 18 token-weighted losses are
finite. Neither role initializes a Torch distributed process group, both role
records identify the application data plane as shared filesystem only, and the
source has no whole-vector equality condition in discovery or apply. This
establishes latest-only fragment adoption without a full-model alignment
barrier.

The formal filesystem produced no transient read
(`transient_read_retry_count = 0`), which is not misreported as runtime delay evidence. The corrected retry
semantics are instead exercised by the packaged deterministic background-worker
regression included in the 160-pass suite.

## Independent counterexample

An additional counterexample not used by the Maker's requirement matrix is a
fixed current slot retaining version `v` while its content identity changes
from `A` to `B`. Silently treating this as repeated current would hide an
illegal same-version global-state replacement and could make learners disagree
about the parameters identified by `v`. The corrected poller tracks both the
highest observed and adopted content identities and raises fatally on either
same-version mismatch; only `PublicationNotReady` is retryable. Thus this
counterexample fails closed without weakening eventual-read retry behavior.

## Requirement judgment

All S1-08 matrix close conditions pass:

- `LEARN-08`: complete verified fragments are eventually fetched, applied only
  at a zero-gradient completed-step boundary, and reset only target counters.
- `LEARN-09`: exact initialized AdamW state hashes and entry counts are
  unchanged across both adoptions.
- `LEARN-10` / `INV-09`: fragments 0 and 2 advance independently while 1 and 3
  remain at version 0, and 18 further optimizer steps remain finite.
- `LEARN-11`: the packaged regression suite covers immutable old-base in-flight
  publication while later snapshots use the newly adopted base context.
- `GLOBAL-04`: fragment 0 adopts version 3 directly from version 0 without
  replaying versions 1 and 2.
- `A-LEARN-02`: each verified target digest changes to the writer payload and
  every non-target digest remains identical.
- `A-LEARN-03`: the sustained mixed-version window is consecutive and finite
  with no version-alignment or distributed-communication barrier.

## Scope boundaries

This is component-level S1-08 evidence. It does not close full Profile A,
multi-learner syncer integration, the 160M/M=4/one-billion-token Stage 1 run,
or the first 8+1 gate; those remain assigned to later Stage 1 loops. Complete
transfer overlap and performance thresholds remain Stage 2.

## Final verdict

`PASS`
