# Independent Checker Report: S1-10

## Verdict

**PASS_WITH_FOLLOWUPS** — closable.

Checked candidate: `7a6ca502e6a9d38febda741cc13992157fa785db`

Selected formal run: `s1-10-2386588.opbs`

Selected evidence: `runtime_runs/S1-10/formal-l2-retry2`

Manifest SHA-256: `14e497a8afef7ee971000f22e4365a6d7745d5c6a92e5bbbfbb6458b3a545e07`

Selected scheduler evidence: `runtime_runs/S1-10/scheduler-2386588.opbs`

## Checker history

The same independent Checker context performed Phase A and Phase B. It did
not submit a formal job or modify production, plans, traceability, or runtime
evidence.

Phase A first returned `BLOCKED_PRECHECK` on candidate `59bdef3` because role
records were not bound to the frozen config digest, descriptor element count
was not checked against payload bytes, and the RSS comparison used a lifetime
high-water mark outside the target-update interval. Candidate `ffae9ef`
corrected those issues and received `ADMISSIBLE`.

The first formal attempt, preserved at `runtime_runs/S1-10/formal-l2`, ran the
tests and two-node workload but failed manifest construction against the
current evidence-builder API. Candidate `4c698d8` repaired the manifest schema,
added an exact regression, passed a one-node failure reproduction, and received
`ADMISSIBLE` for a retry.

The next attempt, preserved at `runtime_runs/S1-10/formal-l2-retry1`, correctly
failed its RSS gate because a two-element warmup did not make the M=1 and M=8
target-shape allocator observations comparable. Candidate `7a6ca50` freezes two
identical M=8, 4 MiB streaming warmups in each dedicated child, binds their
shape/count/peaks in the analyzer, and passed the full-size one-node
reproduction before Phase A authorized the selected retry.

## Package and scheduler integrity

- The manifest binds exact clean commit `7a6ca50`, branch, config digest
  `6d372da2...f4df4c`, source hashes, Miyabi skill commit, absolute paths,
  scheduler identity, and identical declared/actual role maps.
- The validator reports `admissible`, with 28 listed payload files equal to 28
  actual payload files and no errors. All package checksum entries verify.
- Requirement validation reports the exact nine IDs complete: `SYNC-06`,
  `SYNC-07`, `OPT-01`, `OPT-02`, `OPT-04`, `OPT-05`, `OPT-06`, `A-ALG-01`,
  and `A-PERF-01`.
- All five packaged stderr files and the scheduler stderr file are empty.
- PBS job `2386588.opbs` exited 0 with no abnormal nodes after 36 seconds. Its
  qtime converts to the manifest timestamp `2026-07-15T06:33:04Z`.
- The complete Stage 1 suite reports 235 passing tests.

## Independent numeric reconciliation

For the first formal transition, the raw writer facts give element-zero current
`-0.75` and independently sum to merged pseudo-gradient approximately
`-0.000400001809`. Applying first-step Nesterov with `lr=0.1` and `momentum=0.9`
gives approximately `-0.7499239997`; production records float32
`-0.7499240041`, with the production buffer equal to the float32 merged
gradient. This agrees within float32 rounding.

Across all 50 updates, the independently analyzed maximum parameter relative
L2 error is `2.114216746296519e-9`, below `1e-6`. The first-ten maximum is
`1.3233751054520785e-10`, the last-ten maximum is zero, and all 50 update
identities are distinct. Maximum buffer relative L2 is
`3.8297030711808174e-7`, also below `1e-6`. All 16 contribution orders have
zero relative L2 difference.

The mixed-base counterexample independently yields displacements `[2,4]` from
the declared old base and `[4,2]` from the current base. Their equal-weight
merge is `[3,3]`, so application at current `[12,-2]` produces `[9,-5]`, exactly
matching production. The forbidden current-relative stale surrogate produces
`[4.5,-1.5]` and differs by relative L2 `0.5537181355029882`.

## Requirement findings

| Area | Independent finding |
|---|---|
| RED validity | The preserved semantic surrogate exits 1 with four intended failures: current-relative stale merge, old-buffer Nesterov, cache-all payloads, and full-model reads. It exercises wrong semantics rather than failing import or collection. |
| `OPT-01` / `OPT-05` | Every contribution is base-relative, while the merged pseudo-gradient and current outer state are applied at current parameters. The mixed-base trace distinguishes both prohibited alternatives. |
| `OPT-02` | Direct weighted averaging is the explicit default behind a pluggable merge interface. Single/multiple contributors, normalized weights, extreme ordering, and plugin dispatch are covered. |
| `OPT-04` | Production follows updated-buffer PyTorch-style Nesterov, preserves momentum-free and `lr=1` controls, and matches the independent 50-transition state sequence. Fragment-state isolation is covered by the passing Stage 1 suite. |
| `OPT-06` | Canonical identities bind current state, ordered proposals and contents, bases, tokens, staleness, float32 weights, local payload hashes, merge/outer policy, hyperparameters, dtype, and fragment map. The analyzer rejects missing or mutated semantic facts. |
| `A-ALG-01` | One-step and 50-step errors remain far below `1e-6`; the late window does not amplify error, and all 16 read orders remain within tolerance. |
| `SYNC-06` | Both dedicated 4 MiB M=1 and M=8 target updates peak at 9 MiB above their warmed entry RSS, for an M=8-minus-M=1 difference of zero against the 12 MiB gate. Both use two identical M=8 target-shape warmups, one live payload lease, and a three-fragment tensor ledger. Numeric Nesterov traces remain at five fragment multiples, below the frozen eight-fragment limit. |
| `SYNC-07` / `A-PERF-01` | M=1 reads exactly one 4 MiB local payload; M=8 reads exactly eight, while each update has one 4 MiB current input and one 4 MiB successor output. The full model is 16 MiB, no full-model operation occurs, and every operation remains target-fragment scoped. |
| Analyzer fail-closed behavior | The analyzer independently recomputes numeric, order, mixed-base, byte, memory, identity, config, role, and topology facts and rejects the preserved corruption family, including the Checker's forged-config and descriptor-shape counterexamples. |

## Topology judgment

PBS allocated distinct hosts `mg0020` and `mg0023`. The actual and declared
maps agree: the CPU outer syncer ran on `mg0020`, and the proposal writer ran on
`mg0023`. Both role records identify filesystem-only application coordination
and MPI launcher-only use. The writer imported no Torch; the syncer imported
Torch for CPU tensor math. Current-PID GPU memory is zero before and after both
role workloads.

The scheduler trace separately records a 2,097,152-byte GPU-memory peak on
`mg0020`, while GPU utilization is zero on both hosts. The formal script runs
the full Stage 1 validation suite and a Torch-version probe before launching
the two application roles, so this validation-plane observation does not
contradict the role-scoped CPU-only result.

## Independent counterexample

An additional counterexample derived during Phase A declared a three-element
fragment descriptor while supplying two-element current and local fp32
payloads. The original kernel accepted that inconsistent shape because it only
compared current and local byte lengths. The checked candidate requires a flat
descriptor whose element count times four exactly equals the payload bytes;
the regression rejects the counterexample before merge execution.

## Non-blocking follow-up

- Qualify GPU-zero reporting as application-role scoped and record
  validation-plane GPU accounting separately. Where compatible with the full
  Stage 1 suite, hide GPUs from CPU-only validation commands. This is evidence
  precision only and does not require rerunning S1-10.

## Scope boundary

This loop establishes the component-level streaming direct merge and
fragment-wise outer optimizer. Atomic global publication remains S1-11, full
Profile A end-to-end closure remains S1-12, and the first 8+1 training gate
remains S1-13.

## Final decision

`S1-10` satisfies its frozen formal L2 contract and may close as
**PASS_WITH_FOLLOWUPS**.
