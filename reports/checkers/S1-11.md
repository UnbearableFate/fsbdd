# S1-11 Independent Checker Report

## Verdict

**PASS_WITH_FOLLOWUPS** for checked commit
`bc3a89e5d286982cec4987e98e5a893ebf73bfae` against base
`83dcca92bfb85559568db32a53fdb7d4a94b320f`.

The follow-up is limited to later bounded-retention/GC work and does not affect
S1-11 authority, publication, version, or consumption correctness.

## Phase A history

1. Candidate `ce250225e63ab1681057dea9050538c6faf2208b` was **BLOCKED**.
   The formal reader workload could finish its useful observations before any
   replacement, the analyzer did not independently bind fault-replay requests
   to final authorities/frontiers, and no exact-candidate generic preflight
   validator package had been supplied.
2. Commit `5b06623adcb678444ad501953ca80024783e459f` corrected the reader
   barriers/coverage and request-to-authority analyzer checks. Commit
   `bc3a89e5d286982cec4987e98e5a893ebf73bfae` then changed only loop-state
   metadata.
3. The corrected targeted allocation
   `runtime_runs/S1-11/l1-targeted-2387272.opbs` is checksum-valid, records a
   clean `5b06623adcb678444ad501953ca80024783e459f` checkout on `mg0032`, and
   reports `32 passed` with empty stderr.
4. The exact-candidate preflight package
   `runtime_runs/S1-11/preflight-validator-bc3a89e` is admissible `16/16`.
   Its manifest SHA-256 is
   `931c4ad26081a3aa983f44870a03ee975097446f020e20ad3024a2a59432f733`.
   Phase A therefore returned **ADMISSIBLE** for the formal L2 submission.

## Checked Phase B identities

- Formal package: `runtime_runs/S1-11/formal-l2`
- PBS job: `2387333.opbs`, queue `debug-g`, group `xg24i002`
- Code: `bc3a89e5d286982cec4987e98e5a893ebf73bfae`, clean
- Config SHA-256:
  `0f84b66318c086b60a560d605253293ae7449b3ea0885eb2529a6b0c70851a2f`
- Stage 0-4 specification SHA-256:
  `1ea71fb6e34fef0d7b5558d8e7999dfdedeb82957f4331afcf6f80279d1f5a7d`
- Research-plan SHA-256:
  `2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`
- Miyabi skill commit: `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`
- Roles: atomic committer rank 0 on `mg0032`; proposal writer/reader rank 1
  on distinct host `mg0034`; MPI is launcher-only and application coordination
  is filesystem-only.
- Formal manifest SHA-256:
  `21b986a803a103098bfa92ffec63aee8819c7c95ef4e9118e68c0496658fcdfa`
- Manifest inventory SHA-256:
  `607fce1f555b64f8cde818fff9039fe615c6bd4f4e6810acd13c17d66f5c5833`

The package validator reports admissible `383/383`; an independent checksum
pass verified all 384 checksum entries including the manifest. The scheduler
audit is checksum-valid `3/3`, records two distinct execution vnodes, no
abnormal nodes, `Exit_status=0`, and `00:00:34` walltime. All formal stderr
files are empty, and the formal Stage 1 suite reports `267 passed`.

## Independent raw-fact checks

The raw role records, not only the analyzer summary, were inspected.

- There are 40 successful commits with contiguous final versions `[20, 20]`.
  Both raw roles agree on the run/config identity and final versions.
- All 16 fault traces independently satisfy request-to-final binding:
  parameters, outer state, version, previous authority, selection/update
  identities, selected facts, and consumption frontiers match the immutable
  request. All 12 pre-replace interruptions retain the old authority and
  frontiers, keep selected proposals eligible, and publish exactly once on
  replay. All four post-replace interruptions expose the final authority,
  make those proposals ineligible, and replay as exact duplicates without a
  second publication.
- For `before_record_replace`, replay 0, the observed authority fingerprint is
  byte-for-byte the old fingerprint
  `55c592c175564815d7ef94b0541adb9cfc8a41a08b10422d7263892d3cc89594`;
  all four frontier entries remain `(-1, -1)` and eligibility remains true.
  Replay produces version/update-count 1, advances each selected learner to
  sequence/base `(0, 0)`, and its parameter, outer-state, selection, update,
  base, and previous-authority identities equal the recorded request.
- Each of the eight readers observed both fragments at every version 0 through
  20 (21 distinct versions per reader/fragment). Raw counts total 3,141 with
  per-reader minima of 378 and no reader errors.
- The copied main shared state has exactly the two fixed per-fragment current
  records, both at version 20. Each record's payload identity independently
  matches the referenced payload SHA-256; no global head or independent latest
  authority record exists.
- Exact retry, late-arrival deferral/refreshed next-round selection, four fixed
  consumption frontiers, distinct learners, and float32 weight sums all pass
  independent raw JSON recomputation.

These observations satisfy SYNC-08, GLOBAL-02/03, FS-03, INV-03/05, PROP-06,
A-GLOBAL-01/02, and A-PROP-03 for S1-11.

## Follow-up

The copied main global store physically contains 42 immutable payloads after
the two bootstrap publications and 40 successful commits, although only two
are current authority. They are not discoverable authority and do not affect
this verdict, but FS-06/FS-09 bounded-retention work must reclaim such orphaned
payloads in the designated GC loop rather than allowing historical growth in
steady state.
