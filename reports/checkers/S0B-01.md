# Checker Report：S0B-01/7681510

## Verdict

`PASS`

The corrected candidate closes both blockers in
`reports/checkers/S0B-01-blocked-1.md`. The runtime is bound to the actual
installed `miyabi-development` checkout, and the cross-node packages contain
byte-identical canonical JSON/YAML manifests with the required compute, PBS,
code/config, filesystem, module, and actual rank/role/host identities. A
pre-implementation RED, two independent shared-path successes, a reason-specific
node-local negative, focused tests, and the full Stage 0 test set are all backed
by checksum-valid immutable packages and matching PBS history.

This verdict closes only the S0B-01 capability-discovery and harness-readiness
loop. It does **not** evaluate or close `A-BENCH-01..04`; those thresholds remain
for S0B-02 and S0B-03.

## 独立输入

- source hashes: `RESEARCH_PLAN.md = 2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`; `STAGE0-4_SPEC.md = fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`.
- loop card: `plans/FS_DILOCO_CODEX_EXECUTION_PLAN/loops/stage0/S0B-01.md`, SHA-256 `96609382fd71c3c14e387b48dc25c04e6b2e1af32b2a3651915cfa9f98b642d6`.
- checker template: SHA-256 `0cf29aa65c6a0d691cdb2661e5cacc3f2beb1619d729e98add082e36539ae297`.
- checked ancestry: base `5a8b78c9765c1b13c5b21006a5243a32e2afc19d` -> original RED `6b901c99cd34e90c782234d063c697947d4f7f34` -> first blocked candidate `e6ab81ada6306cc5a6f7df0d27d179305c01901f` -> manifest repair `23f5eecf9d9ba25ff6fb64cf74db37d0ee5bc40f` -> runtime/provenance repair `dce034a684993cfd84858ebd09126b4f17863ba1` -> checked report-only result `7681510482548c1f1c8c58f50b2510f72bcbeb43`. `git diff --check 5a8b78c..7681510` passes.
- runtime artifact hashes at `dce034a`: config `9a1a73f4b06649df26708c8a76894723324532d9f4d693ff2926b044b76fc270`; harness `885ef492b43c1e6e6e3bb3a3a0a05b9eb6e97c41dba324a0df77b947891ad8f2`; storage PBS `2b1609e4162bd3ddf0b6eaa5c097da5f5652b01bde26f0a9ce98231f50c0a0cb`; focused tests `65fb71327bdb69d1342fe34e967900649fc5f99303433082293504e3e2184ec3`; CPU wrapper `e153f88100202f1abc33e4cc5bac281258de8550f023be728d5af19c5e7c2dfc`.
- target-FS report at checked result: SHA-256 `a5aef7fe09cd2db0feddc83edf9bf5632f84ac46cc1dd456d51bece1b59230bd`.
- closure evidence:
  - original RED: `evidence/raw/S0B-01/20260714T151400Z-red-n3d6a-d6b0d27e3bb2`;
  - clock-valid provenance RED: `evidence/raw/S0B-01/20260714T154749Z-red-provenance2-n8c5f-c41187a90807`;
  - focused 8-test HARDEN: `evidence/raw/S0B-01/20260714T154036Z-green9-skill-n2f6b-785ceb2564a8`;
  - full 41-test HARDEN: `evidence/raw/S0B-01/20260714T154201Z-harden10-all-n9b4d-5a1a87a340ce`;
  - shared GREEN: `evidence/raw/S0B-01/20260714T154053Z-green10-shared-n4c8e-2018e813ad8e`;
  - node-local negative: `evidence/raw/S0B-01/20260714T154111Z-harden9-local-n7e2a-2018e813ad8e`;
  - independent shared repeat: `evidence/raw/S0B-01/20260714T154217Z-harden11-repeat-n5a1f-2018e813ad8e`.
- 9-node package: not applicable before `S1-13`.

The Checker ran on `miyabi-g1` without a PBS allocation. I used only Git,
SHA-256, file/JSON inspection, `bash -n`, and scheduler history; I did not run
project Python, tests, MPI, benchmarks, or heavy imports on the login node.

## 重新构建的目标

- Single objective: freeze the real later protocol `RUN_ROOT`, record the
  actual Miyabi/PBS/module/filesystem context, and provide a reproducible
  writer/reader harness that distinguishes shared Lustre from node-local
  storage while keeping the login node control-plane only.
- Requirements: supporting harness evidence for `BENCH-01..04`, `FS-08`, and
  the two-primitives boundary in `STOR-01`.
- Acceptance boundary: S0B-01 has no direct `A-BENCH-*` closure ID. S0B-02 must
  measure visibility and atomic replacement; S0B-03 must measure metadata and
  bandwidth. Stage 0 does not trigger the 9-node training gate.

## RED 有效性

- The original pre-implementation RED is genuinely ordered before production
  code: PBS job `2383084.opbs` started on `mg0017` at `2026-07-14 15:13:31Z`
  from the SPECIFY/RED commit, while implementation commit `4c85b2e3...` was
  created later. It deterministically reports three failures in `0.002s`:
  accepting a login host, omitting seven required environment fields, and
  treating `/tmp` as shared. Test exit is `1`; the expected-failure PBS
  contract exits `0`. Its checksum manifest hash is
  `0fb47c4af76a9af9caf404bc3f9e5724b33534ee86377ae468bdfaebd74c720f`.
- The provenance-repair RED repeats the same three specific failures under the
  actual skill checkout and the repaired source identity. PBS job
  `2383254.opbs` was submitted at exactly `2026-07-14 15:47:49Z`, matching its
  run ID, and ran on `mg0015`. Its four-file digest preimage reproduces
  `c41187a90807aee39a80bf120e9f8ce0b86fdc06a287b8e1202302e3f67132e7`;
  all checksum entries pass and the checksum-manifest hash is
  `a15a193cf00747ee5471d3fd890880887dcd8b6541eef7f94a81ecee20948f8b`.
- The RED assertions are deterministic schema/host/path counterexamples, not
  accidental timeouts or dependency failures. The later corrected RED repairs
  provenance; it does not replace the original pre-implementation ordering.

## 代码与协议检查

- Scope: the runtime adds only the Stage 0 storage config, stdlib harness, PBS
  launchers, tests, RED surrogate, and decision evidence. It does not modify
  learner/syncer training semantics, Torch, model/data, or the system topology.
  Commits after `dce034a` are report/control-plane changes; all executable and
  config blobs at checked result remain byte-identical to `dce034a`.
- Compute guard: Python and both PBS wrappers reject a login hostname, an
  `mg<number>` host without `PBS_JOBID/PBS_NODEFILE`, and invalid MPI rank
  topology. Scheduler history independently confirms all runtime packages ran
  on allocated `mg*` compute nodes.
- Data plane: Open MPI only places one Python role on each node and propagates
  environment. Payload, visibility record, acknowledgement, and result records
  are filesystem objects; no MPI, `torch.distributed`, RPC, or socket carries
  application data.
- Publication: payload and JSON records use unique same-directory exclusive
  temporaries, file flush + `fsync`, `os.replace`, and directory `fsync`.
  Payload completes before the visibility record. Readers verify run identity,
  peer host, byte count, and SHA-256.
- Timing: each duration uses `time.monotonic_ns` within one host. The report
  correctly labels these as smoke waits, not a cross-host latency estimate.
- State/discovery: each smoke uses one run-scoped payload, one visibility
  record, one acknowledgement, and two fixed rank results. Fresh nonce/digest
  identities plus fail-if-exists evidence directories prevent a rerun from
  overwriting prior evidence.
- Config identity: storage packages independently reproduce full digest
  `2018e813ad8e7b5bca742b8e1f51f94e9ed784acad83b6aea1a3965b0f017b39`;
  focused tests reproduce `785ceb2564a80ea38923a792853b75adb7b5626f1f58acc0d7ae012aeb71975f`;
  the full test package reproduces
  `5a1a87a340cec3f50e7bbe42e6747d91cc9fb4cc1bd396d9d856590b3d3c4b55`.
  Snapshot hashes match the corresponding Git blobs.
- Skill provenance: the installed checkout is clean at
  `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`. Every closure package records
  that full identity and snapshots `SKILL.md` with SHA-256
  `60a62d28a9c12b57cd67d8b584ce53e0eae5fba3c91e29d3ab6134b2a33ba95c`,
  identical to the installed/committed file. The wrappers compare actual HEAD
  before project runtime; storage runtime also rejects any commit other than
  the frozen one.
- Canonical manifests: for shared GREEN, node-local negative, and shared
  repeat, `manifest.json` and `manifest.yaml` are byte-identical. Each contains
  `node_type=cpu_compute`, queue `debug-g`, group `xg24i002`, full code/config
  identities, config paths, nodefile, modules, filesystem identity, permission
  probe, and completed rank/role/host/job/test-root mapping.
- Target path: `findmnt`, `df`, and `stat -f` identify
  `/work/xg24i002/x10041/fsbdd/runtime_runs` as read/write Lustre backed by the
  recorded four-endpoint `/lustre/work` source. `lfs getstripe`, group quota,
  permissions, and create/read/delete/removal evidence are preserved.
- Static safety: all three Stage 0 PBS wrappers pass `bash -n`. The storage
  wrapper uses `set -eEuo pipefail`, in-job module load/list, skill/config
  checks, a distinct-host check, `/usr/bin/env` rather than conflicting Open MPI
  export mechanisms, expected-negative handling, and checksum finalization
  after the final log line.

## Maker 未列出的反例

1. A checksum-valid directory can still have a run ID whose `...Z` prefix is a
   planned or mislabeled time rather than the real UTC submission instant. I
   excluded every such historical candidate from closure. Every replacement
   package used above has a run timestamp exactly equal to its scheduler
   `qtime`: `15:40:36`, `15:40:53`,
   `15:41:11`, `15:42:01`, `15:42:17`, or `15:47:49` UTC.
2. A role map can contain the right host/rank/role sets yet swap the rank-to-role
   association. I therefore did not rely on set-level manifest validation
   alone: source fixes rank 0 as writer and rank 1 as reader, raw rank records
   show that association, the finalized maps reproduce it, and the scheduler
   host set agrees. The counterexample is absent from all closure packages.
3. A node-local negative could be mislabeled successful after an unrelated MPI
   or config failure. The summarizer requires two distinct-host role records
   and specifically the pair `acknowledgement_timeout` and
   `visibility_timeout`; job `2383217` has exactly those failures and MPI exit
   `2`, while the enclosing expected-negative job exits `0`.

## 运行证据

- Immutable package verification: every entry in all seven closure-package
  checksum manifests passes. Primary checksum-manifest hashes are provenance
  RED `a15a193cf00747ee5471d3fd890880887dcd8b6541eef7f94a81ecee20948f8b`,
  focused HARDEN `356c24f6980791a6d20c844cf9a2738eb6a0afc047c89e14e7ec9a0bb68af7ea`,
  full HARDEN `525f8403570050f6c9d0daf609ea778a91f35be77eb8aab28858ee50c4a4091f`,
  node-local negative `6facd1d6db87e9310f476d7643362ce04083decabc87c7291b0566330361d4da`,
  shared GREEN `cac18b65c3acb3a91161739a1e40eb5bdedb97282fe3ac2219fbb4e208c38a27`,
  and shared repeat `49f40dea4e0cad779462213a10d157ae4196031f2792a8bfc7e90f61cad3ec60`.
- Scheduler identity: `qstat -f -H` confirms every primary job is finished with
  scheduler exit `0`, queue `debug-g`, group `xg24i002`, the declared select
  count, and the same run ID/nonce/digest/skill identity in `Variable_List`.
  Jobs are `2383209` (`mg0006`), `2383213` (`mg0016+mg0017`), `2383217`
  (`mg0016+mg0017`), `2383227` (`mg0016`), `2383231`
  (`mg0016+mg0017`), and `2383254` (`mg0015`).
- Focused/full tests: job `2383209` reports `Ran 8 tests in 0.010s — OK`;
  job `2383227` reports `Ran 41 tests in 0.515s — OK`. Both test exits and PBS
  exits are `0`.
- Shared GREEN: writer `mg0016` and reader `mg0017` agree on 4096 bytes and
  SHA-256 `684c60c5704c1bc036325224f7ff200cbc3e21631bfb94b6aa9e6459bcdc2ec2`
  below the frozen Lustre root; both role and MPI exits are successful.
- Independent shared repeat: a new immutable package on the same two distinct
  hosts agrees on 4096 bytes and SHA-256
  `f7d7d2030481fb8d211304015a3dc0be641db294bd88dca18b3fb792b5d4e48a`.
- Node-local negative: writer `mg0016` cannot observe an acknowledgement and
  reader `mg0017` cannot observe the visibility record under the same absolute
  `/tmp` name; the summary is `node_local_visibility_rejected`.
- Claims boundary: reader-local smoke waits (`1.448ms` and `1.357ms`) and
  writer-side acknowledgement waits (`64.333ms` and `28.426ms`) are not used as
  a `BENCH-01` distribution or cross-host clock measurement. No 100000-replace,
  metadata-throughput, bandwidth, loss, training, GPU, global-cycle, or 9-node
  claim is made.

## Acceptance 判定

| ID | Evidence | Conclusion |
|---|---|---|
| `BENCH-01..04` supporting harness | Frozen axes, deterministic payload, compute-only launcher, target-path identity, clock-valid immutable runtime packages | `SUPPORTING ONLY`; `A-BENCH-01..04` remain `NOT_EVALUATED` |
| `STOR-01` harness behavior | Payload-first/visibility-last atomic publication, two shared successes, exact node-local failure | `PASS` for the S0B-01 harness scope |
| `FS-08` environment provenance | Actual Lustre/PBS/module/path/permission evidence, clean installed skill identity, complete canonical manifests | `PASS` |
| S0B-01 single objective | Repeatable compute-only harness on the frozen target root with independently bound provenance | `PASS` |

## Follow-ups

None for S0B-01 closure. S0B-02 must evaluate `A-BENCH-01/02` (including the
frozen `p99 <= 2.5s` threshold and at least 100000 atomic replacements with
zero violations). S0B-03 must evaluate `A-BENCH-03/04`. These are required next
loops, not evidence already claimed by this verdict.

## 签署

- Checker session/date: fresh independent re-Checker `/root/s0b01_rechecker` / 2026-07-15 Asia/Tokyo.
- initial host/workflow: `miyabi-g1`, Miyabi login/control-plane static-only review.
- checked commit: `7681510482548c1f1c8c58f50b2510f72bcbeb43`.
- runtime source commit: `dce034a684993cfd84858ebd09126b4f17863ba1`.
- blocked history: `reports/checkers/S0B-01-blocked-1.md` remains the immutable first verdict for `e6ab81a`; this report supersedes it only for the corrected candidate and closure set above.
- report hash: compute externally after persistence; a file cannot contain its own stable digest.
