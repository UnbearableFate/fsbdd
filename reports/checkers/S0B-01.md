# Checker Report：S0B-01/49cbf0e

## Verdict

`PASS`

The corrected candidate closes both blockers from
`reports/checkers/S0B-01-blocked-1.md`. The actual installed
`miyabi-development` commit is bound and independently evidenced, and each
two-node run now has byte-identical canonical JSON/YAML manifests containing
the required compute, PBS, code/config, and actual role-placement identities.
The corrected RED, full HARDEN, node-local negative, and shared positive
packages are complete and checksum-valid. This verdict closes the S0B-01
harness/readiness loop only; it does not claim any `A-BENCH-01..04` result.

## 独立输入

- source hashes：`RESEARCH_PLAN.md = 2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`；`STAGE0-4_SPEC.md = fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`。
- loop card：`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/loops/stage0/S0B-01.md`，SHA-256 `96609382fd71c3c14e387b48dc25c04e6b2e1af32b2a3651915cfa9f98b642d6`。
- checker template：SHA-256 `0cf29aa65c6a0d691cdb2661e5cacc3f2beb1619d729e98add082e36539ae297`。
- checked ancestry：base `5a8b78c9765c1b13c5b21006a5243a32e2afc19d` → original RED `6b901c99cd34e90c782234d063c697947d4f7f34` → first blocked candidate `e6ab81ada6306cc5a6f7df0d27d179305c01901f` → manifest repair `23f5eecf9d9ba25ff6fb64cf74db37d0ee5bc40f` → provenance repair `dce034a684993cfd84858ebd09126b4f17863ba1` → checked candidate `49cbf0e385411997f0fc5167b84752ec83e50533`。Ancestry and both `git diff --check 5a8b78c..49cbf0e` and `e6ab81a..49cbf0e` pass.
- candidate artifact hashes：resolved config `9a1a73f4b06649df26708c8a76894723324532d9f4d693ff2926b044b76fc270`；harness `885ef492b43c1e6e6e3bb3a3a0a05b9eb6e97c41dba324a0df77b947891ad8f2`；storage PBS `2b1609e4162bd3ddf0b6eaa5c097da5f5652b01bde26f0a9ce98231f50c0a0cb`；storage tests `65fb71327bdb69d1342fe34e967900649fc5f99303433082293504e3e2184ec3`；environment report `c7bb38486f5d1b7d47b2913d7c679e5b92c034bd884553557c7a2ead5a3c14b6`。
- corrected RED：`evidence/raw/S0B-01/20260714T154100Z-red-provenance-n4e2a-99cd2ed9f546`。
- corrected full HARDEN：`evidence/raw/S0B-01/20260714T154101Z-harden-provenance-n6c3f-5a1a87a340ce`。
- corrected node-local negative：`evidence/raw/S0B-01/20260714T154102Z-red2-provenance-n8a5d-2018e813ad8e`。
- corrected shared positive：`evidence/raw/S0B-01/20260714T154103Z-green-provenance-n1b7e-2018e813ad8e`。
- 9N package：not applicable before `S1-13`。

## 重新构建的目标

- 单一目标：在真实 Miyabi PBS compute nodes 上冻结后续 protocol `RUN_ROOT`，记录真实环境身份，并建立能区分共享 Lustre 与 node-local storage 的可重复 writer/reader harness。
- 受影响条款：`BENCH-01..04` 的 supporting harness、`FS-08`、`STOR-01`。本 loop 不关闭 `A-BENCH-01..04`；S0B-02 负责 visibility/atomicity，S0B-03 负责 metadata/bandwidth。
- 用户补充门禁：使用实际 `miyabi-development` checkout，login node 仅作控制面，runtime 只在 PBS compute allocation；Stage 0 不适用 9 节点训练门禁。
- Checker 边界：初始 host `miyabi-g1`、无 PBS allocation；只执行 Git、SHA-256、JSON、静态 shell 与 PBS history 检查，没有运行 project Python、pytest、benchmark、MPI 或重型 import。

## RED 有效性

- 原始 pre-implementation RED job `2383084.opbs` 使用 commit `6b901c99...`，于 `00:13:31 JST` 在 `mg0017` 启动；production implementation commit `4c85b2e3...` 到 `00:19:04 JST` 才出现。三项确定性失败分别证明 naive harness 错误允许 login host、遗漏七项 compute/FS manifest identity、并把任意绝对 `/tmp` 路径当成 shared。exit `1`，运行 `0.002s`，不是 timeout 或 import failure。
- provenance-repair RED job `2383210.opbs` 在 `mg0016` 从 `dce034a...` 重复同三项特定失败，保留原始 RED ordering，只修复 skill/config/snapshot provenance；PBS wrapper按 expected-failure contract exit `0`。
- 两个 RED 都保留命令、exit、stdout/stderr、source/config snapshots。修复 RED 的 config-digest preimage重算为 `99cd2ed9f546b77e37f39c1977a47501d50899f5d9b9a6fef00fcbc2cfb6595c`。

## 代码与协议检查

- scope：candidate 增加/加固 Stage 0 storage config、stdlib harness、PBS launchers、focused tests、decision draft 与 blocked history；没有 learner/syncer training semantics、Torch version、model/data 或 topology expansion。`dce034a..49cbf0e` 只修改 environment report，故 corrected runtime snapshots仍精确对应 executable/config commit `dce034a`。
- data plane：Open MPI only places one role per allocated host. Payload、visibility record、acknowledgement和 result records均通过被测 filesystem；没有 MPI/torch.distributed application data plane。
- bounded state/discovery：smoke 使用单一 run-scoped payload、visibility record、ack 和 fixed two-role results；unique nonce/digest run ID 与 fail-if-exists evidence directory防止覆盖旧包。
- atomic publication：payload/JSON 写入 same-directory exclusive temp，flush + file `fsync`，`os.replace`，再 directory `fsync`；payload完成后才发布 visibility record。reader验证 run ID、peer host、bytes 与 SHA-256。
- config/identity：corrected two-node packages的 preimage均重算为完整 digest `2018e813ad8e7b5bca742b8e1f51f94e9ed784acad83b6aea1a3965b0f017b39`。三个 input snapshots分别与 `dce034a` 的 config、harness、PBS blobs一致，且 manifest记录完整 code commit、64-hex digest与三条 config paths。
- skill provenance：四个 corrected packages都记录 `c3ddfa47cadfd9132edf939d359b90b28ab5f8ed`，captured status为零字节（clean），captured `SKILL.md` SHA-256均为 `60a62d28a9c12b57cd67d8b584ce53e0eae5fba3c91e29d3ab6134b2a33ba95c`，与该 skill commit的 blob一致。wrappers在 project runtime前比较 installed checkout HEAD。
- canonical manifests：两个 corrected two-node packages的 `manifest.json` 与 `manifest.yaml` byte-identical。两者包含 `node_type=cpu_compute`、`debug-g`、`xg24i002`、full code/config identities、config paths、nodefile/modules/filesystem，以及由 completed smoke summary finalization绑定的 two-rank actual role map。
- static safety：`stage0_storage_smoke.pbs`、`stage0_cpu_tests.pbs` 与 `stage0_simulation_sweep.pbs` at `49cbf0e` all pass `bash -n`。storage PBS uses `set -eEuo pipefail`, compute/PBS guard, in-job module load/list, config/skill checks, distinct-host check, `/usr/bin/env` MPI propagation, expected-negative handling, and writes the final log line before checksums。

## Maker 未列出的反例

1. A manifest can name ranks `0/1` and roles `writer/reader` yet bind a host outside the PBS nodefile or a different job ID. The validator rejects host-set or PBS-ID mismatches; both corrected manifests' role mappings exactly equal their smoke summaries, nodefiles, and independently observed scheduler hosts.
2. A node-local run could be labeled a valid negative after an unrelated launcher/config failure. The summarizer requires both role records on distinct hosts and specifically the pair `visibility_timeout` plus `acknowledgement_timeout`; job `2383214` has exactly those failures and MPI exit `2`, while the enclosing expected-negative job exits `0`.

## 运行证据

- 全量 checksum verification：all entries pass。`checksums.sha256` file hashes are RED `b971dcfe55deb0f2f1e863a55bbcb53d4f035149aad6eeb6f136400e70b10d55`；HARDEN `6d18a86ed1ffb6f2285e2249b361279218823dead900ba8c26b58ca3aff3d8c4`；node-local negative `790573e68b933acf1d96659bba416f5344ff9c62cbe12713ee0a8aeffa018f8c`；shared positive `a531439f278a579bba13e22581855e9b3b1cf4d20c036730d820fd4130506b9f`。
- Scheduler identity：`qstat -f -H` confirms all four jobs finished with scheduler exit `0`, queue `debug-g`, group `xg24i002`。Jobs/hosts are `2383210: mg0016`、`2383212: mg0006`、`2383214: mg0018+mg0030`、`2383216: mg0031+mg0032`；requested select counts are `1/1/2/2` and walltimes are `1s/1s/27s/2s`。
- full HARDEN：job `2383212` runs the full Stage 0 oracle/simulation/storage target list and reports `Ran 41 tests in 0.509s — OK`；test exit `0`。Its 13-file config preimage reproduces `5a1a87a340cec3f50e7bbe42e6747d91cc9fb4cc1bd396d9d856590b3d3c4b55`。
- node-local negative：writer on `mg0018` times out on acknowledgement; reader on `mg0030` times out on visibility using the same absolute `/tmp` name。summary is `node_local_visibility_rejected`。
- shared positive：writer `mg0031` and reader `mg0032` agree on 4096 bytes and SHA-256 `08274e65c07292386ed706ac8fb2824dd934bff2e6c25e5604c8033bf5400a39` under `/work/xg24i002/x10041/fsbdd/runtime_runs`；summary is `shared_visibility_confirmed`。`findmnt` records read/write Lustre and permission probe records create/read/delete plus removal。
- Claims boundary：the `1.935ms` reader-local wait and `143.803ms` writer acknowledgement wait are explicitly smoke-handshake observations, not a BENCH-01 distribution or cross-host clock measurement. No atomicity-count, metadata throughput, bandwidth, loss, training, GPU, global-cycle, or 9-node claim is made。

## Acceptance 判定

| ID | 证据 | 结论 |
|---|---|---|
| `BENCH-01..04` supporting harness | frozen axes, deterministic payload, writer/reader launcher, target-path identity and corrected runtime packages | `SUPPORTING ONLY`; no `A-BENCH` closure claim |
| `STOR-01` harness behavior | payload-first/visibility-last publication, shared positive, reason-specific node-local negative | `PASS` for S0B-01 harness scope |
| `FS-08` environment provenance | actual Lustre/PBS/module/path/permission evidence, actual clean skill checkout and complete canonical manifests | `PASS` |
| S0B-01 single objective | compute-only repeatable harness on frozen target root with independently bound identity | `PASS` |

## Follow-ups

None for S0B-01 closure. The planned next gaps remain S0B-02 (`A-BENCH-01/02`)
and S0B-03 (`A-BENCH-03/04`); they are separate required loops, not defects in
this harness verdict.

## 签署

- Checker session/date：fresh independent rechecker `/root/s0b01_rechecker` / 2026-07-15 Asia/Tokyo。
- initial host/workflow：`miyabi-g1`, Miyabi login/control-plane static-only review。
- checked commit：`49cbf0e385411997f0fc5167b84752ec83e50533`。
- blocked history：`reports/checkers/S0B-01-blocked-1.md` remains the immutable first verdict for `e6ab81a`; this report supersedes it only for the corrected candidate/evidence above。
- report hash：compute externally after persistence; a file cannot contain its own stable digest。
