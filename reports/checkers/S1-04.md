# S1-04 Independent Checker Report

## Context and verdict

- Checker context: `/root/s1_04_checker`
- Maker chat inherited: no
- Checked candidate: `1ff459f3b4fa500ccd9adf2ee850f39a5e4cd818`
- Selected package: `runtime_runs/S1-04/formal-l2`
- Phase A: `ADMISSIBLE` after correction cycles
- Phase B: `PASS`

The same independent Checker context performed both phases. It did not edit
production, test, plan, or evidence files and did not launch the formal job.

## Phase A correction history

The Checker rejected pre-submission candidates before any L2 run for three
material reasons:

1. The authority-ID parser omitted `GLOBAL` and `SYNC`, so the requirements
   command failed while an early package validator still mechanically admitted
   the package.
2. The PBS script invoked project Python before the required Miyabi module
   setup.
3. The deterministic cross-node state used placeholder config/model/map
   identities and did not make its run identity byte-identical to the manifest.

The final candidate generalizes the authority grammar and adds its regression,
binds the internal base-content identity, initializes modules before project
runtime, and freezes canonical deterministic state identities in the evidence
contract. Writer and reader recompute or consume those identities, report them,
and the analyzer and manifest builder reject any mismatch. The exact clean
preflight `runtime_runs/S1-04/preflight-clean-1ff459f` then passed 9/9
requirements, 79 tests, the 10,000-object protocol, and final validation
(17/17 files; manifest `ba513ee6...e526`). Phase A authorized exactly one L2
submission.

## Phase B package audit

- PBS `2384764.opbs`: `Exit_status=0`, no abnormal nodes, 32 seconds walltime.
- All 28 checksum entries verify: manifest plus 27 payload files.
- Validator: `admissible`, 27 listed = 27 actual files.
- Manifest SHA-256:
  `1aae5431a175dd837e934bd21c1bf366f8baa2eb1e7a7c52e1c2ce388957d288`.
- PBS stderr and all six package stderr logs are empty.
- Writer rank 0 ran on `mg0018`; reader rank 1 ran on `mg0019`. Nodefile,
  role records, summary, and manifest agree, and `/work` is shared Lustre.
- PBS qtime `Wed Jul 15 09:34:44 2026` interpreted as JST converts to
  `2026-07-15T00:34:44Z`, exactly matching scheduler and manifest timestamps.
- Run `s1-04-2384764.opbs`, config `dd1dc08d...a3c50`, model
  `d91a92be...82100`, and fragment map `d1fb0964...b2ab` agree across the
  frozen contract, command, both roles, summary, and manifest checks.
- Requirement validation is 9/9 complete and the full Stage 1 suite is
  `79 passed`.

The runtime interrupted bootstrap after two of four fragments. The other node
rejected the partial snapshot. Resume preserved indices 0/1 and published only
2/3; the final vector is `[0,0,0,0]`. Identical bootstrap created zero new
payloads and conflicting version-zero content was rejected without changing
authority. Four current records reference four payloads and retain four bases,
with no global head. Complete snapshot reads remain exactly four both before
and after 10,000 unrelated payload objects.

## Requirement judgment

All nine S1-04 matrix close conditions match the implementation and selected
evidence. The verdict is `PASS` with these scope boundaries:

- `SYNC-02` passes here for per-fragment current parameters/version/outer/base
  state. Consumed-frontier and readiness/grace state remains later work.
- `FS-06` passes for bounded authoritative references and embedded bases, not
  physical reclamation of orphan candidates.
- `A-GLOBAL-02` receives component evidence for version zero and exact `+1`;
  atomic consumed-frontier closure remains S1-11.
- S1-04 claims no learner training, outer-update execution, or nine-node gate.

## Final verdict

`PASS`
