# Checker Report: S1-03

## Verdict

`PASS`

The selected replacement L2 package proves the S1-03 storage-publication
primitive on the target Miyabi shared Lustre filesystem. Identity, checksum,
topology, application observations, interruption outcomes, tests, requirement
coverage, and component-only acceptance handling all agree. No follow-up
affects S1-03 closure.

## Independent inputs

- Same independent Checker context used for both Phase A cycles and Phase B:
  `/root/s1_03_checker`; Maker chat inherited: `false`.
- Executed code commit:
  `e5fc7db5a5f52ec7c407e4867fffc1037d831c5b`.
- Checked persistence candidate:
  `3552c9bdd8f27df2db128ce19511bd14b26f60dd`.
- Diff `e5fc7db..3552c9b`: evidence index, acceptance traceability,
  loop state, storage report, and the already committed Phase A report only;
  no production source, test, runtime config, PBS, dependency, algorithm, or
  Torch change. `git diff --check` passed.
- Selected package: `runtime_runs/S1-03/formal-l2-r2`.
- Preserved but rejected package: `runtime_runs/S1-03/formal-l2`.
- Checker host remained `miyabi-g1` without a PBS allocation. Phase B used
  static Git/file/hash/shell inspection only; it did not execute project
  Python, tests, training, or GPU work.

## Phase A lineage

The initial Phase A admitted the original two-node protocol. Its first L2
application observations passed, but Phase B selection was stopped when the
raw local-JST PBS qtime was found mislabeled as UTC. A target regression failed
against that parser, the script was corrected to assign JST before conversion,
and the same Checker context issued a second `ADMISSIBLE` verdict for exactly
one replacement run at `formal-l2-r2`. Full Phase A history and hashes remain
in `reports/checkers/S1-03-phase-a.md`.

## Findings, ordered by severity

### INFO — selected package identity is complete and internally consistent

- The finalized manifest binds clean code commit `e5fc7db`, branch/repository,
  canonical config digest, both unchanged authority hashes, Miyabi skill
  repository/commit, absolute clean project and selected evidence paths, PBS
  job/queue/group, modules, nodefile digest, initial/compute host, and exact
  declared/actual role maps
  (`runtime_runs/S1-03/formal-l2-r2/manifest.json:24-96`).
- The manifest selects `formal-l2-r2`, while PBS `Variable_List` and submission
  arguments independently record that root and exact executed commit
  (`runtime_runs/S1-03/formal-l2-r2/env/qstat.txt:55-73`). The clean-worktree
  status evidence is empty, matching `dirty=false`.
- Manifest timestamp and scheduler qtime are both
  `2026-07-14T23:21:05Z`
  (`runtime_runs/S1-03/formal-l2-r2/manifest.json:75-93`). Independent parsing
  of raw `qtime = Wed Jul 15 08:21:05 2026` as JST yields exactly that UTC
  instant (`runtime_runs/S1-03/formal-l2-r2/env/qstat.txt:29`). The corrected
  time-source identity is therefore valid.

### INFO — finalized evidence inventory is admissible and independently reproducible

- Independent `sha256sum -c` passed all 26 entries (manifest plus 25 evidence
  payloads) in
  `runtime_runs/S1-03/formal-l2-r2/checksums.sha256:1-26`.
- Recomputed hashes agree with the evidence index: manifest
  `66ffade28606c3f5b6d855bbff542299f90c33df84c389c9d2e9bbef75632c76`,
  checksums file
  `bb58e8bd84dff5a0ac545d63cfc7b2436af96e68aee9fa673c95f581ff17cc47`,
  validation summary
  `6117466e657a609eb46379eac77192161e5cbe129ed81b1e7c76dc04fed0d1dc`,
  storage summary
  `c34488dd01fbb5b832ab6b098848c87569a1fbe6061a033de35eb0d68dc6416d`,
  tests `26f5142f...14f`, and requirements `52541fb1...9540`
  (`evidence/indexes/S1-03.md:46-56`).
- Validator status is `admissible`, errors are empty, and listed/actual files
  are exactly 25/25
  (`runtime_runs/S1-03/formal-l2-r2/validation-summary.json:1-7`). All six
  runtime stderr files are empty.

### INFO — two-node target-filesystem protocol produced the required observations

- PBS allocated `mg0026` and `mg0027` on `debug-g` with group `xg24i002`; the
  captured nodefile contains those two distinct hosts and its digest equals the
  scheduler-bound digest. The manifest and role records map writer rank 0 to
  `mg0026` and reader rank 1 to `mg0027`
  (`runtime_runs/S1-03/formal-l2-r2/env/compute-hosts.txt:1-2`;
  `runtime_runs/S1-03/formal-l2-r2/runtime/roles/writer.json:1-11`;
  `runtime_runs/S1-03/formal-l2-r2/runtime/roles/reader.json:1-13`).
- `findmnt` identifies `/work` as the expected shared Lustre mount
  (`runtime_runs/S1-03/formal-l2-r2/env/findmnt.txt:1-2`). MPI launched the two
  roles only; the analyzer records shared-POSIX-only data plane and
  filesystem-only application coordination
  (`runtime_runs/S1-03/formal-l2-r2/analysis/storage-summary.json:1-2,36-42`).
- The writer completed 500 immutable 128-KiB publications. The reader made
  1,428 valid observations, saw minimum sequence 0, maximum/final sequence 500,
  and 501 unique sequences, with `invalid_reads=0` and `errors=[]`
  (`runtime_runs/S1-03/formal-l2-r2/runtime/roles/reader.json:1-13`;
  `runtime_runs/S1-03/formal-l2-r2/analysis/storage-summary.json:29-35`).
- Fixed-slot discovery reports zero directory scans and zero payload-history
  scans (`runtime_runs/S1-03/formal-l2-r2/analysis/storage-summary.json:37-40`).
  Phase A's independent static source review established that this matches the
  actual `StorageBackend.read` path rather than serving as a standalone
  self-assertion.

### INFO — interruption and negative-control evidence establish old-or-complete-new visibility

The selected crash matrix yields complete-old before payload write, after
payload write, and before record replacement; after record replacement it
yields complete-new. Each outcome records the corresponding whole-payload
identity (`runtime_runs/S1-03/formal-l2-r2/analysis/storage-summary.json:3-27`).
The summary also binds the reused S0B-02 unsafe direct-overwrite control and its
5,208 detected partial/torn reads
(`runtime_runs/S1-03/formal-l2-r2/analysis/storage-summary.json:54-58`). This
provides the required counterexample contrast without changing detectors.

### INFO — GREEN/HARDEN and requirement coverage pass

The selected run reports `68 passed` for the complete Stage 1 suite
(`runtime_runs/S1-03/formal-l2-r2/stdout/pytest.log:1-2`). Requirement
validation reports exact 9/9 coverage for `STOR-01`, `FS-01/02/03/07`,
`PROP-02`, `INV-04`, `A-PROP-01`, and `A-GLOBAL-01`
(`runtime_runs/S1-03/formal-l2-r2/analysis/requirements.json:1`). The runtime
summary is `status=pass` and all promised observations are present.

### INFO — persistence preserves the acceptance boundary

- The evidence index accurately records selected identity, correction history,
  runtime counts, package hashes, requirements, and limitations
  (`evidence/indexes/S1-03.md:3-85`). Its listed package hashes independently
  recompute exactly.
- `A-PROP-01` remains primary-loop `S1-05`, status `not_started`, Checker
  `PENDING`; S1-03 contributes only zero-partial-read storage evidence.
  `A-GLOBAL-01` remains primary-loop `S1-11`, status `not_started`, Checker
  `PENDING`; S1-03 contributes only compound-payload visibility evidence
  (`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/plans/ACCEPTANCE_TRACEABILITY.csv:13,16`).
- The index repeats those component-only boundaries and identifies proposal
  business closure as S1-05 and params/outer/frontier closure as S1-11
  (`evidence/indexes/S1-03.md:58-67,77-85`). No full acceptance status is
  incorrectly claimed by this loop.
- The old `formal-l2` package remains preserved as immutable correction history
  and is explicitly excluded from selection in the evidence index and loop
  state (`evidence/indexes/S1-03.md:21-28,82-85`;
  `plans/FS_DILOCO_CODEX_EXECUTION_PLAN/plans/loop_states/S1-03.yaml:31-38`).

## Requirement disposition

| ID | Phase B disposition |
|---|---|
| `STOR-01` | `PASS` — fixed-slot interface; atomic record replacement plus eventual payload readability only |
| `FS-01` | `PASS` — immutable payload, small visibility slot, no event-log/history discovery |
| `FS-02` | `PASS` — verified payload-first, visibility-last; zero invalid cross-node observations |
| `FS-03` | `PASS` — one record binds one complete compound payload and identity metadata |
| `FS-07` | `PASS` — identity/schema/size/checksum validation and corruption tests pass |
| `PROP-02` | component `PASS` — storage publication completeness; proposal business semantics remain S1-05 |
| `INV-04` | component `PASS` — storage reader cannot discover incomplete payloads |
| `A-PROP-01` | component `PASS`; primary acceptance remains `not_started` for S1-05 |
| `A-GLOBAL-01` | component `PASS`; primary acceptance remains `not_started` for S1-11 |

## Scope and remaining work

S1-03 intentionally does not implement proposal latest-wins/eligibility,
global-state/frontier business semantics, bounded state GC, learner/syncer
training, or a 9-node gate. Those are assigned to later loops and are not
defects or follow-ups for this storage primitive. The next planned gap is S1-04
bootstrap and bounded per-fragment current/base state.

## Sign-off

- Phase A final authorization: `ADMISSIBLE` at report commit
  `1b4023004fae2011c0c8608f7526170b0ecaa03d`.
- Phase B verdict: `PASS`.
- Checked executed commit: `e5fc7db5a5f52ec7c407e4867fffc1037d831c5b`.
- Checked persistence commit: `3552c9bdd8f27df2db128ce19511bd14b26f60dd`.
- Final report hash is recorded externally after committing; it is not
  self-embedded.
