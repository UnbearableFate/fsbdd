# S1-06 Independent Checker Report

## Context and verdict

- Checker context: `/root/s1_06_checker`
- Maker chat inherited: no
- Checked candidate: `016d16e98d63a22824ca3e5c528c09887d243636`
- Selected package: `runtime_runs/S1-06/formal-l1`
- Scheduler supplement: `runtime_runs/S1-06/scheduler-2385434`
- Phase A: `ADMISSIBLE` after one correction cycle
- Phase B: `PASS`

The same independent Checker context performed the pre-submission and final
reviews. It did not edit repository files, submit jobs, or create another
Checker context.

## Phase A correction history

The first implementation candidate established the real HF training and
materialization path, but a Maker dry run found that the manifest subcommand
wrote its draft and then failed while serializing an `argparse` `Path`.
Candidate `c859aa872e7b3effd0ab5cc0ff0d606c2ecef6ae` fixed that formal-launch
blocker, recorded queue/group/node-type provenance, and aligned padding labels
with the frozen loss-bearing-token definition.

The Checker then blocked `c859aa8` for two material evidence gaps:

1. A test claimed disjoint RNG and cursor state without exercising either, and
   the runtime still consumed process-global Torch RNG directly.
2. The required slow-data test introduced no delay, and no finite-forward /
   nonfinite-backward fixture exercised overflow rejection.

Candidate `016d16e` adds a bound, serializable `LearnerRng` whose CPU/CUDA
states execute inside a forked Torch RNG scope and whose process-global state is
verified restored. Run records expose the seed, owner process, activation
count, and before/after state digests. Tests reject RNG aliasing, round-trip its
state, prove ambient RNG restoration, and prove independent packed-shard cursor
advancement. The delayed source now contributes a measured 20 ms wait to the
real monotonic step interval. A finite-loss custom autograd fixture returns an
infinite gradient and proves that clipping fails before parameter or progress
mutation. The Checker declared the exact clean candidate admissible before the
single formal submission.

## Phase B package audit

- PBS `2385434.opbs` ran on one GPU compute node, `mg0019`, in
  `debug-g` for group `xg24i002`.
- Scheduler history reports `Exit_status=0`, no abnormal nodes, and 84 seconds
  walltime. PBS stderr is empty.
- PBS qtime `2026-07-15 11:41:20 JST` independently converts to
  `2026-07-15T02:41:20Z`, exactly matching the manifest submission timestamp.
- Manifest SHA-256 is
  `33ed8ee47ecc57afe6669be071d812576931e4f54e39dbf8424df88ccee74390`.
- The package validator reports `admissible`, with 48 listed and 48 actual
  payload files. All 49 checksum entries (manifest plus payloads) verify.
- Every packaged stderr file is empty.
- The manifest binds the exact clean candidate, profile-set digest
  `0601217f...f7a`, source hashes, Miyabi skill commit, absolute paths,
  node type, job/queue/group, module inventory, and identical declared/actual
  role maps.
- Requirement validation is 6/6 complete and the formal Stage 1 suite reports
  `140 passed`.

Both frozen materializations reproduce their exact manifest hashes:
`9308c7ca...ce94` for GPT-2/WikiText and `1c969197...b6e4` for
Pythia/FineWeb. The tiny, GPT-2, and Pythia runs each expose ten complete safe
boundaries, finite token-weighted losses, finite gradient and per-fragment
update norms, changed parameter digests, explicit local-step scheduling, and no
initialized Torch distributed process group. Their token totals, fragment
counters, loss numerators, and common-interval throughput formulas reconcile
independently. Three distinct process IDs own the three recorded RNG streams,
and both real packed-shard cursors advance from position 0 to 40.

## Requirement judgment

All six S1-06 matrix close conditions pass:

- `LEARN-01`: complete model/optimizer/progress ownership, bound serializable
  RNG state, and deterministic independent shard cursors.
- `LEARN-02`: the core depends only on its batch iterator and has no peer,
  syncer, proposal, quorum, collective, or storage barrier.
- `PROG-04`: optimizer steps, input tokens, loss-bearing target tokens,
  fragment versions, fragment-local steps, and fragment-local tokens remain
  distinct and reconcile after accumulation.
- `PROG-05`: the scheduler advances once per completed optimizer step and
  every event labels that basis explicitly.
- `TEL-01`: monotonic per-step intervals, per-profile common-interval
  throughput, token totals, and explicitly inactive future transfer metrics
  are present without a synthetic combined rate.
- `TEL-04`: every completed update binds a finite token-weighted loss to
  observed parameter changes and fragment-specific norms with honest comparison
  labels.

## Scope boundaries

S1-06 does not publish proposals, adopt global fragments, run peer learners, or
claim transfer/publication latency. Those metrics remain explicitly inactive
until S1-07/S1-08. It freezes and smoke-tests the 160M/FineWeb long-run profile
and scalable materializer, but the M=4, real-filesystem, at-least-one-billion
token execution remains a later Stage 1 gate.

## Final verdict

`PASS`

