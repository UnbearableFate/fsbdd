# Checker Report：S0C-02/a470dcd

## Verdict

`PASS`

S0C-02 的单一目标通过，先前 `reports/checkers/S0C-02-blocked-1.md` 的两个阻塞项均已解除。候选 effective-token 排序不再经 binary64 除法，`2^53` 与 `2^53+1` 可被精确区分；初版 requirement-to-evidence map 现包含 loop 卡全部六个规范条款，且完整性测试从 loop 卡解析 authoritative requirement set。原始 RED、修正后 GREEN/HARDEN 和 S0C-01+02 full regression 的不可变证据均有效。

## 独立输入

- source hashes：`RESEARCH_PLAN.md = 2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`；`STAGE0-4_SPEC.md = fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`。Checker 在 Miyabi login/control-plane 静态重算一致。
- loop card：`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/loops/stage0/S0C-02.md`，SHA-256 `2d8e494773191ae7fc007e087fa4e1100c909bda58bb6356cf22252ff5093ba4`。
- code commit/diff：base `1c41a9025a125d6b8140ddb048c49a6d9dea7028`；checked commit `a470dcdb32a6c4b8a6e2e6f756903946ef36aefc`；8 files，907 insertions。`git diff --check 1c41a90..a470dcd` 通过；Checker 报告落盘前工作树 clean。
- resolved config：stdlib-only CPU oracle；JSON decision vectors；显式 float32 merge/outer-transition rounding；candidate policy `S_max/Q/Q_fresh/max_contributors/lambda_s`；候选优先级使用整数与 `Fraction` 精确比较。无模型、数据、Torch/HF、网络或 production storage/runtime 变更。
- RED：`evidence/raw/S0C-02/20260714T230000Z-red-n2a6c-d4f45a81dbc3`，commit `b6b33053abf206ebe9c84d1e7d0e75d254841d44`。
- corrected GREEN：`evidence/raw/S0C-02/20260714T234500Z-green2-n3c8a-dbe930890539`，commit `a470dcdb32a6c4b8a6e2e6f756903946ef36aefc`。
- corrected HARDEN：`evidence/raw/S0C-02/20260714T235000Z-harden2-n6f2d-dbe930890539`，commit `a470dcdb32a6c4b8a6e2e6f756903946ef36aefc`。
- corrected full regression：`evidence/raw/S0C-02/20260714T235200Z-regression2-n1e7b-1162d1efbaf1`，commit `a470dcdb32a6c4b8a6e2e6f756903946ef36aefc`。
- 先前结论：`reports/checkers/S0C-02-blocked-1.md`；旧 GREEN/HARDEN/regression 保持不可变，但不用于本次关闭。
- 9N package：不适用；Stage 0 且 `CAP_BASE_TRAINING` 尚未建立。

## 重新构建的目标

- 单一目标：建立 distinct-learner、consumed sequence/base、same-base once-only、publication-conditional frontier、direct weighted averaging、current-state application、PyTorch-style momentum/Nesterov reference transitions，以及 Stage 0 初版 requirement-to-evidence map。
- 受影响条款：loop 卡明列 `ORACLE-03`、`ORACLE-04`、`ORACLE-05`、`INV-05`、`PROP-06`、`OPT-04`；数值链同时涉及 `OPT-01`、`OPT-02`、`OPT-05`。
- Acceptance：`A-ALG-00`；S0C-01 提供 fresh/stale displacement、错误 current-relative stale 公式拒绝与 weighting supporting evidence，S0C-02 为 primary closure。
- 用户补充门禁：Stage 0 不跑训练或 9 节点门禁；Miyabi login node 不运行项目 Python。

## RED 有效性

- 错误实现/缺失实现是否失败：是。隔离 surrogate 的 4 个测试分别证明按文件数形成 quorum、same-base 仅检查 sequence、Nesterov 使用旧 buffer、以及缺少 `ORACLE-05` mapping 都会到达特异断言并失败；raw unittest exit code 为 1。
- 失败是否对应需求：是，直接对应 `INV-05`、`PROP-06`、`OPT-04`、`ORACLE-05`，不是 import error、timeout 或环境错误。
- 是否存在偶然 timeout/脆弱断言：未见。RED 为确定性小集合/小向量，4 tests 在 0.002s 完成；PBS wrapper 将预期 unittest failure 正确转换为 scheduler exit 0。

## 代码与协议检查

- scope/data plane：base-to-checked diff 仅含 Stage 0 oracle API、fixtures/tests、RED surrogate、orientation state、requirement/evidence CSV 与保留的 blocked Checker 报告。没有 learner/syncer/storage、训练、网络数据面或 Torch 版本变更。
- eligibility/one-per-learner：候选必须同时满足 `sequence > last_sequence` 与 `base_version > last_base_version`；future、too-stale、identity mismatch 被分类拒绝。代码先按 learner 去重，再按 distinct learner 计算 `Q/Q_fresh`，一次 update 中同 learner 最多一个 proposal。
- deterministic selection：排序严格为 lower staleness → larger effective tokens → newer sequence → fixed learner/proposal identity；3-proposal case 的全部排列产生相同结果。
- exact effective tokens：`lambda_s` 的实际 float 值经 `Fraction.from_float` 精确表示，正整数 token mass 与分母在有理数域比较，不经过 binary64 商。fresh proposals `a=(tokens=2^53, sequence=2)` 与 `b=(tokens=2^53+1, sequence=1)` 的 mass 分别保持为 `9007199254740992` 与 `9007199254740993`，所以 `b` 在 sequence tie-break 前胜出；修正测试在 GREEN、HARDEN、full regression 均通过。
- publication-conditional frontier：失败 publication 返回未推进的 frontier；成功时同一逻辑操作同时提交 selected sequence 与 base version。再次 polling 由 `consumed_sequence` 拒绝，new-sequence/same-base 由 `consumed_base` 拒绝；对非法回退 commit 直接报错。
- bounded state：每 learner/fragment 只保留 `last_sequence` 与 `last_base_version` 两个整数；1000 次成功 update 后仍是一个 `ConsumptionFrontier(999, 999)`，不保存历史。
- base-relative direct merge：每个 contribution 计算 `G_base - L_local`，float32 顺序累加 `sum(w_i*g_i)`。fresh control 的 weights `0.25/0.75` 产生 local average `[6.5, 2.5]`；mixed stale/fresh case 的 merged displacement 为 `[3,3]`。
- current application：mixed case 把 `[3,3]` 作用于 current `[12,-2]` 得 `[9,-5]`，没有回滚到 stale base。full regression 同时保留 S0C-01 的错误 `G_current-L_stale` 拒绝证据。
- Nesterov：首步 buffer 初始化为 gradient；后续 `b = mu*b + g`；Nesterov direction 为 `g + mu*b`，然后 `p = p - lr*direction`。两步 fixture 的 buffers 为 `[2,-4]`、`[0.8,-0.6]`，parameters 为 `[9.62,-4.24]`、`[9.648,-4.486]`，与 zero-dampening PyTorch SGD 顺序一致。`lr=1/momentum=0` 与 direct averaging 等价控制通过。
- requirement/evidence map：测试从 S0C-02 loop 卡的 `规范条款` 行解析 required set，并与 Stage 0 SIM/BENCH/ORACLE/acceptance set 合并。CSV 对 `ORACLE-03`、`ORACLE-04`、`ORACLE-05`、`INV-05`、`PROP-06`、`OPT-04` 均有非空 loop/evidence/status；另补有数值链的 `OPT-01`、`OPT-02`、`OPT-05`。删除任一 loop-card requirement 现在会使 completeness test 失败。

## Maker 未列出的反例

1. 失败 publication 后重放：初始 frontier `(last_sequence=5,last_base=3)`，fresh proposal `(sequence=9,base=4)` 被选择但 publication 失败时，frontier 必须仍为 `(5,3)`，同一 proposal 仍可重试；当前 `commit_consumption(..., False)` 返回未修改副本，满足此语义。成功 publication 后才推进到 `(9,4)`，随后重放被拒绝。
2. 独立 current-application 标量反例：current `10`、stale base/local `4/1` 给 displacement `3`，fresh base/local `10/8` 给 displacement `2`；等权 merge 为 `2.5`，`lr=1` 必须从 current 得 `7.5`，而从 stale base 应用会得 `1.5`，错误 current-relative stale 公式会得 `4.5`。当前 base-relative merge 与 current outer step 的组合选择 `7.5` 路径。

## 运行证据

- checksum manifests：Checker 对 `evidence/raw/S0C-02` 下全部 7 个 immutable run package 逐一执行 `sha256sum -c checksums.sha256`；每包 14/14 entries 全部 `OK`，且 checksum manifest 与除自身外的实际 14 个文件精确同集。用于本次关闭的 manifest SHA-256 为 RED `f9c1fdc0f54c8e8f97977b0b56b31456f521db32ac2ae8cc529ec2b072f1be21`、GREEN2 `9b57829ffb6eacea1bfde5714f97d61f13f556cd1c232e02734a33e25d33ef58`、HARDEN2 `b9348d89ba2d6d7c5535953a49fc4dfa7226db1fe3982bd88acc295ca7f9c01d`、regression2 `6080919bce0c0ec4f0204df32f5b278425e91e0b2f3e0c8d0ba585542aa7907b`。
- RED：job `2382347.opbs`，compute host `mg0067`，4/4 specific assertions failed as expected，raw test exit 1，scheduler exit 0。
- corrected GREEN：job `2382536.opbs`，compute host `mg0005`，9 tests passed，含精确 `2^53` counterexample 与 loop-card-derived map completeness。
- corrected HARDEN：job `2382541.opbs`，compute host `mg0005`，同一 resolved config、新 nonce/run ID，9 tests passed。
- corrected full regression：job `2382555.opbs`，compute host `mg0015`，S0C-01 + S0C-02 共 18 tests passed；wrong stale formula、weighting、selection/consumption、direct merge/current application 与 Nesterov 组合回归成立。
- scheduler/manifest consistency：`qstat -f -H` 独立确认四个 closure jobs 均为 finished/`Exit_status=0`，submit host `miyabi-g1`、group `xg24i002`、单一 compute host、run ID/test target/nonce/config digest 与 manifest 一致；GPU utilization/power/memory 均为 0，符合 CPU oracle。
- environment：closure packages 保存 clean branch status、checked commit、nodefile、modules（`nvidia/25.9`、`nv-hpcx/25.9`）、Python 3.13.13、source hashes与 skill identity；当前 skill checkout 与 manifests 均为 `UnbearableFate/miyabi-development@ad1fd34a9de976b4fb26ba47d9a1770430884765`。`bash -n pbs/stage0_cpu_tests.pbs` 通过。
- Checker 自身：初始 host `miyabi-g1`，无 PBS allocation；仅执行 Git/file/hash/shell/PBS-history 静态检查，未运行项目 Python。
- loss/runtime/global cycles/local steps/fault/perf：纯 Stage 0 mathematical oracle 不适用；没有训练或性能 claim。

## Acceptance 判定

| ID | 证据 | 结论 |
|---|---|---|
| `ORACLE-03` / `INV-05` / `PROP-06` | shared decision vectors、全排列重放、精确 effective-token ordering、same-base/repeated polling、publication-conditional commit、1000-update frontier | `PASS` |
| `ORACLE-04` / `OPT-01` / `OPT-02` / `OPT-04` / `OPT-05` | fresh direct-average control、mixed stale/fresh base-relative merge/current application、两步 Nesterov buffer/parameter trace | `PASS` |
| `ORACLE-05` | loop-card-derived required set、CSV mappings、mutation-sensitive completeness checker | `PASS` |
| `A-ALG-00` | S0C-01 supporting evidence + corrected S0C-02 GREEN/HARDEN + 18-test full regression | `PASS` |

## Follow-ups

无影响当前关闭的 follow-up。CHECK 之后仍须按正常 PERSIST 顺序记录本报告、evidence index、loop/progress/acceptance-traceability 状态；该记账不改变本 Checker 对 checked commit 与 immutable evidence 的 verdict。

## 签署

- Checker session/date：independent Checker `/root/s0c02_rechecker` / 2026-07-14 Asia/Tokyo
- checked commit：`a470dcdb32a6c4b8a6e2e6f756903946ef36aefc`
- report hash：由后续 PERSIST 在本文件落盘后外部记录；不可自嵌入。
