# S1-07 Independent Checker Report

## Context and verdict

- Checker context: `/root/s1_07_checker`
- Maker chat inherited: no
- Checked candidate: `d267052d08d4e88537e7cd8c5e9a14a13a8d5ae4`
- Selected package: `runtime_runs/S1-07/formal-l2`
- Scheduler supplement: `runtime_runs/S1-07/scheduler-2385625`
- Phase A: `BLOCKED` for `5554d6e`, then `ADMISSIBLE` for `d267052d`
- Phase B: `PASS`

The same independent Checker context performed both phases. It did not submit
the formal job, modify implementation or evidence, or create another Checker.

## Phase A correction history

Candidate `5554d6e` was blocked because publication telemetry grew with the
complete proposal history, required mutation/in-flight/failure counterexamples
were incomplete, the formal analyzer recorded several close predicates without
failing on them, and scratch/scheduler/role identity checks were incomplete.

Candidate `d267052d` bounds resident publisher traces to three records per
fragment and streams terminal evidence; proves the bound across 1,001 captures;
adds gradient-time rejection, bytewise snapshot immutability, old-base
in-flight/new-base pending coexistence, restart reservation, replacement/skip,
and injected failure/abandonment fixtures; makes the analyzer fail closed; and
binds scheduler, scratch, rank, role, job/run, config, and manifest identities.
After corrected L1 evidence reported 31 passes, Phase A authorized one L2
submission.

## Final package audit

- PBS job `2385625.opbs` exited 0 with no abnormal nodes after 41 seconds.
  The writer ran on `mg0017` and the filesystem reader on `mg0018`; only the
  writer used GPU memory.
- PBS qtime `Wed Jul 15 12:33:02 2026` JST independently converts to
  `2026-07-15T03:33:02Z`, matching both manifest timestamps.
- The manifest binds the exact clean candidate, config
  `f4d5e0bb...a8b8`, source hashes, skill commit, absolute paths, scheduler,
  module inventory, and identical declared/actual roles.
- The package validator reports `admissible`, with 28 listed and 28 actual
  payload files. All 29 checksum entries verify; manifest SHA-256 is
  `5cf0f93c9ba99cb59dd0107d13b389b3020a0bffb3d3eabfb63be2556c141af8`.
- All packaged stderr files and PBS stderr are empty. Requirement validation is
  7/7 complete, the analyzer reports `pass`, and the full Stage 1 suite reports
  150 passed tests.
- Role records and analyzer agree on run `s1-07-2385625.opbs`, model registry
  `c2a36317...8ef9`, fragment map `df9a3625...d652`, and all four final
  proposals. The remote reader completed 273 polls with no partial/corrupt
  proposal and no latest-sequence regression.

## Complete snapshot chain

Fragment 2, sequence 5 provides a complete independently checked chain:

- offset 3 makes safe step 19 due under `H=4`;
- safe-boundary time `1813894339145040` precedes staging
  `1813894339180177..1813894339863861`;
- local fragment steps are 19 and processed input tokens are 608;
- base version is 0 and base content identity is
  `ecd1bbc1f2e0b06e5f20806bbe36d12bdfed5e3e2a3fb8fbdb0d1c8db7957e60`,
  exactly matching the bootstrapped fragment state;
- the target-only fp32 payload is 19,488 bytes with SHA-256
  `3a422c666ca28586a2b904075a385ace5a275a7b79eb739ded1cf4e5b4227d66`;
- publication runs from `1813894340146038` through `1813894346230296`;
- proposal content identity
  `22523302612a24e85f82ea65cc8c98b0244afb6ef6b52f3c2eb4dc70e9a68a95`
  and every preceding field match the writer latest, analyzer chain, and remote
  reader latest exactly.

The analyzer performs the same reconciliation for all four fragments and all
20 terminal capture traces. Seventeen snapshots publish, three pending
snapshots are replaced, every per-fragment maximum is one pending and one
in-flight, and final resident trace state is 4 of the fixed 12-record bound.

## Learner non-wait check

The deliberately gated fragment-0 publication starts at
`1813893924944044`. Training returns at `1813894364687457` while that proposal
is still in flight. The writer releases the gate only afterward at
`1813894364760897`, and publication completes at `1813894369070265`.
Therefore inner training completed while filesystem publication remained
blocked. Writer code and role evidence contain no reader, syncer, quorum, or
application-level MPI wait; MPI is launcher-only and application coordination
is through shared Lustre.

## Requirement judgment

All seven S1-07 close conditions pass:

- `LEARN-03`: byte-weighted offsets `[1,2,3,0]` spread exactly one fragment
  over each `H=4` step; unequal-byte and future nonuniform-interval fixtures
  pass.
- `LEARN-04`: steps 1–20 are strict completed safe boundaries; active-gradient
  capture fails before reservation, and staged bytes remain immutable across
  later parameter mutation.
- `LEARN-05`: only target-fragment staging blocks the safe boundary; the gated
  background publication does not block training or introduce reader/quorum
  dependence.
- `LEARN-06`: one in-flight plus one latest-wins pending slot is enforced;
  replacement, skip, bounded resident state, failure, abandonment, and drain
  error propagation fixtures pass.
- `LEARN-07`: every terminal payload is complete fp32 target-fragment data and
  is bound to the exact descriptor, bootstrap base, local progress, sequence,
  byte count, and checksum.
- `LEARN-11`: a gated old-base in-flight proposal remains immutable while a
  new-base proposal becomes pending and subsequently publishes with the new
  base.
- `PROG-06`: exact formal traffic is `90432 / 4 = 22608` bytes per learner
  optimizer step, and the same API reports exact
  `sum_f(fragment_bytes_f / H_f)` terms for nonuniform intervals.

## Scope boundaries

This result permits Stage 1's finite synchronous target-fragment staging; full
transfer overlap and performance acceptance remain Stage 2. Global-fragment
adoption is S1-08, and multi-learner syncer integration plus the first 8+1 gate
remain later Stage 1 work.

## Final verdict

`PASS`
