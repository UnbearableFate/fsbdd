# Checker Report：S0A-02/311c81a

## Verdict

`PASS`

The corrected result closes the two blockers in the previous independent report. The production PBS entrypoint actually reused the same `RUN_ID` across a planned interruption: job `2382910.opbs` wrote 25 rows to each of four shards and exited with the planned status `75`; job `2382918.opbs` resumed those checkpoints, retained only rows bound to the same generator commit/digest and valid row/metric identities, and completed all 7,776 rows with exit `0`. The full SIM-03 matrix, SIM-04 metrics, recovery/profile calculations, SIM-05 use boundary, nominal-fastest `1.0s/step` delay convention, and HARDEN results were independently checked without executing project Python on the login node.

## 独立输入

- source hashes：`RESEARCH_PLAN.md = 2c0db3148110da10ea62f445c189d2d53b405527d8da264b9af9817f5ca9d835`；`STAGE0-4_SPEC.md = fad26a8add51e70ddf50d0130b61d80fe06e1c3945221c29508412a5bcab2feb`。
- loop card：`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/loops/stage0/S0A-02.md`，SHA-256 `d08c1b33023770c240f7b56847ae014966900abdd9747b6d1e7cd955040cb5a3`。
- checker template：SHA-256 `0cf29aa65c6a0d691cdb2661e5cacc3f2beb1619d729e98add082e36539ae297`。
- unresolved plan config：`plans/FS_DILOCO_CODEX_EXECUTION_PLAN/configs/stage0_simulation_scan.yaml`，SHA-256 `e176201d75fc95b0e7754174db341da9991954a25d65cebc7df6bac85e684e56`；corrected resolved config：`configs/stage0/simulation_sweep.json`，SHA-256 `4cecef2ba0632e8f6d786b59e115eb08a251ff46669544815ac57e6c39db1d66`。
- previous blocked report：`reports/checkers/S0A-02-blocked-1.md`，SHA-256 `9e966dcc96aaa8da07dcd5b26e2473b9dcdfcca0168b96973894753b470ee962`。
- checked ancestry：base `dd21542401ef1ea66392ea18e2eb702fe6a1244c` → original RED `081f6ed19693ef614a5e3ae233bf47bc7fa7ebbe` → previous checked result `e3b7613359adbe0dd490793404d8ca61e5a6b593` → resume RED `c92ee9806c0cadd0bec4c7f6aa41b2e8057c3be8` → corrected generator `0500bd5897b960d7e2aa3ecc0b8df963021cc7ad` → checked/published result `311c81aa2eaa264adc7ce256060627ea321443ba`。`0500bd5` is an ancestor of `311c81a`; `git diff --check dd215424..311c81a` passes.
- original matrix RED：`evidence/raw/S0A-02/20260714T142600Z-red-n6c4e-2bf344f3d830`。
- resume RED：`evidence/raw/S0A-02/20260714T145200Z-red2-n2f6d-29ab6a13979a`。
- planned-interruption + resumed full GREEN：`evidence/raw/S0A-02/20260714T145700Z-green3-resume-n7b4a-14126dde2f00`。
- corrected HARDEN：`evidence/raw/S0A-02/20260714T150300Z-harden2-n4c8f-028bdf299842`。
- 9N package：不适用；Stage 0 and `CAP_BASE_TRAINING` is not established.

## 重新构建的目标

- 单一目标：以可并行、可恢复的 runner 覆盖 SIM-03 全矩阵，输出 token-weighted discard、accepted-token efficiency、fragment update intervals、stale acceptance and `S_max=1` versus `S_max=0` recovery，and freeze Stage 1/4/5 recommendations.
- 受影响条款：`SIM-03`、`SIM-04`、`SIM-05`；Acceptance `A-SIM-02`、`A-SIM-03`。
- matrix：`M={4,8,16}`、`Q/M={0.5,0.75,1}`、heterogeneity `{1,1.5,2,3}`、grace/H `{0,0.1,0.25}`、visibility `{0,1,5,30}s`，extended by `S_max={0,1,2}`、two speed models and three seeds；fixed `F=4`、`H=50`、`lambda_s=1`、`Q_fresh=1`。
- execution boundary：Checker ran on `miyabi-g1` as the login/control plane. Only Git, shell, CSV/JSON, SHA-256, Ruby static recomputation and `qstat` inspection were used; no project Python, pytest, Torch, simulator or training ran on the login node.

## RED 有效性

- Original RED job `2382753.opbs` on `mg0034` ran three deterministic surrogates and exited `1`: incomplete visibility produced `5,832 != 7,776`, the report omitted interval/recovery fields, and the wrong symmetric interval produced `200 != 50`。The failures directly cover SIM-03 cardinality and SIM-04 report/sanity requirements and ran in `0.002s`.
- Resume RED job `2382848.opbs` on `mg0016` ran three deterministic surrogates and exited `1`: unconditional rejection of an existing checkpoint, acceptance of a row from another generator, and acceptance of a corrupted efficiency metric. All three failures directly reproduce the prior Checker blockers and ran in `0.002s`.
- Neither RED is a timeout, import failure or environmental false negative. Their committed input snapshots reproduce the recorded Git blobs.

## 代码与协议检查

- scope/data plane：the loop changes only Stage 0 sweep config/runner/PBS/tests/reports/control-plane records. It does not change learner/syncer production semantics, runtime correctness paths, Torch/HF versions or network data plane.
- entrypoint identity：new runs require a digest-bearing `RUN_ID`; resume requires `RESUME_MODE=1`, an existing package, the same run ID, nonce, code commit and config digest, a reproducible stored digest preimage, matching frozen input checksums, and byte-identical committed snapshots. Accidental overwrite and resume into a missing package are rejected.
- retained-row identity：every row carries `generator_commit=0500bd5897b960d7e2aa3ecc0b8df963021cc7ad`, `generator_digest=14126dde2f007f906351b4217813a1928f17e1cbadb9b7257a3d91075867cc2e`, canonical simulation `config_digest`, canonical matrix `row_key`, and an SHA-256 `row_digest` over every CSV field except itself. Full static recomputation of all 7,776 bindings passed.
- retained-row validation：resume and aggregation validate schema/index, shard modulo, frozen row key/spec fields, simulation config digest, generator commit/digest, row digest, finite metrics, token units, accepted/discard/stale ratios, delay/H, interval count/mean/buckets, `global_cycle=min(update_counts)`, and `M*F` state bounds. The focused GREEN/HARDEN test also demonstrates rejection of different-generator and corrupted-metric partial rows.
- checkpoint semantics：each shard atomically checkpoints every 25 newly generated rows. Existing validated indexes are skipped; the resumed job therefore continues rather than recomputing the retained prefix. The aggregate path repeats retained-row validation before publishing.
- nominal delay convention：the resolved config requires `nominal_fastest_step_seconds=1.0`; `visibility_delay_over_h = visibility_seconds / (H_steps * nominal_fastest_step_seconds)`. The recommendation explicitly states that real training must recompute the ratio from measured step time.
- SIM-05 boundary：config, summary and recommendation limit simulation to parameter selection, a later Stage 4 measurement anchor and Stage 5 delay points. They state it is neither runtime-correctness nor training-quality evidence and cannot cancel mandatory Stage 4.

## Maker 未列出的反例

1. Re-submit an interrupted package with the same `RUN_ID` but a different checked-out commit, config preimage, nonce or snapshotted input. The entrypoint rejects it before shard execution; it cannot silently mix generations.
2. Put a valid-looking matrix index and row key into the wrong shard, change any metric without updating its row digest, or recompute the digest while leaving a derived efficiency/discard/stale/interval/cycle inconsistency. Shard/aggregate validation rejects each case. A malicious party able to rewrite the evidence and all digests remains outside this non-adversarial evidence model.

## 运行证据

- package checksums：all entries pass `sha256sum -c`; each checksum manifest names exactly all package files except itself. Checksum-manifest SHA-256 values are original RED `96bc9ff3194113b89568316582df6481d130eaa8f6f0c9546bf9fb403f2f1a68`、resume RED `827285f7a2274003e824670558e6559eb06021519a2b4a1f3ff74c1d90676f45`、resumed GREEN `848f83af206a1b9ec2782b4582349028233135f18f710a492fcb42898dab21b7`、HARDEN `f86d11de14846c7eebc53bd023411a4202db4f2052b86a9e0882c619958dbaf4`。
- config digest preimages：independently hash to original RED `2bf344f3d8306b565075729197a2538403ad77f4c3b1c261aedf2535d65af666`、resume RED `29ab6a13979a4229e1ba9488ad3985ba874494af0f57be47180a40152d932987`、resumed GREEN `14126dde2f007f906351b4217813a1928f17e1cbadb9b7257a3d91075867cc2e`、HARDEN `028bdf29984285a50505d298d20bd169b75deac17980a6f67b5c8162a89a8d36`。All 3/3/6/13 frozen snapshots match their recorded commit blobs.
- actual two-job resume：`qstat -f -H` records initial job `2382910.opbs` on `mg0015`, walltime `00:00:02`, exit `75`; all four initial shard outputs report `rows=25`, `expected=1944`, `complete=false`. Completion job `2382918.opbs` reused the same run ID, commit and digest on `mg0015`, walltime `00:04:16`, exit `0`; all four resumed shard outputs report `rows=1944`, `complete=true`, aggregation reports `{complete:true,rows:7776}`, and eight focused tests pass.
- HARDEN：job `2382948.opbs` on `mg0015`, walltime `00:00:02`, exit `0`; all 33 oracle/S0A-01/S0A-02 tests pass, including the resume identity/corruption case, units/state bounds and seed sensitivity.
- raw/manifest publication：four final shards have 1,944 rows each and combine byte-for-field into the sorted 7,776-row raw CSV. The published manifest, summary and recommendation in `reports/stage0/` are byte-identical to resumed immutable evidence and are committed at `311c81a`.
- independent full recomputation：all Cartesian indexes, row keys, simulation config digests, generator identities, row digests, token formulas, delay ratios, interval distributions, cycle minima and bounded-state checks pass. All 2,592 three-seed aggregate rows match independently recomputed mean/sample-stdev/normal-95%-CI/min/max.
- recovery：2,592 paired seed rows give mean `0.038065253125540446` fraction (`3.8065253125540446` pp), sample stdev `0.08904561475518057`, CI half-width `0.003428081270430378`, min `-0.08887310310372826`, max `0.613375`; all match the summary. The 864 profile classifications recompute to `654` ablation/discussion, `110` compressed and `100` full-second-contribution.
- profile selection：rank 1 independently recomputes as `M=8,Q/M=0.5,Q=4,grace=0.1H`, region recovery `0.03362411430894311`, accepted efficiency `0.35827672261088284`, interval `41.20516111281898`, with 36 raw/paired rows. The fixed Stage 4 anchor recomputes to recovery `-0.0004456725198324074` fraction (`-0.04456725198324074` pp), stale accepted-token rate `0.053107946226540664`, and classification `ablation_or_discussion`.
- analytic sanity：all nine `Q=M`, heterogeneity `1`, grace `0`, visibility `0`, `S_max=0`, constant-speed rows have interval mean `50`, zero discarded tokens and zero stale accepted proposals.
- Stage 0 has no model training, GPU loss, learner local-step or 9-node gate claim; those fields are correctly not applicable.

## Acceptance 判定

| ID | 证据 | 结论 |
|---|---|---|
| `SIM-03` / `A-SIM-02` | exact 7,776-row Cartesian matrix; four bound shards; actual verified checkpoint resume; complete raw/manifest | `PASS` |
| `SIM-04` | independently recomputed token-weighted discard/efficiency/stale rates, interval distributions, 2,592 aggregates and 2,592 paired recoveries | `PASS` |
| `SIM-05` / `A-SIM-03` | frozen Stage 1/4 profiles, Stage 4 anchor/classification, Stage 5 delay points and explicit non-correctness boundary | `PASS` |
| loop objective: parallel resumable runner | two real PBS jobs reused one run ID after 25 rows/shard; retained rows bind generator/config/content and are revalidated | `PASS` |
| `S0A-02` checked result | RED/GREEN/HARDEN and corrected result commit satisfy the loop objective | `PASS` |

## Follow-ups

None affecting this Checker verdict. Per the required `CHECK → PERSIST` sequence, the Maker must now use this report to update `plans/PROGRESS.yaml`, `plans/loop_states/S0A-02.yaml`, `plans/ACCEPTANCE_TRACEABILITY.csv`, create `evidence/indexes/S0A-02.md`, and record the next gap before declaring the loop closed; those downstream control-plane writes are intentionally not part of checked commit `311c81a`.

## 签署

- Checker session/date：fresh independent Checker `/root/s0a02_rechecker` / 2026-07-15 Asia/Tokyo
- initial host/workflow：`miyabi-g1`, Miyabi login/control-plane static-only review
- checked commit：`311c81aa2eaa264adc7ce256060627ea321443ba`
- report SHA-256：由本文件落盘后外部计算；哈希值不能自嵌入被哈希内容。
