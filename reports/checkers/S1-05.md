# S1-05 Independent Checker Report

## Context and verdict

- Checker context: `/root/s1_05_checker`
- Maker chat inherited: no
- Checked candidate: `cf5e0fe036dda0743aa92a15ab3064628bb58df4`
- Selected package: `runtime_runs/S1-05/formal-l2`
- Phase A: `ADMISSIBLE` after one correction cycle
- Phase B: `PASS`

The same independent Checker context performed both phases. It did not edit
production, test, plan, or evidence files and did not launch the formal job.

## Phase A correction history

The Checker blocked candidate `0ebaad20002f205a4079a7fa1f5aaf296bc80ec1`
before submission for four material reasons:

1. Monotonic publication depended on an `fcntl` lock, contrary to the
   lock-independent `STOR-01` contract.
2. The consumed frontier was keyed by learner but not bound to the run,
   fragment map, and fragment descriptor being selected.
3. The clean code candidate and formal code identity were not yet frozen.
4. The `DISC-02` formal summary did not expose the decoded compound proposal
   schema or bind its bounded-base component to the checked S1-04 evidence.

The corrected candidate removes filesystem locking, enforces the frozen single
writer/single in-flight invariant by re-reading the latest authority before a
publication completes, binds every frontier to a complete fragment-state
identity, and reports the decoded schema and hash-bound retained-base component.
Independent counterexamples for a delayed rootless task and cross-fragment
frontier reuse then failed closed. Phase A recheck declared the exact clean
candidate admissible and authorized one L2 submission.

## Phase B package audit

- PBS `2385040.opbs`: `Exit_status=0`, no abnormal nodes, 30 seconds walltime.
- All 30 checksum entries verify: manifest plus 29 payload files.
- Validator: `admissible`, 29 listed = 29 actual files.
- Manifest SHA-256:
  `79f82aa846d4240491bf15d57d2700b6dee7739258250a7edc35b850bfdaa16f`.
- PBS stderr and all six package stderr logs are empty.
- Writer rank 0 ran on `mg0009`; discoverer rank 1 ran on `mg0015`. Nodefile,
  role records, summary, and manifest agree, and `/work` is shared Lustre.
- PBS qtime `Wed Jul 15 10:24:03 2026` interpreted as JST converts to
  `2026-07-15T01:24:03Z`, exactly matching scheduler and manifest timestamps.
- Run `s1-05-2385040.opbs`, config `dd1dc08d...a3c50`, model
  `d91a92be...82100`, fragment map `d1fb0964...b2ab`, checked code, and skill
  identities agree across the contract, roles, summary, and manifest.
- Requirement validation is 14/14 complete and the full Stage 1 suite is
  `119 passed`.

The remote discoverer cannot observe a payload-only interrupted proposal and
does observe its completed retry. Discovery performs 32 fixed-slot reads before
and after 10,000 unrelated objects. The same sequence is idempotent with zero
new payloads; conflicting same-sequence content is rejected with zero new
payloads; delayed sequence 2 is rejected after sequence 3 wins. For every one
of four fragments, selection yields eight distinct learners and a repeated poll
yields zero, with eight fixed frontier entries. The formal float32 normalized
weights agree with the oracle within `2e-6`.

## Requirement judgment

All fourteen S1-05 matrix close conditions match the implementation and
selected evidence. The verdict is `PASS` with these scope boundaries:

- `A-PROP-01` and `A-PROP-02` pass here.
- `A-PROP-03` receives component evidence for fragment-bound fixed frontiers
  and repeated polling. Atomic global-state publication and consumption remains
  S1-11, with S1-09 integration.
- Monotonic latest publication relies on the declared one writer and one
  in-flight publication per learner/fragment invariant; no filesystem lock or
  multiwriter arbitration is claimed.
- Formal runtime uses deterministic proposal bytes and `S_max=0`. Positive
  stale policy is covered by the same generic production path and tests, not by
  this cross-node workload.
- S1-05 claims no physical garbage collection, real snapshot generation,
  merge, training, or nine-node gate.

## Final verdict

`PASS`
